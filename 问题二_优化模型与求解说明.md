# 问题二优化模型：三频带物理阶次与文件级极端随机树

## 1. 任务、证据边界与方案

问题二依据问题一提取的源域特征，预测轴承状态 $\mathcal C=(N,OR,IR,B)$，并在独立源文件上评价。题目原文第2问没有规定准确率阈值；**文件级准确率超过95%是用户本轮新增目标**。初版文件—类别双重均衡 RBF-SVM 的23文件测试准确率为19/23（82.61%），见[归档摘要](验收材料/q2/初版RBF结果摘要.json)。优化方案把已由问题一确定的20转窗口和1500—3000 Hz特征保留为基线，增添由SKF6205驱动端轴承几何给出的阶次能量，并在500—1500、1500—3000、3000—5000 Hz解调。窗口只形成文件内中位数；每个原始文件是一个训练或测试单位。

在开发过程中初稿测试集曾被查看，且两种滚动体频率口径的结构比较发生在本轮探索。因此，即使观察测试准确率超过95%，也不能称为全新盲测的独立泛化证明。数值拟合和网格选择都只读训练/验证标签；对已见测试集的结构选择风险在结果报告中单列。

## 2. 符号与已知量

以 $i$ 表示独立源文件，$j$ 表示文件内窗口，$c\in\mathcal C$ 为状态，$b\in\mathcal B$ 为共振频带，$k$ 为特征，$t$ 为树编号。源域共有116文件，固定训练/验证/测试集合 $\mathcal I_{tr},\mathcal I_{va},\mathcal I_{te}$ 分别有70、23、23个文件，$\mathcal I_{dev}=\mathcal I_{tr}\cup\mathcal I_{va}$ 有93个。$x_i[n]$ 是去均值后的DE原始采样信号，$f_{s,i}$ 是源文件采样率，$r_i$ 是清单给出的转速rpm。题面SKF6205 DE轴承的滚动体数 $n_d=9$、滚动体直径 $d=0.3126$ 英寸、节径 $D=1.537$ 英寸；题面频率式未列接触角，按其写法取 $\cos\alpha=1$。$\mathcal B=\{[500,1500],[1500,3000],[3000,5000]\}$ Hz 是问题一既有候选频带，不是从测试错误反推的新频带。$\epsilon=10^{-12}$ 是数值分母防零量，$R=20$ 转，窗口边界与包络谱配置沿用问题一。

## 3. 场景抽象、变量定义与核心推导

源文件的转频为

$$f_{r,i}=r_i/60.\tag{2.31}$$

令 $\rho=d/D$，按照原题表2定义三个故障特征阶次

$$o_{OR}=\frac{n_d}{2}(1-\rho),\qquad o_{IR}=\frac{n_d}{2}(1+\rho).\tag{2.32}$$

$$o_{B}^{\text{题面}}=\frac{D}{d}(1-\rho^2),\qquad o_{F}=\frac12(1-\rho).\tag{2.33}$$

$o_F$ 是保持架频率阶次，用作与故障节律的对照。**滚动体自旋半值**另记为

$$o_{B}^{\text{自旋半值}}=\frac12o_B^{\text{题面}}.\tag{2.34}$$

式(2.33)忠实于题面BSF，式(2.34)是另一个明确标名的候选量；两者不能用同一个“BSF”名称。四类阶次乘 $f_{r,i}$ 才得到该文件以Hz为单位的频率。候选家族为原12特征、题面单频带、题面三频带、自旋半值三频带、两种滚动体频率共同使用的三频带。不能把从文件路径读出的类别、故障尺寸和转速数值直接作为分类特征；转速只用于物理坐标换算。

对每个 $b=[L_b,U_b]$ 用问题一既定对称FIR、反射边界和Hilbert包络处理原信号：

$$u_{ib}[n]=(h_{b,f_{s,i}}*x_i)[n],\qquad e_{ib}[n]=|\mathcal H\{u_{ib}\}[n]|.\tag{2.35}$$

其中 $h_{b,f_{s,i}}$ 按该文件原始采样率设计，$\mathcal H$ 表示解析信号，边界截取后包络降至2000 Hz。窗长和完整窗数分别为

$$L_i=\operatorname{round}\!\left(2000\frac{R}{f_{r,i}}\right),\quad M_i=\left\lfloor\frac{|e_{ib}|-2g}{L_i}\right\rfloor,\quad g=\lceil0.05\times2000\rceil.\tag{2.36}$$

同一文件三个频带共用窗界 $[g+jL_i,g+(j+1)L_i)$，原始信号按采样率比例映回相同时间段。对窗内包络去均值、乘对称Hann窗、无补零FFT，在线性频率与阶次的映射 $f=o f_{r,i}$ 上插值，得到非负阶次谱 $P_{ijb}(o)$，再归一化：

$$p_{ijb}(o)=\frac{P_{ijb}(o)}{\sum_{o'\in\Omega}P_{ijb}(o')},\quad \Omega=\{0.2,0.3,\ldots,20.0\}.\tag{2.37}$$

这里 $p$ 是包络阶次谱质量，不是诊断概率。给定阶次 $o_c$ 及半宽 $\delta\in\{0.1,0.2,0.3\}$，定义可用谐波集合

$$H_c=\{mo_c:m=1,\ldots,\lfloor20/o_c\rfloor\}.\tag{2.38}$$

谐波邻域并集和质量为

$$A_{c,\delta}=\{o\in\Omega:\min_{h\in H_c}|o-h|\le\delta\},\quad E_{ijbc\delta}=\sum_{o\in A_{c,\delta}}p_{ijb}(o).\tag{2.39}$$

并集保证同一谱格点不重复计入多个邻域。基频邻域质量与峰值分别为

$$F_{ijbc}=\sum_{|o-o_c|\le0.2}p_{ijb}(o),\qquad Q_{ijbc}=\max_{|o-o_c|\le0.2}p_{ijb}(o).\tag{2.40}$$

对OR、IR及两种滚动体频率分别构造谐波局部背景 $G_c=A_{c,0.6}\setminus A_{c,0.2}$，对比度为

$$C_{ijbc}=\frac{|A_{c,0.2}|^{-1}\sum_{o\in A_{c,0.2}}p_{ijb}(o)}{|G_c|^{-1}\sum_{o\in G_c}p_{ijb}(o)+\epsilon}.\tag{2.41}$$

$G_c$ 为空时停止而不填造一个分数；保持架阶次谐波太密，不计算此对比度。包络均值 $\bar e$、去均值RMS $s_e$ 与原始窗RMS $s_x$ 给出三个形状/传递特征：

$$V_{ijb}=\frac{s_e}{\bar e+\epsilon},\quad K_{ijb}=\frac1{L_i}\sum_n\left(\frac{e_{ijb}[n]-\bar e}{s_e+\epsilon}\right)^4,\quad T_{ijb}=\frac{\operatorname{RMS}(u_{ijb})}{s_x+\epsilon}.\tag{2.42}$$

再拼接问题一原有12项时域和包络统计特征，组成窗口向量 $z_{ij}$。文件特征 $v_{ik}$ 只由其自身 $M_i$ 个相关窗口构造：

$$v_{ik}=\operatorname{median}_{0\le j<M_i} z_{ijk}.\tag{2.43}$$

在某候选特征家族 $\mathcal F$ 中，训练集合是 $\{(v_i,y_i):i\in\mathcal I_{tr}\}$；每棵极端随机树 $T_t$ 使用随机候选列和随机切点，叶节点不纯度使用Gini：

$$G(A)=1-\sum_{c\in\mathcal C}\left(\frac{\sum_{i\in A}w_i\mathbf1(y_i=c)}{\sum_{i\in A}w_i}\right)^2.\tag{2.44}$$

树的类别平衡权重由训练折文件数计算，$N_c=|\{i:y_i=c\}|$，$N=\sum_cN_c$：

$$w_i=\frac{N}{4N_{y_i}}.\tag{2.45}$$

它不会把N类的少量文件复制成更多独立记录。节点选择随机候选划分并使加权子节点Gini最小；最多使用 $m$ 个候选特征，叶节点至少 $\ell$ 个文件。每棵树叶节点的四类比例 $q_{tc}(v)$ 产生集成分数：

$$s_c(v)=\frac1{500}\sum_{t=1}^{500}q_{tc}(v),\qquad \hat y_i=\mathcal C_{\arg\max_c s_c(v_i)}.\tag{2.46}$$

分数未作校准，不视为真实后验概率；平局由 $(N,OR,IR,B)$ 顺序决定。分数间隔仅定义为分数差

$$\Delta_i=s_{(1)}(v_i)-s_{(2)}(v_i),\tag{2.47}$$

其中 $s_{(1)},s_{(2)}$ 是最大的两个类别分数。定义文件级混淆计数及准确率：

$$M_{cd}=\sum_{i\in\mathcal I_{te}}\mathbf1(y_i=c,\hat y_i=d),\quad A=\frac{\sum_cM_{cc}}{|\mathcal I_{te}|}.\tag{2.48}$$

逐类查准率、召回率、F1 与宏指标为

$$P_c=\frac{M_{cc}}{\sum_aM_{ac}},\quad R_c=\frac{M_{cc}}{\sum_bM_{cb}},\quad F_{1,c}=\frac{2P_cR_c}{P_c+R_c}.\tag{2.49}$$

$$F_1^{macro}=\frac14\sum_cF_{1,c},\qquad BA=\frac14\sum_cR_c.\tag{2.50}$$

分母为0时该比值定义为0。这里的 $BA$ 是平衡准确率，不是基座BA振动通道。

## 4. 数据适配、求解迭代与结果验证

第一阶段只在70训练文件上，对每个特征家族用5个固定种子 $(11,29,42,71,97)$ 的文件级分层二折搜索 $\ell\in\{1,2,3\}$、$m\in\{\sqrt p,0.25p,0.5p,0.75p,p\}$；其中 $p=|\mathcal F|$。每种参数的重复准确率均值定义为

$$\bar A(\ell,m)=\frac15\sum_{r=1}^5A_r(\ell,m),\tag{2.51}$$

先最大化 $\bar A$，再按宏F1、平衡准确率、最低一次准确率、较小叶节点和配置顺序破平局。每个家族以所选参数在70文件拟合，并在23验证文件分别评价。验证准确率、宏F1、平衡准确率、N召回依次决胜；若全部相同，配置中明确给 `multiband_spin_half` 优先。此破平局规则是在本轮已查看旧测试集的背景下冻结，结果解释应包含这一事实。

第二阶段只用已选特征家族，把训练和验证合成93个开发文件，用同样网格和5种种子做分层三折重调并重训500棵树。23个原有测试文件只作固定版本的文件级观察，不参与数值拟合。额外用所有116源文件在冻结参数上作5组分层三折审计；由于特征家族已在开发过程中优化，不能将其作为嵌套无偏验证。

幅值增益和白噪声试验都回到原始MAT重跑式(2.35)—(2.43)。白噪声从iid标准正态有限向量 $\xi_i[n]$ 生成，精确按该有限向量的RMS定幅：

$$x_i^{(\eta)}[n]=x_i[n]+\eta\operatorname{RMS}(x_i)\frac{\xi_i[n]}{\operatorname{RMS}(\xi_i)},\quad \eta\in\{0,0.01,0.05,0.1\}.\tag{2.52}$$

噪声种子是固定种子、文件ID和扰动水平的SHA-256映射；对 $\eta=0$ 的重算特征与原特征逐项比较。增益水平 $a\in\{0.5,1,2\}$ 对原始波形作 $x_i^{(a)}=a x_i$，而不是只修改选中特征。预测保持率为

$$H(a)=\frac1{23}\sum_{i\in\mathcal I_{te}}\mathbf1\{\hat y_i^{(a)}=\hat y_i\}.\tag{2.53}$$

程序输出每文件特征、分数和标签、交叉验证、混淆矩阵、鲁棒性结果；树的左右子节点、分裂特征与阈值和叶分布导出JSON/NPZ，可独立重建式(2.46)。14幅图各存300DPI PNG及可编辑SVG。问题三使用此源分类器时，目标轴承的几何和转速需要重新确认；SKF6205源域几何不能自动当成列车目标轴承的已知条件。
