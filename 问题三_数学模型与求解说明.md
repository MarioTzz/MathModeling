# 问题三：无标签列车轴承的保守迁移诊断模型

## 1. 任务、数据和模型取舍

问题三要求利用问题二的源域诊断知识，为目标域 A—P 的 16 个无标签文件分别给出 N、OR、IR、B 工作状态，并展示和分析迁移结果。源域仍取问题一确定的 116 个 SKF6205 驱动端文件，按既有 70/23/23 文件级划分；目标域是 32 kHz、每段 8 s 的单通道信号，题面转速约 600 rpm。目标轴承几何、传感器标定和真实类别没有提供，文件名中的 B、N 均只是编号。

问题二的 90 维主模型使用 SKF6205 的理论故障阶次；把这组几何参数直接套在目标轴承上没有依据。因此本问保留**问题二的文件级极端随机树分类结构**，在两域共同可计算的 9 个无量纲变量上重新拟合。问题一的 20 转、1500—3000 Hz 包络特征表是输入；前三个原始幅值量在目标域已全部超出源训练文件的 5%—95% 范围，且两域幅值物理单位未标定，故不进入迁移分类器。保留峰值因子、Pearson 峭度、偏度及六个包络阶次统计量。

主线为**机理分块、收缩协方差的部分 CORAL 对齐**，其中对齐强度可以由源域代理任务选为零；零强度仍是使用共同物理表征与源域监督分类器的无标签模型迁移基线。CORAL 源于 Sun、Feng、Saenko 的[原始论文](https://ojs.aaai.org/index.php/AAAI/article/view/10306)；本题针对 16 个目标文件把特征限制在小维度机理块，按文件等权并加入收缩和回退强度。协方差接近只说明统计形态接近，不证明故障标签正确。

## 2. 符号、假设与数学模型

记源域开发文件集为 $S_{dev}$、固定源测试集为 $S_{te}$、无标签目标集为 $T$；分别有 93、23、16 个文件。类别顺序固定为 $\mathcal C=(N,OR,IR,B)$。第 $i$ 文件的转频 $r_i$ 单位为 Hz，源域由文件记录给出，目标域仅用题面近似值 $600/60=10$ Hz。每个源或目标文件构成一个独立分类单位，同文件窗口始终不跨训练与评价集合。

**关键假设 K1：** 四个故障部位类别在两域的任务含义相同；这是题面给出两域共同状态的依据。若目标出现题面四类之外状态，本模型仍会强制四选一，需通过人工核查低支持文件。**关键假设 K2：** 目标域约 600 rpm 足以构造近似等转数阶次特征；通过转速上下扰动重算检验。**可放宽假设 F1：** 三个冲击形状量和六个包络量可以作为两域共享描述，但其分布无需相同；通过代理迁移和域差异检查检验。**可放宽假设 F2：** 16 个目标文件的等权均值与收缩协方差可描述这一批目标文件的统计形态；通过目标留一重估检验。模型不假定目标四类各有四份，也不假定目标几何等于源域轴承。

第 $i$ 文件按问题一的 2000 Hz 包络采样率、边缘保护 $G_0=100$ 点切成 20 转完整窗口，窗口长度和数目为

$$L_i=\operatorname{round}\left(\frac{2000\times20}{r_i}\right),\qquad m_i=\left\lfloor\frac{|e_i|-2G_0}{L_i}\right\rfloor.\tag{3.01}$$

令 $x_{ij,k}$ 为文件 $i$ 第 $j$ 窗的第 $k$ 项无量纲特征；其物理定义、包络滤波和阶次谱计算沿用问题一式(1.04)—(1.23)。分类特征集合固定为

$$\mathcal F=(\text{crest},\text{kurtosis},\text{skewness},\text{entropy},\text{centroid},\text{spread},\text{peak},\text{harmonic},\text{low\_order}).\tag{3.02}$$

式(3.02)依次对应问题一特征表的实际列名 `crest_factor`、`kurtosis_pearson`、`skewness`、`env_entropy`、`env_centroid_order`、`env_spread_order`、`env_peak_order`、`env_harmonic_ratio`、`env_low_order_ratio`；程序只按这些列名读取，不从文件路径推测特征。

每文件每项取窗口中位数，避免记录长短改变文件权重：

$$v_{ik}=\operatorname{median}_{0\le j<m_i}x_{ij,k},\quad k\in\mathcal F.\tag{3.03}$$

在每次代理任务中，仅以其**拟合侧的有标签源文件**计算各特征的训练中位数 $a_k$ 和四分位距 $b_k$；最终模型仅以 $S_{dev}$ 计算。零四分位距用 1 作数值尺度并单独记录；零 IQR 不必然意味着该特征在所有文件恒定：

$$a_k=\operatorname{median}_{i\in S}v_{ik},\quad b_k=Q_{0.75,S}(v_{\cdot k})-Q_{0.25,S}(v_{\cdot k}),\quad d_k=\begin{cases}b_k,&b_k>0,\\1,&b_k=0.\end{cases}\tag{3.04}$$

$$z_{ik}=\frac{v_{ik}-a_k}{d_k}.\tag{3.05}$$

按物理含义分为冲击形状块 $G_1$（前三项）与包络阶次块 $G_2$（后六项），块维数 $d_1=3,d_2=6$。在某次对齐的拟合侧源文件 $S$ 和无标签对齐侧文件 $U$ 上，每文件一行计算块均值和经验协方差：

$$\mu_{D,g}=\frac1{n_D}\sum_{i\in D}z_{i,g},\qquad C_{D,g}=\frac1{n_D}\sum_{i\in D}(z_{i,g}-\mu_{D,g})^\mathsf T(z_{i,g}-\mu_{D,g}),\quad D\in\{S,U\}.\tag{3.06}$$

收缩率 $\rho_{D,g}$ 由该块文件数据的 Ledoit–Wolf 估计得到，不用目标标签拟合。设 $\epsilon_{D,g}=10^{-8}\max\{1,\operatorname{tr}(C_{D,g})/d_g\}$，构造正定矩阵

$$\widehat C_{D,g}=(1-\rho_{D,g})C_{D,g}+\rho_{D,g}\frac{\operatorname{tr}(C_{D,g})}{d_g}I_{d_g}+\epsilon_{D,g}I_{d_g}.\tag{3.07}$$

对正定矩阵 $C=Q\operatorname{diag}(\lambda_1,\ldots,\lambda_d)Q^\mathsf T$，定义

$$C^{1/2}=Q\operatorname{diag}(\sqrt{\lambda_1},\ldots,\sqrt{\lambda_d})Q^\mathsf T,\quad C^{-1/2}=Q\operatorname{diag}(\lambda_1^{-1/2},\ldots,\lambda_d^{-1/2})Q^\mathsf T.\tag{3.08}$$

源域向目标域的块映射及部分对齐为

$$A_g=\widehat C_{S,g}^{-1/2}\widehat C_{U,g}^{1/2},\qquad h_{\alpha,g}(z)=(1-\alpha)z+\alpha\big[(z-\mu_{S,g})A_g+\mu_{U,g}\big].\tag{3.09}$$

两个块使用同一个 $\alpha\in\mathcal A=\{0,0.25,0.5,1\}$，拼接 $h_\alpha(z_{i,G_1}),h_\alpha(z_{i,G_2})$ 作为源训练输入；对齐侧文件使用原训练尺度 $z_i$。$\alpha=0$ 保留不对齐模型。因收缩及部分对齐，式(3.09)不声称经验协方差完全相等。

文件级极端随机树沿用问题二的 500 棵树、`max_features=sqrt`、最小叶样本 1 和随机种子 42。若当前拟合集合有 $K$ 类、类别 $c$ 有 $N_c$ 个文件，其类权重和树分数为

$$w_c=\frac{|S|}{K N_c},\qquad s_c(z)=\frac1{500}\sum_{t=1}^{500}q_{t,c}(z),\tag{3.10}$$

其中 $q_{t,c}$ 是第 $t$ 棵树的叶类别比例，$s_c$ 是分类分数而非经过校准的正确概率。代理任务只含 OR、IR、B 三类；正式模型为四类。

## 3. 无标签迁移参数的选择与最终预测

选择强度时，在既有源域划分上构造两项跨采样条件代理任务：12 kHz 的 37 个故障**训练**文件拟合，48 kHz 的 9 个故障**验证**文件评价；反向用 48 kHz 的 31 个故障训练文件拟合，12 kHz 的 13 个故障验证文件评价。每次仅使用评价侧**无标签特征**求式(3.06)—(3.09)，其标签只在预测完成后按固定类别顺序 `(OR, IR, B)` 计算文件级三类宏 F1。正常文件只存在于 48 kHz 正常组，不能纳入这两项三类代理评价。记方向 $e\in\{12\to48,48\to12\}$ 的宏 F1 为 $F_e(\alpha)$，则

$$\alpha^*=\underset{\alpha\in\mathcal A}{\operatorname{lexargmax}}\left(\min_e F_e(\alpha),\;\frac12\sum_eF_e(\alpha),\;-\alpha\right).\tag{3.11}$$

词典序先重视较差方向的表现，完全平局选较小对齐强度。评价侧标签不用于缩放、协方差或拟合，只用于统一选择全局强度。问题一的频带/窗长在源域 70 个训练文件上确定，没有使用这 22 个代理验证文件的标签；但本问的九维特征取舍已查看过原验证集表现，因此代理分数属于开发阶段的回看，不称独立外部迁移验证。代理条件也只覆盖源域的采样条件差异，不等价于实际列车转速、轴承和传感器差异。

冻结 $\alpha^*$ 后，用 93 个源开发文件拟合训练尺度、源/目标块统计量及四类分类器，对目标 16 文件输出分数、标签和分数间隔：

$$\widehat y_i=\underset{c\in\mathcal C}{\operatorname{argmax}}\ s_c(z_i),\qquad \Delta_i=s_{i,(1)}-s_{i,(2)},\quad i\in T.\tag{3.12}$$

$s_{i,(1)}$ 与 $s_{i,(2)}$ 分别是该文件最大和次大分数；$\Delta_i$ 只表示模型内部分离程度，不是正确概率。给 A—P 全部文件输出标签，不按目标文件名或预设类别数量分配结果。

用目标留一重估量化对齐对 16 文件统计量的依赖：对每个 $i\in T$，剔除它重估 $\mu_{U,g},\widehat C_{U,g}$，由新映射重新变换全部源训练文件并拟合分类器，再预测该剔除文件，记

$$u_i^{\mathrm{LOO}}=\mathbf1\{\widehat y_i^{(-i)}=\widehat y_i\},\qquad \overline u^{\mathrm{LOO}}=\frac1{16}\sum_{i\in T}u_i^{\mathrm{LOO}}.\tag{3.13}$$

另外把目标转速近似值统一改为 540 和 660 rpm，从原 MAT 重新提取目标窗口特征；各情形重估目标域统计量、重新映射源训练特征并拟合分类器，记录标签是否与 600 rpm 基准相同：

$$R_i=\frac12\sum_{u\in\{540,660\}}\mathbf1\{\widehat y_i^{(u)}=\widehat y_i^{(600)}\}.\tag{3.14}$$

源域固定测试集按两条明确定义的路线评价：源基线以 $\alpha=0$ 在 93 个开发文件拟合，直接预测未变换的 23 个源测试文件；对齐模型在 $h_{\alpha^*}(z_{S_{dev}})$ 上拟合，并以**同一已冻结映射** $h_{\alpha^*}(z_{S_{te}})$ 预测源测试文件。这仅检查源域性能保持，并非列车目标准确率。目标文件还分别由零强度源基线与正式对齐模型预测，两路标签是否相同就是“模型分支分歧”。目标域真实标签未知，**不能计算目标准确率**。同时报告代理两方向结果、源测试、目标留一稳定性、转速扰动及分支分歧。即使部分 CORAL 降低式(3.06)的块协方差差异，也只把它解释为分布对齐，不替代代理分类表现。

## 4. 求解顺序、数据适配与回退

程序按以下顺序实现：读取并核对问题一窗口表与文件清单；按式(3.02)—(3.03)形成文件级九维表；在上述 37→9 与 31→13 代理方向计算式(3.04)—(3.10)并选 $\alpha^*$；以 93 个开发源文件和 16 个无标签目标文件重估正式模型；按源基线和同映射对齐模型两路评价已见源测试文件，并给 A—P 输出逐文件分数；做目标留一与转速扰动；生成差异、预测和稳定性图表并导出复现清单。目标文件的 `label`、`label_code`、文件字母、约 600 rpm 数值以及源域采样组都不作为分类器输入特征。

当前数据只支持给出模型预测和内部稳定性。若代理的最佳强度为 0，则正式结果明确标作“几何无关共同特征的源模型迁移”，不把 CORAL 写成已产生收益；若目标留一或转速扰动使标签变化，逐文件标记待复核。任何目标真实准确率主张都需要另取带真值且未参与模型选择的目标文件。
