# 问题二阶段验收记录

本阶段只交付问题二的源域文件级诊断模型；目标域标签及问题三、四尚未求解。初版双重均衡 RBF 保留为对照，当前主线为三频带轴承阶次特征与极端随机树。原题第 2 问要求建立和评价诊断模型，没有给出 95% 的准确率阈值；95% 是用户本轮新增的文件级验收目标。

## 阶段结论

| 门禁 | 独立复验 | 核验范围 |
|---|---|---|
| M1 | PASS | 原题滚动体故障阶次与自旋半值分列；23 个编号公式、82 个数学片段、模型假设和符号一致性 |
| P1 | PASS | 隔离目录中真实 4+4 文件小样本、90 特征、11 棵树、4/4 留出正确；20 项单测通过；原 88 条树阈值边界反例与调参平局复查 |
| P2 | PASS | 隔离目录全量独立运行、原始输入与生成输出哈希、测试分数及模型导出复算、扰动、图件和链接检查 |

验收者未参与优化模型及数值代码的编写。初轮 P1/P2 曾检出导出树状态的浮点输入类型与 sklearn 推断类型不一致，以及 `max_features` 平局时未按配置次序选取两项阻塞缺陷。修正后，对原 88 条阈值探针的两个导出分数入口均与 sklearn 逐元素相同，最大误差和标签差均为 0；完全平局选择配置首项 `sqrt`。另修正“置信间隔”为“分数间隔”、18 个自旋半值变量的特征归属及“五个特征家族”说明。修正后的 M1/P1/P2 均重新独立验收通过，未遗留 P0/P1/P2 缺陷。

## 冻结结果与复现证据

- 116 个 DE 源域文件按原有文件级划分为训练 70、验证 23、测试 23；目标域文件使用数为 0。最终以 93 个开发文件拟合 500 棵树，采用 20 转窗口、三个 500—5000 Hz 频带和 90 项文件中位数变量。
- 固定测试集正确 22/23，文件级准确率 **0.9565217391**、宏 F1 **0.9653679654**、平衡准确率 **0.9772727273**；唯一误判为 `source/48kHz_DE_data/OR/Centered/0014/OR014@6_0.mat` 的 OR→B。初版 RBF 为 19/23（0.8260869565）。明细见[正式结果](results/q2_optimized/问题二_结果报告.md)与[摘要](results/q2_optimized/summary.json)。
- 5 组全源分层三折补充审计的准确率范围为 0.9569—0.9655、均值 0.9621。增益 0.5 和噪声幅度 0.05 的压力测试各为 21/23；其他指定条件为 22/23。
- 问题一对应的 948 个源窗口及 12 项基础特征与本链路逐值相同；正式测试导出模型分数与 sklearn、零噪声原始信号重算的最大差均为 0。
- [复现清单](results/q2_optimized/复现清单.json)列出 134 项输入（其中 116 个原始 MAT）与 65 项输出，正式目录和独立目录均已检查全部哈希。15 个 CSV、4 个 JSON、1 个 NPZ、2 个 Markdown 数值或文本一致；28 张 PNG 字节相同，14 张 SVG 结构一致。14 幅图的 SVG 可编辑、无内嵌栅格；严格图审退出码均为 0，HTML 图链有效。

独立复验使用本地临时隔离目录 `.analysis-work/q2-review/optimized-p1-fixed` 和 `.analysis-work/q2-review/optimized-p2-fixed`；其中依赖缓存与过程日志不纳入 Git。可从[运行说明](问题二_优化运行与代码说明.md)中的依赖和命令重跑；正式运行入口为 `问题二_求解.py --overwrite`，隔离运行使用 `--output-root`，单测执行 `python3 -m unittest discover -s tests -v`。

## 结果解释边界

**95.65% 是曾被查看的固定 23 文件测试集上的观察准确率。** 旧版模型和本轮结构探索均查看过该测试集，自旋半值候选也在探索期间加入；因此该数字达到本轮观察数据的 95% 门槛，但不构成独立盲测超过 95% 的保证。5 组全源交叉验证同样是结构探索后的补充审计。正常类测试仅 1 文件，类级召回的分辨率有限；若需要对新工况作外部准确率主张，应另取未参与选择的新文件盲测。

按用户约定，本阶段提交 GitHub 后等待问题二确认，再进入下一问。
