# 问题一：等转数包络特征模型、推导与程序合同

## 1. 本阶段解决什么问题

问题一要选择源域数据，分析具有代表性的振动记录，并把入选文件转成后续分类和迁移可用的特征。采用已确认的 Q1-A：等转数包络分析与文件级稳定特征筛选，以固定一秒窗口作为同模型族的基础对照。

本阶段同时为两条研究路线输出数据：推荐主线使用窗口特征，优先冲奖对照的非平衡最优传输使用同一特征的文件中位数和四分位距。两条路线共享特征定义和训练拟合范围，避免在问题三比较时混入前处理差异。本次不执行问题二分类器、问题三域适应或目标标签推断；完成问题一、提交 GitHub 并取得用户确认后再进入下一问。

数学模型、参数合同和程序函数一一对应。公式编号采用 `(1.xx)`，后续问题采用 `(2.xx)` 等独立前缀；本报告仅定义一个共同表征模型，不为增加模型数量而另造一套输入。

## 2. 数据与符号

源域核心子集为 116 个文件：12 kHz DE 60 个、48 kHz DE 52 个、正常 4 个。按已有文件划分继承训练/验证/测试 70/23/23，正常类为 2/1/1。全部 16 个目标文件用于特征变换和无标签敏感性展示，不参与特征筛选与参数选择。另 45 个 FE 故障文件在筛选清单中标记为扩展对照，不从磁盘删除。

| 符号 | 实际含义 | 单位或范围 |
|---|---|---|
| $i,j,k$ | 原文件、该文件窗口、候选特征索引 | 整数索引 |
| $N_i,m_i$ | 文件原始采样点数、处理后完整窗口数 | 点、个 |
| $\mathcal I_{\mathrm{tr}}$ | 当前拟合使用的源训练文件集合 | 全量为 70 文件；内折再取子集 |
| $y_i,K,N_c$ | 源文件类别、类别数、训练类 $c$ 文件数 | N/OR/IR/B，$K=4$ |
| $x_i[n],\widetilde x_i[n]$ | 原始振动、去均值振动 | 原始尺度，物理单位未知 |
| $F_i,F_e,r_i$ | 原名义采样率、包络采样率、轴转频 | Hz；$F_e=2000$ |
| $R,T_i,L_i$ | 窗口目标转数、名义时长、包络窗点数 | 转、s、点 |
| $h_b,v_i^{(b)},e_i^{(b)}$ | 第 $b$ 频带滤波器、带通信号、包络 | 滤波器无量纲；信号原始尺度 |
| $o_\ell,P_{ij\ell},p_{ij\ell}$ | 阶次网格、谱功率、归一化谱质量 | 无量纲、相对功率、无量纲 |
| $z_{ijk},u_{ik}$ | 窗口特征、文件特征中位数 | 随特征定义 |
| $\mathcal S,\pi_k$ | 最终特征集合、文件重采样入选比例 | 集合、0—1 |
| $\mu_k,\sigma_k$ | 源训练窗口文件等权中心与尺度 | 与第 $k$ 特征同量纲 |
| $\widehat z_{ijk},v_i,\widehat v_i$ | 标准化窗口特征、文件描述量、标准化文件描述量 | 无量纲 |
| $\delta$ | 转速相对扰动 | 人工压力情景，不是实测误差估计 |

候选特征分三块：幅值块为交流 RMS、绝对均值、峰峰值；冲击形状块为峰值因子、Pearson 峭度、偏度；包络调制块为谱熵、阶次质心、阶次离散度、主峰阶次、谐波集中度、低阶能量比。RMS 与均方根是同一个量。

## 3. 必要假设与依据

1. 单个文件具有题面指定的一种状态，文件内部窗口是相关观测。依据是源文件标签和题面单点单故障设置；目标文件只作一个待判对象，不据此假定其标签。
2. 窗内转速以文件平均值近似。源域使用字段或文件名转速，目标使用题面约 600 rpm。没有瞬时转速/键相信号，因此这是平均转速阶次表征；通过 ±5%、±10% 扰动检验，不声称实现精确角度跟踪。
3. 三个候选带内可能包含可用共振响应。这是待检验的信号处理假设，不是已知故障频段。所有带低于最小采样率的 6 kHz 奈奎斯特上限，最高截止留至 5 kHz；频带只由源训练统计选择。
4. 短窗内故障类别不变，但包络结构允许波动。窗口内去均值及单位尺度比值不改变故障标签；是否损失判别信息留待后续分类对照验证。

前六项特征来自各自原采样率的原波形，不同文件的采样带宽并未统一。其幅值和冲击统计可能同时受故障状态、采集带宽与传感器条件影响；后续域迁移对照需评估这些量，不能把全部差异解释为故障差异。

不假设目标轴承等于 SKF6205，不假设 A—P 四类等量，不把振动值强行换算为 g 或 m/s²，不将大冲击视为坏点。

## 4. 编号公式与完整求解过程

### 4.1 读取与源域筛选

核心源域由采集体系确定，主测点统一为 DE；目标使用唯一信号：

$$\mathcal I_s=\mathcal I_{12\mathrm{k,DE}}\cup\mathcal I_{48\mathrm{k,DE}}\cup\mathcal I_{\mathrm N},\qquad |\mathcal I_s|=60+52+4=116.\tag{1.01}$$

先核验输入 SHA-256、数组维度和有限值，然后每文件去直流：

$$\overline x_i=\frac1{N_i}\sum_{n=0}^{N_i-1}x_i[n],\qquad\widetilde x_i[n]=x_i[n]-\overline x_i.\tag{1.02}$$

转频由实际记录来源换算，Hz 表示每秒转数：

$$r_i=\frac{\mathrm{RPM}_i}{60}.\tag{1.03}$$

不对 NaN/Inf 插补；发现输入变化、恒定信号或缺失转速时停止并报告具体文件。

### 4.2 共振解调：先保留原采样率，再提取包络

滤波器采用对称有限冲击响应（FIR），半长度 $M_i=\lceil0.01F_i\rceil$。对带边界 $f_a,f_b$、中心 $f_c=(f_a+f_b)/2$，令 $q=-M_i,\ldots,M_i$：

$$g[q]=\frac{2f_b}{F_i}\operatorname{sinc}\!\left(\frac{2f_bq}{F_i}\right)-\frac{2f_a}{F_i}\operatorname{sinc}\!\left(\frac{2f_aq}{F_i}\right),\quad h_b[q]=\frac{g[q]w[q]}{|\sum_qg[q]w[q]\exp(-2\pi\mathrm j f_cq/F_i)|}.\tag{1.04}$$

其中 $\operatorname{sinc}(u)=\sin(\pi u)/(\pi u)$，$w[q]$ 是参数 8.6 的 Kaiser 窗；程序使用 `firwin(..., scale=True)` 实现中心频率归一。0.01 s 与 8.6 是滤波器长度/窗形的公开实现参数，不代表轴承阻尼或实测噪声。

信号两端各作 0.1 s 反射延拓；对称卷积居中对齐后，从带通信号的解析表示取幅值：

$$v_i^{(b)}=h_b*\widetilde x_i,\qquad e_i^{(b)}[n]=\sqrt{(v_i^{(b)}[n])^2+(\mathcal H v_i^{(b)}[n])^2}.\tag{1.05}$$

$\mathcal H$ 为希尔伯特变换。卷积和希尔伯特变换都在延拓信号上计算，再裁掉延拓部分。希尔伯特变换并非有限支撑，因此仍需边界敏感性检查。

包络经抗混叠多相重采样变为 2000 Hz：

$$\bar e_i^{(b)}=\operatorname{ResamplePoly}\!\left(e_i^{(b)},\frac{F_e}{\gcd(F_i,F_e)},\frac{F_i}{\gcd(F_i,F_e)}\right).\tag{1.06}$$

使用固定 Kaiser 5.0 与线性延拓参数。2000 Hz 的奈奎斯特频率 1000 Hz，高于源域 20 阶在最高约 30 Hz 转频下对应的约 600 Hz；主要分析包络的调制频率，原高频载波没有被先行丢弃。

### 4.3 等转数窗口与一秒对照

改进版本候选 $R\in\{10,20\}$，基础对照 $T_i=1$ s：

$$T_i=R/r_i,\qquad L_i=\operatorname{round}(F_eT_i),\qquad T_i^{\mathrm{act}}=L_i/F_e.\tag{1.07}$$

取边界保护 $G=\lceil0.05F_e\rceil$，完整窗口数与第 $j$ 窗索引为：

$$m_i=\left\lfloor\frac{\bar N_i-2G}{L_i}\right\rfloor,\qquad s_{ij}=G+jL_i,\quad t_{ij}=s_{ij}+L_i,\quad j=0,\ldots,m_i-1.\tag{1.08}$$

$\bar N_i$ 是包络采样点数，$s_{ij},t_{ij}$ 为窗口起止索引，终点不包含在窗内。窗口时间映射回原采样率，用 $\operatorname{round}(s_{ij}F_i/F_e)$、$\operatorname{round}(t_{ij}F_i/F_e)$ 截取同一观测区间。尾部保留在原始记录，完整窗表记录未使用时长。

准入条件必须在所有选定文件、转速扰动及边界候选下成立：

$$\min_i m_i(R,\delta,G)\ge1,\qquad r_i^{(\delta)}=(1+\delta)r_i.\tag{1.09}$$

最短训练记录只有约 39.778906 转，40 转候选已经拒绝；不通过补零、重复或删文件制造完整窗。边界保护对照为 0.025/0.05/0.075 s。

### 4.4 包络阶次谱

每窗包络减去该窗均值，乘对称 Hann 窗 $w_H$，作实数离散傅里叶变换：

$$P_{ij}(f_q)=\left|\sum_{n=0}^{L_i-1}\bigl(\bar e_i[s_{ij}+n]-\overline e_{ij}\bigr)w_H[n]\exp(-2\pi\mathrm j qn/L_i)\right|^2,\quad f_q=qF_e/L_i.\tag{1.10}$$

不通过零填充宣称额外频率分辨率。阶次坐标将 Hz 除以轴转频，在共同网格 $o_\ell=0.2+0.1\ell\le20$ 线性插值功率并归一：

$$P_{ij\ell}=\operatorname{Interp}\{P_{ij}(f_q),\ f=r_io_\ell\},\qquad p_{ij\ell}=\frac{P_{ij\ell}}{\sum_\ell P_{ij\ell}}.\tag{1.11}$$

所有网格间隔相同，积分中的共同间隔在比值中相消。这是谱坐标适配，不是缺失值插补；分母为零则明确报错。0.1 阶是跨方案共同采样网格，不代表所有方案都具有 0.1 阶的独立分辨率。

### 4.5 十二个候选特征

令 $a_n$ 为式 (1.08) 对应原信号片段再减去窗均值，窗内有 $L_x$ 个原采样点。前三项保留幅值，随后三项描述无量纲冲击形状：

$$z_1=\sqrt{L_x^{-1}\sum_na_n^2}.\tag{1.12}$$

$$z_2=L_x^{-1}\sum_n|a_n|.\tag{1.13}$$

$$z_3=\max_na_n-\min_na_n.\tag{1.14}$$

$$z_4=\max_n|a_n|/z_1.\tag{1.15}$$

$$z_5=L_x^{-1}\sum_n(a_n/z_1)^4.\tag{1.16}$$

$$z_6=L_x^{-1}\sum_n(a_n/z_1)^3.\tag{1.17}$$

$z_1$ 是交流 RMS；$z_2$ 是绝对均值；$z_3$ 是峰峰值；$z_4$ 是峰值因子；$z_5$ 是未减 3 的 Pearson 峭度；$z_6$ 是偏度。$z_1=0$ 时不允许以任意小常数强行生成特征。

设谱网格长度为 $L_o$，其余六项均在式 (1.11) 的同一谱质量上定义：

$$z_7=-\frac{\sum_\ell p_{ij\ell}\log p_{ij\ell}}{\log L_o},\qquad 0\log0=0.\tag{1.18}$$

$$z_8=\sum_\ell o_\ell p_{ij\ell}.\tag{1.19}$$

$$z_9=\sqrt{\sum_\ell(o_\ell-z_8)^2p_{ij\ell}}.\tag{1.20}$$

$$z_{10}=o_{\ell^*},\qquad\ell^*=\arg\max_{\ell:\,1\le o_\ell\le10}p_{ij\ell}.\tag{1.21}$$

主峰搜索 1—10 阶是宽范围的候选特征规则，覆盖源域题面故障阶次，仅作结构统计；目标几何未知，不把该峰命名为确定的故障类型。

令 $\Delta_o=\max(0.1,1/(r_iT_i^{\mathrm{act}}))$，把所有整数谐波邻域取并集，避免重叠处重复计数：

$$\mathcal A=\bigcup_{h=1}^{\lfloor20/z_{10}\rfloor}\{\ell:|o_\ell-hz_{10}|\le\Delta_o\},\qquad z_{11}=\sum_{\ell\in\mathcal A}p_{ij\ell}.\tag{1.22}$$

$$z_{12}=\sum_{\ell:o_\ell\le2}p_{ij\ell}.\tag{1.23}$$

$z_7$ 为归一化谱熵，$z_8,z_9$ 为阶次质心及离散度，$z_{10}$ 为观测主峰阶次，$z_{11}$ 为谐波集中度，$z_{12}$ 为低阶能量比例。有限网格的谐波指标可能偏爱低阶主峰，应与不含该特征的后续分类结果对照。理论上 $z_7,z_{11},z_{12}\in[0,1]$，程序逐项检验。

### 4.6 只在训练文件上选择频带、转数和稳定特征

先将每个窗口特征聚合为文件中位数，防止长文件在筛选中占更多权重：

$$u_{ik}=\operatorname{median}_{j=0,\ldots,m_i-1}z_{ijk}.\tag{1.24}$$

每个特征先按当前训练文件的标准差无量纲化。设 $\bar u_{ck}$、$s_{ck}^2$ 是类内均值、总体方差，$\bar u_{\cdot k}=K^{-1}\sum_c\bar u_{ck}$，类别平衡 Fisher 分数为：

$$J_k=\frac{K^{-1}\sum_c(\bar u_{ck}-\bar u_{\cdot k})^2}{K^{-1}\sum_cs_{ck}^2+10^{-12}}.\tag{1.25}$$

常数列先移除，$10^{-12}$ 只处理无量纲数值的除零边界。正常类内折只有 1 个文件时类内方差为 0，结果仍可算，但不能当成精确的总体区分能力。

按 $J_k$ 降序选择，入选特征与已选任意特征的 Spearman 绝对相关需不超过 0.9，最多 8 列：

$$\mathcal S_{t+1}=\mathcal S_t\cup\{k\},\quad \max_{h\in\mathcal S_t}|\rho_{kh}|\le0.9,\quad |\mathcal S_{t+1}|\le8.\tag{1.26}$$

训练 70 文件按类交错分为两折，正常类各留一份；每折在另一半训练文件选择特征，在当前留出折计算式 (1.25) 的分数。对 3 个频带×2 个转数，共 6 个候选组合 $q$，定义：

$$V(q)=\frac12\sum_{v=1}^2\frac1{|\mathcal S_{q,-v}|}\sum_{k\in\mathcal S_{q,-v}}\log(1+J_{q,v,k}),\qquad q^*=\arg\max_qV(q).\tag{1.27}$$

验证集 23 文件、测试集 23 文件及目标域不参与此选择。平局按配置中先出现的方案选择；一秒方案仅在选定频带上作基线。该统计准则是问题一的可执行筛选准则，问题二仍须用文件级分类检验其价值，不将 $V$ 称为准确率。

在全部源训练文件上分层有放回重采样 100 次，每次按式 (1.26) 选特征，得到：

$$\pi_k=\frac1{100}\sum_{b=1}^{100}\mathbf1\{k\in\mathcal S^{(b)}\}.\tag{1.28}$$

按入选率优先、完整训练 Fisher 分数其次排序，仅保留 $\pi_k\ge0.6$ 且通过式 (1.26) 相关约束的列，不强行补至 8 列：

$$\mathcal S=\operatorname{GreedyNonredundant}\{k:\pi_k\ge0.6\},\quad1\le|\mathcal S|\le8.\tag{1.29}$$

阈值 0.6 为预先固定的稳定性筛选工作参数，不是显著性水平；附带记录 0.5/0.6/0.7 三种阈值下的入选集合，阈值不根据目标图调整。空集合直接停止，回看特征逻辑。

### 4.7 推荐主线与最优传输对照的共用数据出口

训练标准化按文件等权、文件内窗口等权拟合；$n_{\mathrm{tr}}=|\mathcal I_{\mathrm{tr}}|$：

$$w_{ij}=\frac1{n_{\mathrm{tr}}m_i},\quad\mu_k=\sum_{i\in\mathcal I_{\mathrm{tr}}}\sum_jw_{ij}z_{ijk}.\tag{1.30}$$

$$\sigma_k=\sqrt{\sum_{i\in\mathcal I_{\mathrm{tr}}}\sum_jw_{ij}(z_{ijk}-\mu_k)^2},\qquad\widehat z_{ijk}=\frac{z_{ijk}-\mu_k}{\sigma_k}.\tag{1.31}$$

这里是坐标拟合权重，不是问题二的类别平衡损失权重。被选列若 $\sigma_k=0$ 则停止检查。验证、测试和目标均用同一组 $\mu_k,\sigma_k$。换训练折时必须重新运行筛选和尺度拟合，不能直接复用本阶段最终表来做内折验证。

运输对照使用相同特征的文件统计，而不是把相关窗口作为独立运输节点：

$$v_i=\left(\operatorname{median}_j\widehat z_{ijk},\ Q_{0.75,j}(\widehat z_{ijk})-Q_{0.25,j}(\widehat z_{ijk})\right)_{k\in\mathcal S}.\tag{1.32}$$

所有分位数采用线性插值定义。$v_i$ 长度为 $2|\mathcal S|$，某文件只有一窗时四分位距为 0，这是信息不足的真实结果，不插补。

运输文件描述量再按源训练文件等权缩放：

$$\eta_\ell=\frac1{n_{\mathrm{tr}}}\sum_{i\in\mathcal I_{\mathrm{tr}}}v_{i\ell},\quad\tau_\ell^2=\frac1{n_{\mathrm{tr}}}\sum_{i\in\mathcal I_{\mathrm{tr}}}(v_{i\ell}-\eta_\ell)^2,\quad\widehat v_{i\ell}=\frac{v_{i\ell}-\eta_\ell}{\max(\tau_\ell,\epsilon_{\mathrm{num}})}.\tag{1.33}$$

若训练列常数，记录并从运输出口剔除；$\epsilon_{\mathrm{num}}=10^{-12}$ 为数值阈值，不把该列当成有信息。此处只生成输入，不求解运输矩阵或标签。

### 4.8 结果检验与敏感性

对目标 16 文件改变近似转速，保持频带、筛选集合和训练尺度冻结。不同转速情景会改变窗数，以文件中位数比较：

$$D_i(\delta)=\sqrt{\frac1{|\mathcal S|}\sum_{k\in\mathcal S}\left[\operatorname{median}_j\widehat z_{ijk}^{(\delta)}-\operatorname{median}_j\widehat z_{ijk}^{(0)}\right]^2}.\tag{1.34}$$

分别为一秒和等转数方案计算。一秒基线冻结主线的频带及特征集合 $\mathcal S$，仅使用自身源训练窗口拟合尺度，并额外保存统一主线尺度下的比较量。$D$ 仅表示特征稳定性，不说明目标分类正确。

增益压力测试与明确的理论预期核对：对 $a'_n=g a_n$，$g>0$，

$$z'_k=gz_k\ (k=1,2,3),\qquad z'_k=z_k\ (k=4,\ldots,12).\tag{1.35}$$

使用 0.5/1/2 倍增益检查实现；信号滤波和包络都是正增益齐次，归一化谱比例保持不变。误差超出浮点容差先检查索引、归一化和谱计算，不强行裁剪成理论结果。

源域代表信号只使用已知 SKF6205 几何，核对题面故障频率。滚动体数 $Z=9$，滚动体直径 $d_b=0.3126$ 英寸，节径 $D_b=1.537$ 英寸：

$$\frac{f_{\mathrm{BPFO}}}{r_i}=\frac Z2(1-d_b/D_b),\quad\frac{f_{\mathrm{BPFI}}}{r_i}=\frac Z2(1+d_b/D_b),\quad\frac{f_{\mathrm{B,题面}}}{r_i}=\frac{D_b}{d_b}\left[1-(d_b/D_b)^2\right],\quad\frac{f_{\mathrm{FTF}}}{r_i}=\frac12(1-d_b/D_b).\tag{1.36}$$

题面滚动体频率按其公式核验，不改写为常见自旋频率的另一倍频口径。理论线是源域核对位置，不是要求实测峰必然吻合；目标图不画未知几何对应的“理论故障线”。

## 5. 参数来源、敏感性与代码对应

模型的可调处理参数集中于 `configs/q1.json`，固定数学定义和实现检验容差同时由公式及程序记录。采样率、转速和几何来自题目及记录；其余是计算设置，作用和取值理由如下。

| 参数 | 取值及依据 | 检验或修正 |
|---|---|---|
| 共同带 | 500—1500、1500—3000、3000—5000 Hz | 均在所有主通道可测带宽内，避免 DC/极低频；三候选的训练内统计真实比较 |
| 转数 | 10、20；一秒对照 | 覆盖约 10/20 个转动周期并满足最短文件；40 转已拒绝 |
| FIR 半长/窗参数 | 10 ms、Kaiser 8.6 | 是滤波实现设置；保存频响，逐带检查中心增益及带外衰减，不等同轴承物理参数 |
| 延拓/裁边 | 100 ms / 50 ms | 延拓超过滤波半支撑；裁边 25/50/75 ms 对照检验希尔伯特端点影响 |
| 阶次网格 | 0.2—20，步长 0.1 | 包含低阶调制及多个源域故障谐波；报告步长与真实分辨率区别 |
| 特征预算/相关阈值 | 最多 8，0.9 | 配合仅 70 独立训练文件及后续小目标协方差；不强行保留冗余列 |
| 稳定选择 | 100 次，阈值 0.6 | 计算预算固定；0.5/0.7 入选集合同时记录 |
| 数值阈值 | $10^{-12}$ | 无量纲稳定计算和退化列识别，不用于替换原始采样值 |
| 随机性 | 种子 42 | 只用于文件分折、重采样及绘图抖动，保存在复现清单 |

程序的实现核验另用固定数值容差：FIR中心增益误差不超过 $10^{-10}$，距通带边界至少300 Hz的阻带峰值低于−40 dB，训练加权均值/二阶矩误差不超过 $10^{-9}$，增益齐次性相对尺度误差不超过 $10^{-8}$。这些是用于发现实现错误的宽松数值检查，不是题面物理参数，也不参与频带或特征的评分；300 Hz用于避开有限长FIR的过渡带。

| 程序函数 | 对应公式 | 职责 |
|---|---|---|
| `read_inventory`、`load_record` | 1.01—1.03 | 文件筛选、哈希与数组核验、转速溯源 |
| `prepare_record` | 1.04—1.06 | FIR、延拓、希尔伯特包络、包络重采样 |
| `window_features` | 1.07—1.23 | 窗口索引、同时间原波形及阶次谱、十二特征 |
| `fisher_scores`、`select_nonredundant`、`choose_variant` | 1.24—1.27 | 文件级两折参数选择 |
| `stable_selection` | 1.28—1.29 | 文件重采样稳定筛选 |
| `fit_window_scaler`、`apply_scaler` | 1.30—1.31 | 训练文件等权标准化 |
| `file_descriptors` | 1.32—1.33 | 运输输入的文件中位数/IQR 与尺度 |
| `run_sensitivity`、独立检验脚本 | 1.34—1.36 | 转速、边界、增益及源域机理核验 |

求解先完成有限候选的数据计算和训练内部评分，再固定方案、筛选和尺度；最后批量变换所有入选文件。任何候选出现空窗或非有限量，先追踪文件和处理组合；不为了得分或图形效果静默换标签、删行或变参数。

## 6. 本阶段结果与验收范围

实际数值、参数选择及图表统一写入运行生成的 `results/q1/问题一_结果报告.md`，不预填模型收益。当前说明是公式与实现合同，结果报告是真实运行证据，两者通过复现清单绑定。

原始图展示文件数量、采样/转速条件和代表波形；过程图展示候选评分、解调过程、稳定选择；结果图展示文件级特征分布、相关性、敏感性。另绘制问题一流程及两条研究路线的总体阶段图，未来阶段明确标注“待确认后实施”。全部图用一致、色觉友好的蓝/橙/绿/紫配色，导出可编辑 SVG 和 300 DPI PNG，正文图注说明样本单位及统计量。

M1 核对本模型合同；P1 用真实少量文件跑通输入到两个数据出口；P1 通过后全量计算与正式绘图；P2 独立重跑并核对数据、公式、输出和图。问题一完成后只提交本阶段，等待用户确认，不自动训练下一问。

## 7. 来源

1. [原始赛题](E题：高速列车轴承智能故障诊断问题.pdf)第 1—8 页：任务、数据条件及式 (1.36) 的题面机理。
2. [预处理说明](预处理数据集/README.md)与[文件清单](预处理数据集/files.csv)：原始哈希、划分、数组长度、RPM 来源。
3. [创新建模方案](创新建模方案与模型适配预处理.md)：已确认的 Q1-A 和 Q3-C 共用输入约定。
4. 刘文朋等，2025，[轮轨激励下高速列车轴箱轴承故障诊断方法研究](https://doi.org/10.3901/JME.2025.06.249)：共振解调及背景干扰的研究依据，本模型不冒称复现其全部算法或实验成绩。
