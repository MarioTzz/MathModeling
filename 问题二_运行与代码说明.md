# 问题二运行与代码说明

## 1. 代码完成的任务

问题二程序从问题一导出的两张未标准化候选窗口表开始，按以下顺序执行：

1. 读取并核对 20 转与 1 秒特征表、源域文件标签和固定的 `70/23/23` 文件划分；
2. 在每个拟合折内重做文件级稳定特征筛选和文件等权标准化；
3. 显式训练四个一对其余 SVM，并以窗口样本权重实现窗口均权、类别均衡和文件—类别双重均衡；
4. 在 70 个训练文件内调参，用 23 个验证文件选择两种窗口表示；
5. 冻结表示后，以 93 个开发文件重调四个消融方案，再对 23 个测试文件评价一次；
6. 检查支持向量状态能否重建分数，核对权重和、类别权重、拟合文件范围与零噪声重算；
7. 对幅值增益和原始信号白噪声作冻结模型压力试验；
8. 生成结果表、模型状态、9 张以上三类图、两张流程图及复现清单。

主入口是 `问题二_求解.py`，公式数值函数在 `utils/q2_model.py`，绘图函数在 `utils/q2_plots.py`。代码中的函数分工与数学模型对应如下。

| 数学环节 | 公式 | 代码 |
|---|---|---|
| 文件中位数、Fisher 分数和稳定筛选 | (2.01)—(2.04) | `stable_select_for_files` |
| 文件等权尺度 | (2.05)—(2.07) | `fit_file_equal_scaler`、`transform_features` |
| 三种损失权重 | (2.08)—(2.11) | `sample_weights`、`audit_weights` |
| RBF 核和四个 OvR 分类器 | (2.12)—(2.16) | `fit_ovr` |
| 文件分数、诊断间隔和投票比例 | (2.17)—(2.20) | `aggregate_predictions` |
| 文件分组内折与网格搜索 | (2.21)—(2.22)、(2.26)—(2.27) | `make_group_folds`、`build_cv_cache`、`tune_model` |
| 文件级宏 F1 与平衡准确率 | (2.23)—(2.25) | `classification_metrics` |
| 线性核、增益和噪声试验 | (2.28)—(2.30) | `amplitude_perturbation`、`noise_stress` |

## 2. 数据读取与信息隔离

程序读取：

- `模型适配数据集/q1/window_features_all.csv`：固定频带下的 20 转候选表；
- `模型适配数据集/q1/baseline_1s_features_all.csv`：相同频带下的 1 秒候选表；
- `模型适配数据集/q1/files.csv`：文件标签、划分、原始路径和哈希；
- `results/q1/summary.json`：噪声重算所需的频带、窗口和滤波参数。

Q1 的频带已经由 70 个训练文件选择，本问不会在内折中重新选频带。因此 Q2 内折分数只表示固定 Q1 表征条件下的调参结果。两条候选表均未预先标准化；特征选择与尺度在每个拟合折内重算。验证、测试和目标文件不进入相应拟合过程，目标域在本问的使用文件数为 0。

## 3. 参数计算和模型求解

随机种子固定为 42。每类文件标识先排序，再按固定随机置换交替分入两个内折。RBF 搜索 24 组参数：

$$
C\in\{1,4,16,64,256,1024\},\qquad
\gamma\in\{0.03125,0.125,0.5,2\}.
$$

线性核只搜索 6 个 $C$。每组参数训练 `N/OR/IR/B` 四个二分类器；`class_weight=None`，样本权重只传入一次。求解容差为 $10^{-3}$，最多迭代 200000 次，出现收敛警告或 `fit_status_ != 0` 时立即停止。

每折的选择顺序是：文件级宏 F1 最大、平衡准确率最大、$C$ 较小、$\gamma$ 较小。模型输出采用四个窗口决策分数的文件内算术平均；分数相同时按固定类别顺序 `N, OR, IR, B` 决胜。

## 4. 实际运行结果

验证集选择了 1 秒表示：它的宏 F1 为 0.8975、平衡准确率为 0.9250；20 转表示分别为 0.8675 和 0.8893。93 个开发文件重调后，预注册 RBF 双重均衡主模型取 $C=1024$、$\gamma=0.5$，保留 7 个特征。

封存测试的文件级结果为：

| 方案 | 准确率 | 宏 F1 | 平衡准确率 | 正常召回 |
|---|---:|---:|---:|---:|
| RBF＋窗口均权 | 0.8261 | 0.6339 | 0.6629 | 0 |
| RBF＋类别均衡 | 0.8261 | 0.6339 | 0.6629 | 0 |
| RBF＋文件—类别双重均衡 | 0.8261 | 0.6339 | 0.6629 | 0 |
| 线性核＋文件—类别双重均衡 | 0.7391 | 0.7946 | 0.8258 | 1 |

三种 RBF 方案对 23 个测试文件给出了相同标签；双重均衡没有形成可观测增益。RBF 主模型把唯一正常测试文件判为滚动体故障。线性对照少判对了 2 个文件，但类别均衡指标和正常召回更高。这一结果表明，当前低维特征上的非线性核没有自动带来更好的文件级类别平衡。

主结果保持预注册的窗口分数均值。多数投票的测试宏 F1 为 0.8999，但它是测试后观察到的鲁棒汇总对照，不能替换主结果。幅值翻倍时主模型标签保持率为 0.7391、宏 F1 为 0.4659；所测 40、26.02 和 20 dB 白噪声下标签均保持不变。白噪声先生成独立标准正态序列，再按该有限序列的实际 RMS 归一化，使所加噪声 RMS 精确等于指定比例。稳健性结论限于这些受控数字扰动。

## 5. 运行方法

项目使用隔离依赖目录。当前环境的唯一正式命令为：

```bash
MPLCONFIGDIR=.analysis-work/matplotlib \
XDG_CACHE_HOME=.analysis-work/font-cache \
PYTHONPATH=.analysis-work/q2-deps:.analysis-work/q1-deps:.analysis-work/python-deps:. \
/Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 \
问题二_求解.py --overwrite
```

最小链检查命令为：

```bash
MPLCONFIGDIR=.analysis-work/matplotlib \
XDG_CACHE_HOME=.analysis-work/font-cache \
PYTHONPATH=.analysis-work/q2-deps:.analysis-work/q1-deps:.analysis-work/python-deps:. \
/Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 \
问题二_求解.py --smoke --output-root .analysis-work/q2-smoke --overwrite
```

正式结果位于 `results/q2/`，可复用模型位于 `模型适配数据集/q2/`，图位于 `figures/q2/`。`source_classifier.*` 是预注册 RBF 主模型；四个具名模型文件保存相同开发集上的冻结消融方案，其中 `linear_file_class_balanced.*` 是本次类别均衡指标更高的对照。模型 JSON 保存类别顺序、尺度和偏置，NPZ 保存支持向量与对偶系数。

## 6. 结果使用边界

SVM 分数、诊断间隔和投票比例都不是校准概率。测试集正常类只有一个文件，正常召回无法稳定估计。测试结果没有回流修改表示、参数网格、主模型或汇总规则。本问没有对目标域 A—P 进行推断；问题三若经用户确认开始，应在开发代理实验中预先规定 RBF 主模型与线性对照的使用方式，不能仅凭本次测试结果临时挑选目标域输出。
