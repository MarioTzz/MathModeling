# 问题二：97% 文件级准确率目标的优化核验

## 1. 目标与结论

本轮沿用问题二固定的 116 个源域 DE 文件及 70/23/23 文件级训练、验证、测试划分。用户新增的目标是文件级准确率严格超过 97%。测试集只有 23 个文件，22/23 为 95.65%，因此本划分上超过 97% 必须 **23/23 全部正确**。原题第 2 问没有规定这个阈值。

我们用训练和验证文件探索窗口长度、物理频带、文件特征汇总和外圈故障位置子类。开发集表现最好的可复现实验是 **30 转、1500—3000 Hz、38 维文件中位数、外圈位置子类加权的极端随机树**：93 个开发文件的五组分层三折平均准确率 98.49%，70/23 划分上的回看准确率为 23/23。该 23 个验证文件已参与 93 文件的结构与权重选择，回看结果不是独立候选外部验证。以 93 个开发文件重训后，原固定测试集只有 **21/23（91.30%）**；原已提交的 20 转三频带模型仍是 **22/23（95.65%）**。因此目前没有达到用户要求的固定测试集 97% 目标，现有主模型保持不变。

上述固定测试集在前轮模型探索中已经被查看，本轮候选与分析也无法获得“全新盲测”的地位。后续即使某个改法在这 23 个文件上取得 23/23，也需用未参与方案选择的新标签文件检验稳定性。

## 2. 30 转位置子类候选的完整模型

该候选只用于诊断为什么开发集提升不能外推，不作为已部署模型。记第 $i$ 个源文件转频为 $f_{r,i}$、降采样包络采样率为 $f_e=2000$ Hz、完整窗数为 $M_i^{(30)}$；$g=100$ 个包络点为既有边缘保护宽度。原信号读取、去均值、FIR 解调、Hilbert 包络及阶次谱沿用[已验收模型](问题二_优化模型与求解说明.md)式(2.31)—(2.42)，仅把窗口转数及物理频带设为本节值。

$$R'=30,\qquad b'=[1500,3000]\ {\rm Hz}.\tag{2.54}$$

30 转窗长与完整窗数为

$$L_i'=\operatorname{round}\!\left(\frac{f_eR'}{f_{r,i}}\right),\qquad M_i^{(30)}=\left\lfloor\frac{|e_{ib'}|-2g}{L_i'}\right\rfloor.\tag{2.55}$$

窗 $j=0,\ldots,M_i^{(30)}-1$ 的包络范围为

$$W_{ij}'=[g+jL_i',\,g+(j+1)L_i').\tag{2.56}$$

按原模型提取 12 项基础特征及单频带 26 项题面故障阶次特征，窗口向量记为 $z'_{ij}\in\mathbb R^{38}$。每个原始文件只形成一行分类输入：

$$v'_{ik}=\operatorname{median}_{0\le j<M_i^{(30)}}z'_{ijk},\qquad k=1,\ldots,38.\tag{2.57}$$

训练时，源域文件路径中的已知外圈故障安装位置只用于拆分 **训练标签**，不作为预测特征。外圈居中记为 $OR_C$，其他外圈位置合并为 $OR_O$：

$$y'_i=\begin{cases}OR_C,&y_i=OR\ \text{且训练元数据标记为居中},\\OR_O,&y_i=OR\ \text{且训练元数据标记为其他位置},\\y_i,&y_i\in\{N,IR,B\}.\end{cases}\tag{2.58}$$

于是内部类别集合为 $\mathcal C'=(N,OR_C,OR_O,IR,B)$。在任一训练折 $I$ 中，$N'_c(I)$ 是子类 $c$ 的文件数，$n(I)=|I|$；权重每折重算：

$$N'_c(I)=\sum_{i\in I}\mathbf1(y'_i=c),\qquad n(I)=\sum_{c\in\mathcal C'}N'_c(I).\tag{2.59}$$

$$w_i'(I)=\frac{n(I)}{5N'_{y'_i}(I)}\,\lambda^{\mathbf1(y'_i=OR_C)},\qquad \lambda\in\{1,1.5,2,3,4,5,8,12\}.\tag{2.60}$$

$\lambda=4$ 是本轮开发集交叉验证中的最优值，不是轴承物理常数。五类极端随机树的加权节点类别占比及 Gini 不纯度为

$$p'_c(A)=\frac{\sum_{i\in A}w'_i\mathbf1(y'_i=c)}{\sum_{i\in A}w'_i},\qquad G'(A)=1-\sum_{c\in\mathcal C'}p'_c(A)^2.\tag{2.61}$$

500 棵树均取 `max_features="sqrt"`、`min_samples_leaf=1`、随机种子 42。记第 $t$ 棵树落叶后的子类比例为 $q'_{tc}(v')$，集成分数为

$$s'_c(v')=\frac1{500}\sum_{t=1}^{500}q'_{tc}(v').\tag{2.62}$$

预测时把两个外圈子类的分数相加，输出仍为原题四类：

$$s_{OR}'=s'_{OR_C}+s'_{OR_O},\qquad s_N'=s'_N,\quad s_{IR}'=s'_{IR},\quad s_B'=s'_B.\tag{2.63}$$

$$\hat y_i'=\underset{c\in(N,OR,IR,B)}{\operatorname{argmax}}\ s'_c(v'_i).\tag{2.64}$$

给定文件集合 $S$，文件级准确率为

$$A(S)=\frac1{|S|}\sum_{i\in S}\mathbf1(\hat y_i'=y_i).\tag{2.65}$$

五个固定种子 $r\in\{11,29,42,71,97\}$ 各按**原题四类 $y_i$** 分层做一次三折文件级交叉验证；每个拟合折再依式(2.58)拆分内部训练子类并计算式(2.60)权重。以五个完整折外准确率的平均值和最小值检查表现：

$$\bar A_{dev}=\frac15\sum_{r=1}^{5}A_r(\mathcal I_{dev}),\qquad A_{dev}^{min}=\min_r A_r(\mathcal I_{dev}).\tag{2.66}$$

70→23 验证计算仅用于回看：30 转窗口与 $\lambda$ 已经利用全部 93 个开发文件的折外分数选择，不能再把该 23 文件称为独立验证集。最终测试模型用 93 个开发文件重新计算式(2.60)权重并拟合，再对 23 个固定测试文件逐个预测。严格超过 97% 的整数条件为

$$A(\mathcal I_{te})>0.97\quad\Longleftrightarrow\quad\sum_{i\in\mathcal I_{te}}\mathbf1(\hat y_i'=y_i)=23.\tag{2.67}$$

以上是可运行的候选流程：原始 MAT → 30 转单频带窗口 → 文件中位数 → 训练折内子类与权重 → 极端随机树 → 子类分数合并 → 文件级验证。测试文件的路径、安装位置和标签均不输入分类器；预测只读取信号特征。

## 3. 数据适配与实际比较

30 转候选沿用原 MAT、去均值、转速及采样率记录，不插值故障标签，不把同一文件的窗口拆进不同折。10 转窗口在开发集未带来提升。40 转使 `source/48kHz_DE_data/IR/0014/IR014_0.mat` 没有完整有效窗口，不具备全文件覆盖条件，因此没有进入正式候选。更高共振频带、窗口分位数、窗口级拟合及全记录阶次谱也做过开发集筛查；它们未同时提供比本节候选更高的开发集与验证集表现，不作为正式替换方案。

| 方案 | 93 文件五组三折平均 | 五组最低 | 70→23 回看 | 固定 23 文件测试 |
|---|---:|---:|---:|---:|
| 已提交主线：20 转、三频带、四类 ExtraTrees | 95.27% | 94.62% | 22/23 | **22/23，95.65%** |
| 30 转、单频带、四类 ExtraTrees | 97.85% | 96.77% | 22/23 | 21/23，91.30% |
| 30 转、单频带、位置子类、$\lambda=4$ | **98.49%** | **97.85%** | **23/23** | 21/23，91.30% |

30 转位置子类候选在全 116 源文件上的五组分层三折补充审计平均准确率为 95.52%，低于开发集的 98.49%。这一补充审计发生在方案探索后，只用于定位不稳定性。固定测试集两项 OR→B 错误分别是 `source/12kHz_DE_data/OR/Opposite/0007/OR007@12_1.mat` 和 `source/48kHz_DE_data/OR/Centered/0014/OR014@6_0.mat`；后一文件在原主线中也是唯一错误。开发集的高分没有消除该测试条件上的混淆。

## 4. 模型取舍与后续验证

当前最有利的开发集方案没有通过固定测试集，因此**不替换**[问题二现有主线](问题二_优化模型与求解说明.md)及其模型文件。把固定测试集反复用于选择针对这两份错误文件的规则，只能提高这 23 份已见数据的分数，不能支持“更稳定”的结论。

23 文件测试的准确率步长是 $1/23=4.35$ 个百分点。即便观察到 23/23，按二项分布 Clopper–Pearson 双侧 95% 区间，其总体准确率下界仍约为 85.18%；仅凭这个小测试集无法证明真实准确率稳定高于 97%。要评价这一目标，需要新增未参与模型设计的带标签文件，重点覆盖 48 kHz 外圈居中、零载荷及易混的滚动体故障，并事先冻结模型和评价口径。已有四个正常源文件也限制了正常类召回率的精度。

## 5. 代码与复现

[核验程序](问题二_97目标核验.py)按原 MAT 读取、30 转特征提取、文件级汇总、训练折权重计算、五组三折验证、固定测试和结果导出的顺序运行。它不改写原主模型与目标域预测。先用 `--smoke --output-root .analysis-work/q2-review97/smoke` 核查最小计算链，再在项目根目录按[复现清单](results/q2_97_audit/复现清单.json)中的 `reproduce_command` 全量运行。全量结果见[审计摘要](results/q2_97_audit/summary.json)、[开发集交叉验证明细](results/q2_97_audit/development_cv_detailed.csv)、[固定测试逐文件预测](results/q2_97_audit/test_predictions.csv)和[全源补充审计](results/q2_97_audit/full_source_cv_detailed.csv)。

程序同时导出 595 个30转窗口与 116 个文件的特征表，供公式(2.54)—(2.57)逐项复核。输出只含 116 个源域 DE 文件；目标域文件数为 0。22 项单元测试通过，其中新增测试检查训练折内权重及推断分数不依赖留出文件的路径、标签和划分字段。`summary.json` 的 `runtime_seconds` 是机器耗时，不是模型评价指标。
