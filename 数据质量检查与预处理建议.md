# 高速列车轴承数据质量检查与预处理建议

本报告基于当前目录的原题和全部 MAT 文件，回答哪些数据问题已经出现、哪些处理有必要，以及处理会怎样影响后续诊断。检查日期为 2026-09-20。已执行的是只读数据检查和统计分析；标准化、重采样、插值及诊断训练尚未实施。

## 一、检查结论

数据能够完整读取，当前没有需要按 NaN 或 Inf 填补的振动采样值。后续处理的重点是通道与元信息不齐、采样条件和幅值尺度差异、局部可疑信号，以及数据划分时的关联性控制。振动冲击本身可能包含故障信息，不能把统计离群点直接当作坏点删除。

| 检查项目 | 本次实际发现 | 初步处理结论 |
|---|---|---|
| 文件与信号 | 177 个 MAT 文件，427 条 float64 列向量，共 54,870,522 个振动采样值 | 保留原始输入，建立逐文件、逐通道索引 |
| 数值缺失 | NaN、Inf、空信号、整条常量信号均为 0 | 当前不需要缺失值插补 |
| 通道缺失 | 源域 161 条 DE、153 条 FE、97 条 BA | 将缺失通道标为不可用，不补零或复制其他通道 |
| 转速字段 | 源域 151 个文件有 RPM 字段，另外 10 个可从文件名读取转速 | 补全元数据并记录信息来源，不作数值插值 |
| 异常候选 | 个别源域通道存在明显直流偏置、分段能量变化、重复极值和平顶；部分目标域信号具有强冲击特征 | 保留并标记，按原因复核，不统一删点或截断 |
| 重复数据 | 未发现整文件或整条数值信号完全重复；5 个源域文件的 DE、FE 在长段重叠区间呈时移、比例对应 | 除哈希查重外检查通道关联，相关数据保持同组 |
| 单位与尺度 | 采样频率有 12、32、48 kHz；转速和几何量各有单位；振动幅值单位及标定关系未说明 | 明确单位后再换算，幅值尺度差异暂不解释为单位换算关系 |
| 类别分布 | 源域 OR/IR/B/N 分别为 77/40/40/4 个文件 | 划分与评价保留类别信息；平衡措施仅作用于训练部分 |

DE、FE、BA 分别是驱动端（Drive End）、风扇端（Fan End）和基座（Base）测点。OR、IR、B、N 分别表示外圈故障、内圈故障、滚动体故障和正常状态。目标域 A—P 是文件编号，其中 B、N 不代表已知工作状态。

## 二、检查方法与判定口径

逐个读取 MAT 文件，检查字段、形状、数值类型及有限性，再对各振动通道计算均值、标准差、均方根（Root Mean Square，RMS）、分位数、峰度、连续相等值长度及极值出现情况。RMS 为原始采样值平方平均后开根；峰度使用 Pearson 四阶标准矩，未减去 3。

为查看一条记录内部的幅值变化，将其按索引分为 8 个近等长连续块，各块独立去均值后计算 RMS，并比较最大值与最小值。这一比值是变化线索，不是平稳性检验结论，也没有被设为自动删段阈值。

统计离群检查包括偏离本通道均值超过 3 个标准差、落在四分位距（Interquartile Range，IQR）的 1.5 倍围栏之外，以及偏离中位数超过 6 倍尺度化中位绝对偏差（Median Absolute Deviation，MAD）。尺度化 MAD 使用 1.4826 倍 MAD。1.5 倍 IQR 是常见探索性标记方法；本次 3σ 和 6 倍尺度化 MAD 用作并列筛查口径，均不作为仪器故障或数据删除标准。[NIST 箱线图说明](https://www.itl.nist.gov/div898/handbook/eda/section3/boxplot.htm)

重复检查分别覆盖文件字节、整条数值序列、从索引 0 开始的非重叠 4096 点完整块，以及对齐前 4096 点的高度相关筛查。另补充源域同文件跨通道及目标域文件之间的时移相关检查。具体覆盖范围见第五节。

## 三、缺失值与结构不一致

### 3.1 振动采样值无需当前插补

427 条信号均可读取，未检出 NaN 或 Inf。最长连续零值只有 1 点，最长连续相等值为 4 点，未发现长段全零或整条常量记录。因此，没有依据对现有采样值实施均值填补、前向填充或插值。

文件没有逐点时间戳，`time` 后缀表示振动时间序列，而不是时间戳数组。因此，数值完整性检查不能排除采集阶段曾发生但未编码为空值的丢采样，也不能仅由数组长度判断记录是否被截短。

若后续另有证据确认少量采样缺口，应保留缺失掩码，先检查缺口时长与冲击周期的关系。只有在局部信号适合插值、并验证插值没有改变关键特征时，才考虑修补。长缺口或跨越故障冲击的缺口宜将对应窗口标为不可用。线性插值可能削弱冲击峰值，前向填充可能制造平顶，均会改变频谱和峰度；当前数据没有触发这一步。

### 3.2 通道缺失属于观测信息不足

源域 8 个文件缺少 FE，64 个缺少 BA。97 个文件有 DE、FE、BA 三通道，56 个有 DE、FE 两通道，8 个仅有 DE。缺少 FE 的 8 个文件恰为 `12kHz_DE_data` 中 B、IR 两类 0.028 英寸故障记录，缺失模式与数据分组有关。

建议将是否存在各通道记录为元数据。需要统一单通道输入时，DE 覆盖全部源域文件，可作为候选观测通道；是否适合迁移仍需结合故障轴承位置和目标域条件判断。需要多通道分析时，使用通道齐全的子集或显式处理缺失的结构，并报告实际保留数量。

用 0 填补整条缺失通道，会把“没有测量”伪装成“测到了零振动”；复制 DE 作为 FE 则会制造冗余证据。即使使用通道缺失标记，也要防止模型利用其与源域类别的对应关系走捷径。目标域仅有一个未标明测点的通道，不能直接要求目标域提供三通道输入。

### 3.3 转速可以补元数据，目标域标签不能补作真值

151 个源域 RPM 字段的取值为 1718—1798 rpm。缺少内部 RPM 字段的 10 个文件是：B、IR 两类全部 8 个 0.028 英寸故障文件，以及 `N_1_(1772rpm).mat`、`N_2_(1750rpm).mat`。其文件名保留了转速，可解析到独立的转速变量，并将来源标为“文件名”。本次未发现同时存在的文件名转速与字段转速冲突。

目标域只给出约 600 rpm，应保留“题面近似值”标记，而不是生成一列看似逐文件精确测量的转速。真实类别、轴承几何参数和测点位置仍属未知。特别是目标域类别保持空值或专用的未标注状态，不进行众数填充；否则会把题目要求预测的结果预先写成假标签。

### 3.4 长度不一致不等于缺失

| 数据组 | 实际长度分布，以同一文件的一条通道计 | 处理含义 |
|---|---|---|
| `12kHz_DE_data` | 60 个文件均为 96,000 点 | 可以使用统一时间口径组织窗口 |
| `12kHz_FE_data` | 44 个为 96,000 点，`OR/Orthogonal/0014/OR014@3_1.mat` 为 121,168 点 | 长记录保留，切窗时控制其权重 |
| `48kHz_DE_data` | 47 个为 192,000 点，另 5 个长度见下表 | 不宜统一拉伸为固定总长度 |
| `48kHz_Normal_data` | 3 个为 384,000 点，`N_0.mat` 为 243,938 点 | 正常类文件少且长度不同，需要按文件组织划分 |
| 目标域 | 16 个文件均为 256,000 点，与 32 kHz、8 s 一致 | 标签最终对应原始文件 |

`48kHz_DE_data` 中长度不同的 5 个文件如下。

| 相对子路径 | 采样点数 |
|---|---:|
| `IR/0014/IR014_0.mat` | 63,788 |
| `OR/Opposite/0007/OR007@12_0.mat` | 129,969 |
| `OR/Opposite/0021/OR021@12_0.mat` | 130,549 |
| `OR/Orthogonal/0007/OR007@3_0.mat` | 124,602 |
| `OR/Orthogonal/0021/OR021@3_0.mat` | 128,663 |

需要固定长度模型输入时，应先明确物理采样率和窗口时长，再取窗；不足一窗的尾部可明确舍弃并记录数量，或在模型支持掩码时补齐并屏蔽填充值。把不同总时长的整条记录插值到同样点数会改变时间尺度和故障频率，不能作为单纯的长度统一方法。

## 四、异常值：已发现什么，是否应删除

### 4.1 大峰值和高峰度需要保留机理解释

所有通道合计有 561,542 个点超过各自通道的 3σ 范围，有 1,844,836 个点落在 1.5 倍 IQR 围栏之外。这些是筛查命中数，不是坏点数。题目附件 2 已说明，轴承缺陷会产生重复冲击，冲击信号本来就可能呈现重尾和高峰度。

目标域中的具体例子如下，幅值均使用文件中的原始数值，物理单位未标定。

| 文件 | 原始 RMS | Pearson 峰度 | 绝对峰值 | 超出 3σ 的采样点比例 |
|---|---:|---:|---:|---:|
| C | 31.5228 | 28.288 | 349.501 | 2.516% |
| E | 15.6650 | 40.934 | 179.968 | 2.296% |
| I | 29.6648 | 21.232 | 299.755 | 1.938% |
| J | 30.0961 | 37.106 | 432.438 | 2.107% |
| N | 9.2665 | 33.007 | 174.534 | 1.333% |

例如，E 文件有 5,879 个点超过 3σ。直接删除或截断这些点，可能抹去需要用于故障识别的冲击。建议先保留上述记录，结合冲击是否重复出现、频谱或包络是否一致以及采集质量信息复核。这里没有据幅值或峰度为这些文件赋予故障标签。

### 4.2 直流偏置、幅值变化与疑似削顶

以下路径均相对于 `数据集/源域数据集/`。

| 文件与变量 | 直接证据 | 建议与可能影响 |
|---|---|---|
| `48kHz_DE_data/OR/Centered/0014/OR014@6_0.mat`，DE | 均值 0.017374，原始 RMS 0.150478，均值绝对值约为 RMS 的 11.55% | 分析交流振动时生成去均值副本；保留原始均值和 RMS，避免把直流与振动能量混合 |
| 同文件 FE | 均值 −0.015996，均值绝对值约为 RMS 的 10.95% | 同样检查偏置；同时复核与 DE 的时移对应关系 |
| `48kHz_DE_data/B/0021/B021_2.mat`，DE | 8 块去均值 RMS 为 0.1790—0.5145，最大与最小之比约 2.874 | 保留变化，后续以局部窗口分析；不能假定整个记录幅值稳定 |
| `48kHz_DE_data/B/0021/B021_3.mat`，DE | 8 块去均值 RMS 比约 2.470；最小/最大值合计出现 61 次，正、负极值均有连续 4 点的平台 | 标记疑似削顶窗口，核对波形及量程；不通过插值“恢复”未知峰顶 |
| `12kHz_FE_data/OR/Opposite/0007/OR007@12_0.mat`，FE | 最小/最大值合计出现 22 次，最大值有连续 2 点的平台 | 作为较弱的削顶或量化候选复核，不能仅凭两点相等删除 |

其中 `B021_3.mat` 的 DE 极值约为 −3.429637 和 3.406064，局部多次达到同样极值并形成平顶，支持将其列为优先复核对象。没有传感器量程和原始采集记录，仍不能确认发生硬件饱和。相等采样值也可能来自量化，最长 4 点的平台不等于长段传感器失效。

去均值会去掉直流成分，改变含直流的 RMS，因而应同时保留去均值前后指标。线性去趋势、滤波和降噪则不作为统一必做项：它们可能改变低频成分、冲击幅值和包络特征，需要用相同数据划分比较处理前后的效果。疑似削顶已经损失的信息不能靠标准化或平滑可靠找回。

## 五、重复值与数据关联

### 5.1 已确认的查重结果

| 层次 | 本次结果 | 结论适用范围 |
|---|---|---|
| 文件字节 SHA-256 | 无相同文件组 | 排除当前文件的字节级拷贝 |
| 整条数值序列 | 427 条信号无相同长度且完全相同的数值序列 | 即使变量名不同，整条完全重复也已纳入检查 |
| 4096 点完整块 | 无相同块 | 仅检查从索引 0 开始的非重叠完整块，不覆盖所有可能起点 |
| 对齐前缀 | 对齐前 4096 点的绝对相关系数未达到 0.9999 | 仅是对齐前缀筛查，不排除任意时移后的相似 |
| 文件名 | 存在 52 组同名文件 | 跨目录同名不代表相同记录，不能按文件名去重 |

振动序列中重复出现某个幅值也不是重复样本。对一维波形按数值去重会破坏时间顺序、采样间隔和周期结构。文件唯一标识应使用域与相对路径，而不是 `B007_0` 这样的单一文件名。

### 5.2 时移和幅值比例能够掩盖重复信息

在 `48kHz_DE_data/OR/Centered/0014/OR014@6_0.mat` 中，将 DE 相对 FE 平移 42,870 个采样点后，149,130 个重叠点的相关系数约为 1，FE 对 DE 的比例系数约为 0.984848，截距接近 0，仿射拟合残差标准差与 FE 标准差之比约为 2.1×10⁻¹⁶。即，在这段重叠区间内两者几乎是相同波形的平移与比例版本。

这属于数值关系事实，不能据此推断文件生成过程或采集真实性；但它说明这两条通道不能作为相互独立的观测证据。后续多通道分析应检查冗余，训练与测试划分至少保持同一原始文件的所有通道和窗口处于同一组。如果选择单通道，应保留筛选依据，原始 MAT 文件仍不修改。

补充检查对源域同文件通道对、目标域所有文件对搜索整数采样点平移，要求至少重叠较短序列的 50% 且不少于 4096 点，以绝对相关系数 0.995 作为复核阈值，再计算重叠段的仿射残差。共检查 467 对，其中源域同文件通道对 347 对、目标域文件对 120 对。检出 5 对，全部是源域同文件的 DE、FE；目标域文件之间没有达到该阈值的候选。

5 个源文件均位于 `48kHz_DE_data/` 下，对应关系如下。索引从 0 开始；将两个通道从表中起点分别截取“重叠点数”个点，即得到用于比较的两个片段。

| 相对子路径 | 原始编号 | DE 起点 | FE 起点 | 重叠点数 |
|---|---|---:|---:|---:|
| `B/0014/B014_0.mat` | X189 | 0 | 1,068 | 190,932 |
| `B/0021/B021_0.mat` | X226 | 0 | 33,686 | 158,314 |
| `IR/0021/IR021_0.mat` | X213 | 14,598 | 0 | 177,402 |
| `OR/Centered/0014/OR014@6_0.mat` | X201 | 42,870 | 0 | 149,130 |
| `OR/Centered/0021/OR021@6_0.mat` | X238 | 0 | 13,318 | 178,682 |

上述 5 对重叠片段的相关系数均大于 0.99999999999999，FE 对 DE 的比例系数均约为 0.984848，仿射残差标准差比均小于 2.2×10⁻¹⁶。它们在重叠区间的冗余已经由数值验证。建议优先核对这 5 个文件的通道来源；在信息澄清前，不把 DE、FE 的高度一致当成两个独立传感器对诊断结论的共同验证。

以上范围不等于对所有源域跨文件组合进行了任意时移穷举，也不排除更短片段或非线性变换后的重复。此前“无整条完全重复”的结论与这里的局部时移、比例对应并不矛盾，两种查重口径需要同时保留。

## 六、单位、采样频率和幅值尺度

### 6.1 哪些单位可以统一

| 数据或变量 | 当前情况 | 建议统一方式 | 对模型的影响 |
|---|---|---|---|
| 采样频率 | 12、32、48 kHz | 元数据统一存为 Hz，同时保留原始说明 | 正确构造时间轴和频率轴，不能只修改单位标签而不换数值 |
| 转速与转频 | 源域 RPM；目标域约 600 rpm | 转速保留 rpm，另构造转频 Hz，数值为 rpm 除以 60 | 将机械转速与信号采样频率分开；目标转频约 10 Hz，仍带近似属性 |
| 几何量与故障尺寸 | 题面使用英寸 | 若统一到 mm，按 1 英寸 = 25.4 mm 换算；保留原值 | 同类长度量需同单位，缺损直径与滚动体直径仍是不同变量 |
| 载荷 | 0—3 马力 | 保留题面单位及档位；没有需要时不转为 kW | 不将载荷误读为转速或故障严重度标签 |
| 振动幅值 | 文件未说明物理单位、标定及增益关系 | 暂记“原始幅值，单位未确认” | 不自动乘以 9.81 或人为匹配两个域的能量 |

采样频率不同是采样条件差异，不是简单的单位不统一。当前脚本将源域目录给出的 12/48 kHz 记为“目录名义值”，不据此认定同文件每个辅助通道的真实时钟均已核验。涉及辅助通道频谱或跨通道相位分析前，应确认其采样口径。

### 6.2 幅值尺度差异已经出现，原因尚未确定

| 比较对象 | 文件/通道数 | 原始 RMS 最小值 | 中位数 | 最大值 |
|---|---:|---:|---:|---:|
| 源域 DE | 161 | 0.0631 | 0.2337 | 2.1438 |
| 目标域单通道 | 16 | 1.4620 | 8.8575 | 31.5228 |

两组 RMS 中位数相差约 37.9 倍。这里每条信号先计算 RMS，再取跨文件中位数，没有将较长文件赋予更多权重。这一差异可提示幅值尺度或域分布不同，但不能仅据数值判断目标域故障更严重，或认定差异恰由 g 与 m/s² 的换算造成。测点、运行条件、类别组成和采集增益均未被控制。

### 6.3 是否需要统一采样频率

若后续模型需要同一时间步长的原始波形输入，可以考虑共同采样率；若只比较物理含义一致的特征，也可以在各自原始采样率下提取，再统一特征定义和有效频带。

例如，以 12 kHz 作为共同波形采样率是一个可验证的候选方案：48 kHz 数据按 1/4、32 kHz 数据按 3/8 重采样，12 kHz 数据保持原采样率。降采样前需要抗混叠低通处理，并明确保留的有效频带。SciPy 的多相重采样将上采样、低通滤波和下采样组合实现，可用于该过程。[SciPy resample_poly 文档](https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.resample_poly.html)

这一候选方案会限制可直接保留的高频信息，尤其可能损失后续包络分析所需的高频共振成分，因此本次不预先固定为唯一方案。可以比较在原采样率上提取相关特征与先降采样的差别。把 12 kHz 上采样到 32 kHz 也不会新增原来没有的高频信息。

重采样要保持物理时长，且不能用普通线性插值替代必要的抗混叠处理。边界延拓和滤波瞬态需记录，不能把边界伪影当作故障冲击。同样，统一采样率只统一观测网格，不会消除源域约 1700—1800 rpm 与目标域约 600 rpm 的工况差异。

## 七、标准化、归一化、插值与编码的具体建议

### 7.1 按处理对象决定，不对整张数据表统一操作

| 处理对象 | 是否建议处理 | 为什么这样做 | 可能影响及控制方式 |
|---|---|---|---|
| 原始波形的均值 | 分析交流振动时生成去均值副本 | 减少直流偏置影响 | 保留原始均值与 RMS，避免丢失偏置信息 |
| 每个窗口的原始幅值 | 单位方差或单位 RMS 归一化作为候选，不是默认覆盖操作 | 弱化增益差异，使模型更关注波形形状 | 会抹去窗间能量差异；保留未归一化统计量并做对照 |
| RMS、峰峰值、频带能量等数值特征 | 形成特征表后，按模型需要逐列标准化 | 避免较大数值尺度支配距离、损失或正则化 | 标准化参数只由源域训练部分学习；测试和目标数据使用同一规则 |
| 重尾特征列 | 可比较中位数/IQR 稳健缩放，正值特征也可比较合适的对数变换 | 减少少量大值对尺度估计的支配 | 稳健缩放不是删除冲击；对数需说明参考尺度及零值处理 |
| 已经无量纲的特征，如峰度、能量比例 | 不因“无量纲”就一律跳过标准化 | 无量纲变量之间仍可能尺度不同 | 是否缩放由后续模型与训练验证决定 |
| 目标域信号或特征 | 使用明确、可复现的变换规则 | 保持源域与目标域特征含义一致 | 不把标准化当作已经完成域适配；目标域分布仍可能超出训练范围 |
| 缺失的采样值 | 当前不插值 | 没有 NaN/Inf 或已确认采样缺口 | 避免无依据地修改冲击 |
| 缺失 FE、BA | 不做波形插补 | 无法从“没有测量”恢复真实通道 | 若改用完整子集，需报告类别构成变化 |
| 缺失 RPM 字段 | 从可信文件名补入元数据 | 信息已存在，只是存储位置不同 | 记录来源，保留原文件 |
| N/OR/IR/B 标签 | 使用固定映射编码，例如 N=0、OR=1、IR=2、B=3 | 方便训练和还原预测结果 | 数值仅为类别标识，不表达大小；目标域未标注值独立处理 |
| 载荷、测点等类别变量 | 用于分组与审计；确需输入模型时再考虑独热编码 | 保留类别语义，不制造顺序关系 | 目标域不可获得的字段不直接作为预测输入 |
| 文件编号、路径、故障尺寸编码 | 保留为追溯元数据，不直接输入分类器 | 文件名中常含真实类别信息 | 防止模型读取答案或学习来源捷径 |

标准化指逐特征减去训练均值并除以训练标准差；稳健缩放使用训练中位数和四分位距。二者不会保证数据呈正态，也不消除两个域的条件差异。训练列若标准差或 IQR 为零，应标记常量或退化列并采用显式规则，不直接除以零。[StandardScaler](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.StandardScaler.html)、[RobustScaler](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.RobustScaler.html)

Min–Max 归一化只在后续输入确实需要固定范围时考虑。它不降低离群值本身的影响，训练范围之外的新数据也可能变换到 [0,1] 之外；默认强行截断可能遮蔽域差异。若使用，范围参数由训练部分确定，并记录超范围比例。[MinMaxScaler](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.MinMaxScaler.html)

### 7.2 先固定划分，再学习跨样本处理参数

建议以原始文件为基本分组，并将已确认关联或重复的记录绑定为同组，然后划分源域训练、验证和测试部分，再从各组内部切窗。同一文件的不同通道、相邻窗口和重叠窗口不能随机混入训练与测试两侧。若未来发现跨文件重复或同一次采集的拆分记录，分组边界还要扩大。

均值、方差、IQR、特征筛选、类别平衡和任何从一组样本学习的处理参数，应仅在当前训练折拟合，再应用于对应验证或测试折。固定公式的单位换算与单记录变换可以独立计算，但规则选择仍需遵守测试集隔离。scikit-learn 将使用测试数据拟合预处理列为数据泄漏来源。[数据泄漏与预处理说明](https://scikit-learn.org/stable/common_pitfalls.html#data-leakage)

源域正常状态只有 4 个原始文件。切成大量窗口不会增加独立正常记录数量；若要求每个测试折都包含独立正常文件，分组折数会受这 4 个文件限制。类别加权或训练内抽样可作为后续候选，不能在划分前复制或合成样本，也不能根据目标域 16 个文件推定四类各 4 个。

如果迁移方法明确使用无标签目标数据估计某些分布或归一化参数，应把它作为适配步骤单独声明，并说明评价的是这批目标文件的传导式诊断还是对未来新文件的泛化。不能在普通预处理里悄悄合并全部源域和目标域拟合参数。

## 八、建议重新构造的变量

以下是后续数据表设计建议；字段名称属于实现约定，不是新增的题目条件。

| 变量组 | 建议字段或含义 | 来源与用途 | 是否适合作为诊断输入 |
|---|---|---|---|
| 唯一标识 | 域、相对路径、通道名、原始记录编号、文件哈希 | 追溯与分组，解决同名文件问题 | 否，保留为索引 |
| 标签 | 工作状态、数值编码、标签是否已知 | 源域目录与题面说明，目标域保持未知 | 状态是预测目标，不是输入 |
| 测量条件 | 故障轴承位置、传感器位置、名义采样率及其来源、转速及其来源 | 区分故障发生位置和测点位置，记录近似/待核实信息 | 仅在预测阶段也可获得且经过验证时考虑 |
| 时序索引 | 采样点数、采样间隔、记录时长、窗口起止索引、窗口所属原始文件 | 确认采样率后构造；时长用点数除以采样率 | 主要用于切窗与结果归并，不让长度成为类别捷径 |
| 质量标记 | 通道是否可用、疑似削顶、偏置、局部能量变化、插补掩码 | 质量控制与敏感性分析 | 默认作为审计信息，避免缺失模式泄漏类别 |
| 几何与工况 | 缺损尺寸、滚动体尺寸、节径、载荷 | 保留单位与出处，支持机理分析 | 缺损尺寸等目标域未知信息不作必需输入 |
| 幅值统计 | 原始均值、原始 RMS、去均值 RMS、峰峰值 | 保存去均值或归一化可能损失的幅值信息 | 候选特征，跨域物理可比性待核验 |
| 形状与能量比例 | 峰度、峰值因子、定义一致的频带能量比 | 在确定窗口与频带后构造 | 候选特征，不预先声称有效 |
| 转速相关变量 | 转频；必要时用频率与转频之比表示阶次 | 降低纯 Hz 轴对转速的依赖 | 转速与故障几何仍有不确定性，需验证 |

对均匀采样信号，时间索引可从 0 开始按采样间隔递增；记录时长与最后一个采样点的时间应区分。目标域每条记录覆盖的采样长度为 8 s，最后一点的相对时间比 8 s 少一个采样间隔。

源域几何参数不能直接填入目标域。采用转速归一化频率或阶次分析也不自动解决轴承几何差异；目标域约 600 rpm 的不确定性会直接影响相关特征的位置。故障尺寸与滚动体直径应分别保留，不能合成一个含义模糊的“直径”变量。

## 九、建议执行顺序及后续验证

1. 保存原始文件，建立带哈希、单位和来源说明的数据索引；从文件名补齐可追溯的转速信息，统一标签编码。
2. 标记通道缺失、名义采样率待核实项，以及本报告发现的偏置、疑似削顶和时移相似通道；当前不删除原始记录。
3. 确定源域使用范围及通道方案，按原始文件和已知关联关系固定数据划分。
4. 明确共同时间尺度、有效频带和切窗方式；按所选分析目标生成去均值或重采样副本，保留变换参数及处理前指标。
5. 在训练部分形成特征并学习缩放参数；验证、测试和目标域使用已定义的处理规则，目标域适配另行标明。
6. 用相同划分比较去均值、波形归一化、重采样及可疑窗口处理的影响，再确定最终流水线。

| 待比较项 | 需要检查的影响 | 能支持的结论 |
|---|---|---|
| 保留幅值与逐窗归一化 | 特征稳定性、各类别召回、文件级误判 | 归一化是否改善当前验证任务，以及是否损失有用能量信息 |
| 原采样率特征与共同采样率 | 有效频带、冲击与包络特征变化、诊断结果 | 降采样是否损失当前任务所需信息 |
| 保留与屏蔽确认受损窗口 | 类别和文件数变化、结果稳定性 | 模型结论是否过度依赖可疑片段 |
| 普通与稳健特征缩放 | 数值范围、分组验证表现、极端特征的影响 | 哪种缩放更适合实际特征与模型 |
| 不同窗口长度 | 覆盖的转动周期、频率分辨能力、文件级结果一致性 | 时间尺度选择是否足够稳定 |

本次尚未训练诊断模型，因而不能给出某项预处理提高了多少准确率。上述影响有统计或信号处理依据，但其在本数据上的净收益需要通过后续对照确认。目标域没有真值，可检查稳定性和机理一致性，不能把这些检查转换成真实分类准确率。

## 十、证据、复现与范围

题目依据为原始 PDF 第 2—8 页的任务、数据说明及轴承机理；原题 SHA-256 为 `d13f851ffd1f5a2017de40ba1a63dcf82809989591450b40cbf73f9c92adef58`。本报告引用的外部资料仅用于解释统计检查与处理机制，不替代本地数据的事实依据。

逐通道统计见 `.analysis-work/data-quality/full/signal_statistics.csv`；字段、哈希及查重明细见同目录的 `audit.json`、`manifest.json`。重点信号复核见 `.analysis-work/data-quality/focused-review.json`，时移相关明细见 `.analysis-work/data-quality/shift-full/shifted_similarity.json`。程序使用 Python 3.12.14、NumPy 2.3.5、SciPy 1.18.1，无随机步骤，运行结束重新校验全部输入文件哈希。

从项目根目录复现基础检查的命令为：

```bash
/Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 .analysis-work/data-quality/audit_data.py
```

补充时移相关检查的复现命令为：

```bash
/Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 .analysis-work/data-quality/check_shifted_channels.py --output .analysis-work/data-quality/shift-full
```

本次新建分析文档，既有《题目分析报告》和《术语表格》保持原文。检查结论只覆盖已经执行的核查方法；采集标定、逐通道真实采样时钟、目标域真实标签及疑似削顶的仪器原因仍待进一步证据确认。
