# 问题一运行与代码说明

本次实现等转数包络表征，并保留固定一秒对照。两条后续研究路线共享问题一特征：推荐主线读取窗口表，最优传输对照读取文件中位数与四分位距表。问题二以后的分类、迁移和解释程序尚未执行。

## 1. 直接运行

在项目根目录，使用 Python 3.12：

    python -m pip install -r requirements-q1.txt
    python -m unittest discover -s tests -p 'test_q1_model.py' -v
    python 问题一_求解.py

已存在本程序输出时，可显式覆盖再现：

    python 问题一_求解.py --overwrite

需要保留上一批输出时，运行到独立目录：

    python 问题一_求解.py --output-root .analysis-work/q1-reproduction

真实小样本检验命令：

    python 问题一_求解.py --smoke --output-root .analysis-work/q1-smoke-new

小样本模式仅验证代码链；其频带选择和特征选择不作为正式结论。完整运行默认完成全部敏感性试验并绘图，未设置任何问题二分类器或问题三运输求解开关。

中文图需要可用的中文字体，如 Noto Sans CJK SC、微软雅黑、苹方或兰亭黑。本次环境使用的字体名写入图表清单。字体缺失或文字越界时程序停止出图，而不是生成带方框的图片。

## 2. 按题意执行的七个步骤

| 步骤 | 程序与函数 | 输入 | 实际输出与数学对应 |
|---|---|---|---|
| 数据读取 | read_inventory、load_record | 既有文件清单和原始MAT | 核心116源文件、16目标文件；逐文件SHA-256、数组长度及转速核验；(1.01)—(1.03) |
| 数据处理 | prepare_record、window_features | 原采样率去直流振动 | 对称FIR、反射延拓、希尔伯特包络、2000Hz包络、同时间原波形与完整窗口；(1.04)—(1.11) |
| 参数计算 | features_one_window、fisher_scores | 包络阶次质量与原波形 | 十二候选量、文件中位数和训练类间/类内统计；(1.12)—(1.25) |
| 模型求解 | choose_variant、stable_selection | 仅源训练文件 | 六候选的二折评分、100次文件重采样、非冗余特征集合；(1.26)—(1.29) |
| 结果检验 | validate_exports、run_sensitivity、测试脚本 | 冻结特征、尺度和数据出口 | 加权中心/方差、窗口唯一性、未知标签、转速/裁边/增益压力试验；(1.30)—(1.36) |
| 可视化 | utils/q1_plots.py | 实际数据与计算表 | 原始、过程、结果图和两张流程图；SVG与300DPI PNG |
| 结果导出 | 主程序、build_manifest | 数值表、参数和图 | CSV、JSON、报告、图表清单和输入/输出哈希 |

主程序负责流程顺序；utils/q1_model.py 是数值核心；utils/q1_plots.py 只读取计算结果绘图。特征顺序由 FEATURES 固定，特征中文名称、机理块、单位和公式编号在数据字典中一一对应。

## 3. 模型如何真正求解

这里是有限候选选择与统计计算，不需要人为设定神经网络层数或随机优化器。对每个候选频带和转数，先求每窗特征，再按原文件求中位数；训练内部二折给出每个候选的实算分数。取得最高分后，在全部70个训练文件上作稳定筛选，继而拟合文件等权标准化。排序和同分规则、随机种子、分位数定义、卷积边界及数值阈值均已固定。

频带与转数的选择终止于6个组合全部计算完成；稳定选择固定为100次重采样，没有根据目标图像效果延长或提前终止。FFT、FIR和统计量均由明确公式直接计算，不存在未披露的“模型已收敛”判断。

一秒对照使用主线选中的频带与特征集合，但单独用源训练一秒窗口拟合标准化。转速漂移同时导出“各自尺度”和“统一主线尺度”，正式比较图使用后者。

## 4. 当前预处理怎样适配模型

- 从原始MAT读取并核验哈希，在原采样率下解调，保留高频共振载波；包络提取完成后才降到2000Hz。
- 从已有文件划分重新切等转数窗口，所有派生窗继承原文件归属。已有一秒预处理表不冒充新窗口。
- 按同一窗口时间截取原波形；各文件FFT长度可不同，再把功率插到共同阶次网格。插值用于坐标对齐，不填补原始缺失值。
- 原波形幅值量保留原尺度，再用源训练数据拟合标准化；谱质量在窗内归一。两种操作对象不同，均记录在模型公式中。
- 不删除故障冲击，不进行无依据单位转换，不用目标文件编号编码类别。原始与既有预处理目录只读，新表另存。
- 源训练仅有2个正常文件，采用按文件分层二折与文件重采样；不会通过切更多窗口宣称增加正常类独立样本数。

## 5. 检验的含义

测试使用已知60Hz调制、20Hz转频的合成信号，检查主峰应为3阶；同时比较12/32/48kHz采样的包络指标。增益测试重新经过完整滤波链，检查幅值齐次和归一化指标不变。另用不等窗口数的两文件例子核验文件等权，并修改所有非训练数据的数值和标签确认候选选择不变。

这些测试回答实现是否符合数学合同。源域诊断精度与迁移效果需要问题二、三的独立评价；本次以真实敏感性结果决定是否支持“更稳健”的表述。

模型选择、标准化和特征筛选都是拟合的一部分。问题二内部交叉验证必须用当前内折训练文件重新执行拟合步骤；不能直接将本阶段用全部70个训练文件拟合的特征表输入交叉验证，再声称完全没有信息泄漏。当前未标准化窗口表只有最终频带与转数；它支持重新筛选特征和拟合尺度。需要重新选择频带或转数时，应从原始MAT重算六个候选组合，具体的分层拟合方案在问题二中定义。

## 6. 数据出口和审计证据

数据结构见 [数据说明](模型适配数据集/q1/README.md)；本次实算结论见 [结果报告](results/q1/问题一_结果报告.md)；图像见 [图表面板](figures/q1/图表面板.html)。

复现清单保存实际依赖版本、种子42、运行命令、132个原始输入以及模型/配置/代码哈希、输出哈希。中间调试产物留在忽略提交的 .analysis-work/ 中，正式交付不依赖该目录中的数据或代码。
