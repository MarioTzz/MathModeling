# E题：高速列车轴承智能故障诊断问题——题目分析报告

> 阶段：建模分析与实验设计。对象：2025 年中国研究生数学建模竞赛 E 题。本文依据用户提供的 9 页题目 PDF、当前目录下全部 177 个 MAT 文件的只读盘点及已核验的方法文献编写。已检查变量、长度、缺失元数据和基础幅值统计；尚未训练诊断模型，不包含模型准确率或 A–P 的预测标签。参数候选均为待验证方案。

## 1. 题意判断与分析结论

本题是一个**源域有标签、目标域无标签的四分类迁移诊断问题**。研究对象不是列车速度预测，也不是轴承剩余寿命预测，而是根据振动信号判别正常状态 N、外圈故障 OR、内圈故障 IR 和滚动体故障 B。真正的难点在于：台架与列车之间的转速、结构、传感器、噪声和类别比例均可能不同，源域识别准确不意味着迁移到列车后仍准确。

建议主线为：机理引导的共享特征提取 → 按独立记录分组验证的加权 SVM → 正则化 CORAL 域适应 → 文件级分类、稳定性分析及故障机理解释。题目没有强制采用深度网络；在仅 161 个源域文件、4 个正常文件、16 个无标签目标文件的条件下，优先建立低维、可追溯的方案。是否需要更复杂模型，应由后续跨工况实验决定。

本轮已确定问题结构、实际文件组成与通道、基础质量情况、公式、模型输入输出及验证路径，并初步划定 116 个源域候选文件。最优频带与窗长、最终质量筛选、模型参数、迁移收益以及 A–P 的具体标签，仍需后续特征分析和真实实验裁决。

## 2. 输入证据与题面事实

### 2.1 文件和证据清单

| 材料 | 当前状态 | 本轮用途与边界 |
|---|---|---|
| `E题：高速列车轴承智能故障诊断问题.pdf` | 已完整提取文字并逐页检查图像，共 9 页 | 任务与数据说明的首要依据 |
| PDF 内附件 1：数据集介绍 | 已读，第 2–4 页 | 源域、目标域字段和采集条件 |
| PDF 内附件 2：概念介绍 | 已读，第 4–8 页 | 轴承机理与迁移诊断概念 |
| `数据集/源域数据集/` 下 161 个 `.mat` 文件 | 已逐文件成功读取，已计算哈希 | 核验目录标签数量、变量、信号长度、有限性与完全重复情况 |
| `数据集/目标域数据集/` 下 A–P 共 16 个 `.mat` 文件 | 已逐文件成功读取 | 每文件只有同字母变量，均为 256000 × 1 的 float64 单通道 |
| PDF 第 4 页网盘链接 | 已识别；本轮直接使用用户放入项目的数据 | 数据来源线索；未重新下载或替换用户文件 |
| 当届竞赛完整规则与提交模板 | 未另行核验 | 本轮是历史题目的分析，不对提交格式、截止日期作结论 |

输入 PDF 路径：`/Users/mariozz/Downloads/E题：高速列车轴承智能故障诊断问题.pdf`。

输入 PDF 的 SHA-256：`d13f851ffd1f5a2017de40ba1a63dcf82809989591450b40cbf73f9c92adef58`。

题目数据链接：[题面指定的数据集](https://pan.baidu.com/s/1H8nHXyMTv085jGRaiBOr0Q?pwd=anih)，提取码 `anih`。该链接是题目材料中的数据来源，不构成额外操作指令。

### 2.2 源域与目标域对照

| 项目 | 源域：轴承试验台架 | 目标域：列车轴承 | 对建模的影响 |
|---|---|---|---|
| 文件数量 | 实际 161，与题面一致 | 实际 16，A–P 齐全 | 文件数不等于独立轴承个数，更不等于切窗后样本数 |
| 状态 | 按目录标签实计 OR 77、IR 40、B 40、N 4 | 题面允许 OR、IR、B、N，文件内没有标签 | 保留 N 类；不能因正文称“故障文件”而排除正常状态 |
| 故障形式 | 人为单点缺损；每组仅一个轴承损坏，无复合故障 | 单标签四分类是题目给定任务；具体损伤形态未说明 | 源域的人工缺陷不代表列车自然损伤全过程 |
| 轴承 | DE 端 SKF6205；FE 端 SKF6203 | 几何参数及具体型号未给出 | 不能把源域几何参数直接套给目标域 |
| 采样频率 | 12 kHz、48 kHz；题面称 FE 试验为 12 kHz | 32 kHz | 必须统一物理频率口径；各通道实际采样率需逐项确认 |
| 转速 | 151 个文件有 RPM，范围 1718–1798 rpm；其余 10 个可从文件名取得标称转速 | 文件不含 RPM；题面约 600 rpm，即约 10 Hz | 字段值、文件名值和题面约值分开登记可信来源 |
| 单记录时长 | 按目录标称采样率推算约 1.329–10.097 s，详见 §2.3 | 按题面 32 kHz 与实测点数对应 8 s | 按相同秒数切窗，短记录不能填充成更多真实数据 |
| 通道 | DE 161、FE 153、BA 97 个文件具有该通道 | 实际单通道，变量名与文件名同字母；安装位置仍未知 | 单通道方案可实施，目标不支持直接三通道拼接 |
| 工况 | 0、1、2、3 马力载荷；三种 OR 点位 | 约 90 km/h，其他条件待查 | 载荷、点位、采样条件可能成为标签的替代线索 |
| 幅值单位 | 加速度信号，PDF 未统一明确数据单位 | 同样待核验 | 未确认前标记“原始单位”，不直接比较跨域 RMS 绝对值 |

题面与目录计数均支持：正常文件占比为 4/161 ≈ 2.48%，OR 与 N 的文件数之比为 19.25:1。所有目标数组实际长度为 256,000 点，与题面 32,000 × 8 一致；采样率和时长仍来自题目说明，目标 MAT 内没有独立采样率或时间戳字段。

源域故障尺寸：OR 为 0.007、0.014、0.021 in；IR 与 B 另有 0.028 in。尺寸和 OR 的 3/6/12 点钟位置是试验条件，不是本题要求新增的类别。主标签固定为四类，严重程度仅作分层验证变量。

### 2.3 当前目录数据的实查结果

数据根目录为 `/Users/mariozz/MathModeling/数据集`，177 个 MAT 文件合计 373,997,711 字节（约 374 MB）。全部通过 SciPy MAT 读取；共 427 条振动数组，均为 float64 列向量，其中源域 411 条、目标域 16 条。151 个 RPM 字段均为 1 × 1 的 uint16。

| 子目录 | 文件数 | OR | IR | B | N | DE 信号点数与按目录采样率推算的时长 |
|---|---|---|---|---|---|---|
| `12kHz_DE_data` | 60 | 28 | 16 | 16 | 0 | 全部 96000 点，8 s |
| `12kHz_FE_data` | 45 | 21 | 12 | 12 | 0 | 44 个为 96000 点；1 个为 121168 点，约 10.097 s |
| `48kHz_DE_data` | 52 | 28 | 12 | 12 | 0 | 47 个为 192000 点（4 s）；另 5 个为 63788–130549 点（约 1.329–2.720 s） |
| `48kHz_Normal_data` | 4 | 0 | 0 | 0 | 4 | N_0 为 243938 点（约 5.082 s）；另 3 个为 384000 点（8 s） |
| 目标域 A–P | 16 | 未知 | 未知 | 未知 | 未知 | 每文件 256000 点，按题面 32 kHz 对应 8 s |

具体质量和元数据发现如下。

- **可读性与数值完整性**：177 个文件全部可读，427 条振动数组未发现 NaN、Inf 或整条恒定信号；文件字节和完整信号数组的 SHA-256 均未发现重复。该结论不排除短片段重复、近重复、局部削顶或同一物理轴承跨工况重复试验。
- **通道缺失**：8 个 0.028 in 的 DE 故障文件只有 DE 通道，因此 FE 共 153 个；BA 仅在 12 kHz DE 的 52 个文件和 12 kHz FE 的 45 个文件中出现，共 97 个。每文件已提供的振动通道长度一致，但相同长度本身不能证明所有通道原始采样硬件配置相同。
- **转速缺失字段**：缺 RPM 的是 DE 目录下 B028、IR028 各四个载荷文件，以及正常 N_1、N_2；文件名分别提供 1797/1772/1750/1730 rpm 或 1772/1750 rpm。读取时优先真实 RPM 字段，无字段时采用文件名并标记来源，不填总体平均转速。
- **短记录**：`48kHz_DE_data/IR/0014/IR014_0.mat` 的 DE/FE 都只有 63788 点，标称时长约 1.329 s。2 s 窗长方案不能覆盖它；比较窗长时必须报告有效文件覆盖，并在相同文件集合上比较性能，不能隐性排除难样本。
- **目标输入接口**：`A.mat` 的变量是 `A`，依此至 `P.mat` 的 `P`，均无采样率、RPM、单位、型号或标签字段。目标文件 `B.mat`、`N.mat` 的字母不表示故障 B 或正常 N。
- **幅值尺度**：未去均值的原始目标 RMS 范围约为 1.4620（O）至 31.5228（C），约相差 21.6 倍。此处只用于提示幅值差异，单位未知且不能据此分配标签。跨域绝对幅值对比仍需确认传感器单位和增益。
- **试验独立性**：源域变量中的 X 编号逐文件不同，但它们是记录标识，不足以证明每个文件来自不同的物理轴承。正常 4 个文件在载荷上各一例，物理轴承身份仍未确认。

数据清单指纹为 `5534359368da9f9d1d4b049d58b9682ea11b63f5f79b840c8db8e188d85b7c9f`，由相对路径排序后各行“路径 + 制表符 + 文件 SHA-256”拼接计算。内部证据位于 `.analysis-work/dataset-audit.json`；只读盘点脚本为 `.analysis-work/audit_dataset.py`。采样率现仍采用题面和目录标称值，未把它写成 MAT 内已存在的元数据。

## 3. 四项任务与模型合同

### 3.1 子问题拆解

| 子问题 | 应回答的结论类型 | 必需输入 | 预期输出 | 主要验证口径 |
|---|---|---|---|---|
| 问题 1：数据分析与特征提取 | 选了哪些源域记录、为何适合迁移；哪些特征反映冲击、调制和频率结构 | 源域信号、标签、元数据；目标域无标签信号和采集信息 | 文件筛选清单、共享特征字典、全体入选记录的特征表、代表性信号分析 | 数据质量、频带可比性、跨工况稳定性、训练折内判别能力 |
| 问题 2：源域诊断 | 模型能否识别未参与训练的独立源域记录 | 问题 1 的共享特征与源域标签 | 固定训练/测试分组、文件级预测、混淆矩阵、分类指标 | 文件或试验组隔离；宏平均 F1、平衡准确率、各类召回率 |
| 问题 3：迁移诊断 | 无标签适应如何利用源域知识；A–P 各属于哪一类，哪些判断不稳定 | 源域共享特征及标签、目标域共享特征 | 16 行文件级预测表、迁移前后对照、置信与稳定性说明 | 隐藏标签的源域代理迁移实验；目标域仅作无标签诊断检查 |
| 问题 4：可解释性 | 哪些信号成分支持预测，适应改变了什么，解释能否经扰动检验 | 已冻结的迁移模型、信号、共享特征、元数据 | 迁移变化分析、特征组贡献、代表案例及不确定案例 | 机理一致性、解释稳定性、相关频带与对照频带遮蔽实验 |

四问是有依赖的证据链。问题 1 的数据选择和特征映射服务于迁移；问题 2 提供可比较的分类器；问题 3 复用该分类器家族；问题 4 必须解释实际用于问题 3 的模型。

### 3.2 统一合同

| 合同项 | 约定 |
|---|---|
| 任务定义 | 四分类；最终决策单位为原始文件或确认的独立试验组，窗口仅是内部计算单位 |
| 数据 | 输入文件只读；建立文件哈希、试验组、标签、通道、采样率、转速和质量台账 |
| 结论类型 | 分类和迁移效果分析，不预写准确率、不推断寿命，不将模型置信度称为安全概率 |
| 模型 | 问题 1：一个机理引导信号表征框架；问题 2：一个加权核 SVM 家族；问题 3：CORAL 特征变换与复用 SVM 两个串联部分；问题 4：机理和扰动解释，不新增分类器 |
| 数学定义 | 第 5–9 节；全局符号以 `术语表格.md` 为准，说明性下标用正体 |
| 实现 | 优先 Python；先数据清单与最小完整链路，再参数选择、全量实验及出图 |
| 验证 | 全部数据驱动步骤限制在训练折；源域测试集冻结；真实目标域不使用标签；输出文件级指标 |
| 风险 | 重点防范泄漏、正常类支持不足、未知几何参数、工况混淆、负迁移和无标签评价失真 |

## 4. 数据审计与预处理方案

### 4.1 已执行检查与后续深化顺序

1. 已列出全部文件并计算 SHA-256、核对题面数量和实际变量，初步结果见 §2.3；后续仍需核对原始采集元数据与目录标签。`time` 后缀表示时间序列信号，本次文件内未发现另设的时间戳列。
2. 逐文件记录所有变量名、形状、数据类型、采样率来源、各通道长度、RPM、载荷、故障端、故障尺寸和点位。`.mat` 版本不同可能需要 `scipy.io` 或 `h5py`。
3. 检查非有限值、空序列、恒定段、截断、饱和、长度不符、重复与近重复波形；将“无法读取”和“质量较低”分开记录。强冲击可能是真实故障证据，不能机械地按三倍标准差删除。
4. 确认同一原始采集的多通道、重采样版本或重复文件是否属于同一组；若有物理轴承/试验编号，优先以该编号分组。只有文件名时，将文件分组结果表述为较弱的独立性保证。
5. 按“类别 × 载荷 × 故障端 × 采样率 × 尺寸”统计覆盖；检查类别是否与采样率或传感器位置绑定。统计实际独立组数量，尤其是 N 类。
6. 数据质量统计和窗口示例可全量只读展示；任何会影响模型的特征选择、阈值、频带选择和超参数，只能在指定训练/验证流程中确定。

### 4.2 源域筛选的具体规则

按实际目录和字段，首轮主数据集候选已明确为 `12kHz_DE_data` 60 个、`48kHz_DE_data` 52 个及正常 4 个，共 **116 个文件**，构成为 OR 56、IR 28、B 28、N 4。各文件均有有效 DE 数值序列，使用其 DE 通道，保留已覆盖的载荷、尺寸与 OR 点位。故障端含义依据题面和目录，物理采集元数据仍待进一步核验；这是一份可实施的初筛集合，不代表全部信号质量已通过最终验收。

优先保留所有通过质量检查的正常独立记录；故障类通过文件权重控制占比，避免为了平衡而丢失大量工况。保留 12 kHz 与 48 kHz 记录时先统一频带。实际 FE 故障目录共 45 个文件，只有 OR、IR、B 三类、没有正常文件，可作为三类跨故障端压力测试；不能声称它提供了四分类跨轴承验证。

若 DE 子集不能覆盖四类或正常支持不足，回退到全部合格源域记录，按故障端与观测通道分层报告；不能默认“DE 传感器测到的就一定是 DE 故障”。若目标传感器信息显示 DE 通道缺乏代表性，再通过源域代理迁移验证比较通道方案。无目标标签时，源目标分布接近仅是辅助依据，不能用它删除源域某一类别。

纳入 FE 故障文件也不会增加正常独立组数；正常支持不足必须通过降低验证复杂度、限制结论或补充独立正常数据解决。

题面源域字段与 CWRU 官方资料相符，但未逐文件核验前不将“官方全量 CWRU 数据”与“题目给定 161 个文件”视为同一集合。[R6]

### 4.3 统一频带与切窗

主方案先采用 12 kHz 共有采样率：48 kHz 信号经抗混叠低通后按 1/4 重采样，32 kHz 目标信号按 3/8 有理重采样，12 kHz 信号保持采样率。保守共有通带候选上限为 5 kHz，为 6 kHz 奈奎斯特频率预留过渡带。5 kHz 是工程候选，不是题目规定；应检查原始高频能量并在源域代理任务比较是否丢失关键共振信息。

重采样统一的是时间网格和可见带宽，**不能消除转速、结构和载荷差异**。上采样也不会补出原始 12 kHz 数据中不存在的高频信息。若共有频带方案明显损失可诊断信息，只在支持四类独立验证的前提下另选高采样率源域子集，重新建立公平对照，不把多个不同采样率的 FFT 下标直接拼接。

先划分独立组，再处理各组内窗口。初始候选窗长为 0.5、1、2 s，主起点为 1 s；训练可使用 50% 重叠，测试和最终文件聚合优先使用无重叠有效窗口。同组窗口不得跨训练、验证、测试集合；过滤器边缘和不足一个窗的尾部按固定规则剔除并登记。

目标约 10 Hz 转频下，1 s 窗包含约 10 转，频率分辨率约 1 Hz，名义阶次分辨率约 0.1；这些仍是设计推导。按文件记录实际有效窗口数，若没有有效窗口则报数据错误，不能凭空预测。

去均值和必要的去趋势用于去除偏置；带通与 Hilbert 包络用于突出冲击调制。保留原幅值分析支路和尺度无关特征支路：不在计算 RMS 后再声称经每窗单位方差归一化的 RMS 有诊断意义。幅值单位未统一时，原始 RMS 仅用于同一采集条件下的比较。

## 5. 故障机理与数学口径

### 5.1 信号机制

局部损伤使滚动体经过缺陷时产生重复冲击，冲击经过结构传递形成衰减振动；观测还叠加转动相关成分与噪声。由此需要同时观察冲击性、包络频谱、谐波和边带，而不是仅比较原始信号最大值。包络分析的理论依据见 [R1]，故障频率口径见 [R2]、[R3]。

| 状态 | 机理线索 | 推荐观察 | 不能据此直接断言 |
|---|---|---|---|
| OR | 外圈固定、缺陷与载荷区相对位置较稳定 | BPFO 及其谐波附近的重复冲击 | 外圈故障一定等幅，或幅值一定最大 |
| IR | 内圈随轴旋转，缺陷周期通过载荷区 | BPFI 谐波及以转频为间距的调制边带 | 仅出现转频峰就一定为内圈故障 |
| B | 缺陷滚动体自旋并随保持架公转 | BSF、2BSF 相关成分及 FTF 调制 | 任意高峭度波形一定为滚动体故障 |
| N | 没有足够支持局部故障的证据 | 相对平稳的统计量、缺陷频率证据较弱 | 幅值小就一定正常，或正常信号没有谱峰 |

### 5.2 特征频率与题面歧义

以转速 $r$（rpm）、转频 $f_{\mathrm r}=r/60$（Hz）、滚动体数 $Z$、滚动体直径 $d$、节径 $D$ 和接触角 $\theta$ 表示，外圈固定、内圈旋转且近似纯滚动时：

$$
f_{\mathrm{BPFO}}=\frac{Z}{2}f_{\mathrm r}\left(1-\frac dD\cos\theta\right),\qquad
f_{\mathrm{BPFI}}=\frac{Z}{2}f_{\mathrm r}\left(1+\frac dD\cos\theta\right).
$$

$$
f_{\mathrm{BSF}}=\frac{D}{2d}f_{\mathrm r}\left[1-\left(\frac dD\cos\theta\right)^2\right],\qquad
f_{\mathrm{FTF}}=\frac12 f_{\mathrm r}\left(1-\frac dD\cos\theta\right).
$$

上述通用口径来自 [R2]、[R3]。题目 PDF 第 5 页表 2 把滚动体一项写成 $f_{\mathrm r}(D/d)[1-(d/D)^2]$ 并标为 BSF；在 $\theta=0$ 时，它等于上述自旋频率的两倍。SKF 资料区分球缺陷冲击频率与球自旋频率，[R3] 也讨论一周自旋对内外圈的两次冲击。

本文保留两种定义并显式映射：

$$
f_{\mathrm{B,stmt}}=2f_{\mathrm{BSF}}\quad(\theta=0).
$$

下标 stmt 表示题面（statement）定义。后续同时检查 BSF 与 2BSF，图注注明采用哪一种；不静默改写题目，也不把倍频现象当作额外类别。PDF 第 3 页用 $n$ 表示滚动体数、第 5 页又用 $n$ 表示转速，本报告改用 $Z$ 与 $r$ 避免一符多义。

将频率除以转频，得到阶次 $q=f/f_{\mathrm r}$。按题面源域参数、取 $\theta=0$，得到下表；这是理论计算值，不是实测谱峰：

| 轴承 | $Z$ | $d$ / in | $D$ / in | BPFO 阶次 | BPFI 阶次 | BSF 阶次 | 2BSF 阶次 | FTF 阶次 |
|---|---|---|---|---|---|---|---|---|
| SKF6205 | 9 | 0.3126 | 1.537 | 3.584776 | 5.415224 | 2.356722 | 4.713443 | 0.398308 |
| SKF6203 | 9 | 0.2656 | 1.122 | 3.434759 | 5.565241 | 1.993839 | 3.987677 | 0.381640 |

阶次归一化只消除转速尺度，不能消除 $Z$、$d/D$ 和接触角差异。目标轴承几何参数未知，因此不能拿本表乘以目标 10 Hz 后就把结果称为列车轴承的理论故障频率。

## 6. 问题 1：机理引导的共享特征

### 6.1 两类用途，统一输入维度

**共享诊断特征**应同时能在源域和目标域提取，优先采用无量纲冲击统计、相对带能量、包络形态和宽阶次区间信息。**几何相关证据**利用源域已知参数定位 BPFO/BPFI/BSF/FTF，用于代表性样本解释和特征设计检验；在目标几何缺失时，不将其作为跨域主分类器的必需输入。

如果后续获得可靠目标轴承参数，可增补两域同义的故障阶次邻域能量比，再重新验证整条模型链。不得在源域把真实故障位置用于挑选输入频带，却在目标域没有同等输入条件；若按某种故障频率构造候选特征，必须对每一条记录都计算所有类别候选，不借真实标签选择特征。

### 6.2 特征字典与定义

令 $x_\ell$ 为一个已去均值窗口中的第 $\ell$ 个加速度值，窗口包含 $L$ 个采样点。基础统计量定义为：

$$
A_{\mathrm{RMS}}=\sqrt{\frac1L\sum_{\ell=1}^{L}x_\ell^2},\qquad
K=\frac{L^{-1}\sum_{\ell=1}^{L}x_\ell^4}{(L^{-1}\sum_{\ell=1}^{L}x_\ell^2)^2},\qquad
C_{\mathrm f}=\frac{\max_\ell |x_\ell|}{A_{\mathrm{RMS}}}.
$$

$K$ 使用 Pearson 峭度，不减 3。全零或近恒定窗口须先标记质量问题，不用分母加常数掩盖其无效性。偏度为三阶矩除以标准差的三次方；波形因子为 RMS 除以绝对均值；脉冲因子为绝对峰值除以绝对均值；裕度因子为绝对峰值除以“绝对值平方根的均值”的平方。

带通信号记为 $x_{\mathrm b}(t)$，包络及归一化频带能量为：

$$
a(t)=\left|x_{\mathrm b}(t)+\mathrm j\mathcal H[x_{\mathrm b}](t)\right|,\qquad
E(\mathcal B)=\frac{\sum_{f_k\in\mathcal B}P_k}{\sum_{f_k\in\mathcal F}P_k}.
$$

$\mathcal H$ 为 Hilbert 变换，$\mathrm j$ 为虚数单位；$P_k$ 为同一窗口、同一谱估计口径的非负谱功率，$\mathcal F$ 是冻结的分析频段。包络谱先去掉包络均值/DC。离散谱熵为

$$
H_{\mathrm s}=-\frac{\sum_{k=1}^{M}p_k\ln p_k}{\ln M},\qquad
p_k=P_k\big/\sum_{j=1}^{M}P_j,
$$

其中 $M>1$，零概率项按 $0\ln0=0$ 处理；所有记录使用相同频率或阶次栅格及频段。$P_k$ 若来自 PSD，等间隔频率上的积分宽度在能量比中抵消；不比较不同归一化方式的谱幅值。

| 特征组 | 候选量 | 跨域使用规则 |
|---|---|---|
| 时域冲击性 | 峭度、偏度、峰值因子、波形因子、脉冲因子、裕度因子 | 无量纲，可进入共享输入；检查共线性与重复信息 |
| 幅值 | RMS、标准差、峰峰值 | 单位待查；主方案仅作诊断辅助，单位与增益可靠时才纳入对照 |
| 相对频谱 | 共有通带中等宽子带的能量比、归一化谱重心、谱熵 | 主分析采用固定物理频带；反映共振形态，但不能当作天然域不变特征 |
| 包络与阶次 | 包络谱熵；宽阶次区间能量比；主要包络峰阶次 | 初始宽区间为 (0,2]、(2,4]、(4,8]、(8,16]、(16,32]；范围是待验证工程候选，不对应固定故障类别 |
| 时频稳定性 | 窗内短时能量的变异系数 | 短时帧长候选 0.1 s，所有记录保持同一秒数定义；检查短窗是否足以观察调制 |
| 机理证据 | BPFO/BPFI/BSF/2BSF 的谐波邻域能量、转频及 FTF 边带 | 仅在几何已知时计算明确物理位置；不作为未知几何目标的硬输入 |

带通初始候选为共有带宽内 1–5 kHz，并比较 1–3、3–5 kHz 子带；不预先认定某个频带最佳。理论频率邻域半宽可按“至少一个频率栅格宽度 + 转速不确定性造成的频率变化”设置，滑移影响通过源域实测峰宽确定；不采用无依据的固定命中阈值。

可复现的首轮起始配置为：1 s 窗、12 kHz 采样率；去均值后对整窗加 Hann 窗，用单边修正周期图估计 PSD，不以补零声称提高真实分辨率。原始谱采用 (0,5] kHz，划为五个 1 kHz 等宽带；先线性插值到共同 2 Hz 栅格，再计算能量比与谱熵。归一化谱重心定义为 $\sum_k f_kP_k/(5000\sum_kP_k)$，频率均用 Hz。包络去均值后的 PSD 按 $q=f/f_{\mathrm r}$ 映射到共同 0.25 阶栅格，分析 (0,32] 阶并使用上表五个宽阶次带；主峰取该区间最大谱功率对应阶次。公共栅格是统一计算口径，不创造频率信息。六个时域无量纲量、五个原始子带能量比、原始谱重心与谱熵、五个包络阶次能量比、包络谱熵与主峰阶次、短时能量变异系数合计 21 个起始特征；后续删冗余后维度可减少。窗长、频带和特征取舍只能按 §8.3 的开发流程调整。

共享向量记为 $\mathbf z=\phi(x)$，维度为 $p$。先控制到几十维以内；在训练折内删除常量和高度冗余项，冻结特征名称、顺序、单位、缺失处理、频带和转速来源。目标文件中的几何未知字段不靠填 0 伪造，否则 0 会被误解为真实低故障能量。

### 6.3 代表样本与输出

代表样本按四类、载荷和尺寸覆盖选择，不只挑最好识别的个例。源域几何已知时，将实际 RPM 对应的理论频率叠加到包络谱上；目标图只有在几何核验后才画带明确类型含义的理论线。

计划输出两张表：文件清单记录入选/排除及原因；窗口特征表记录原始文件、分组、窗口起止时间、标签来源、共享特征和质量标志。问题 1 中“整体数据集”按完整的入选源域集合处理；同时用同一映射处理所有合格目标文件，为问题 3 作准备。

## 7. 问题 2：源域加权 SVM 与有效验证

### 7.1 划分原则优先于训练

建立一次冻结的源域测试组，候选占比约 20%–25%，但独立组与四类覆盖比比例更重要。若 4 个正常文件确认为 4 个独立组，可保留 1 个正常组测试、其余 3 个供训练和内部验证；这是条件方案。若正常独立组少于该数量，须减少折数或承认不能获得完整四分类独立估计，不能把同一个正常文件切片分给多折。

训练集内部交叉验证折数不得超过训练集中最少类别的独立组数；仍须逐折检查四类在训练侧均出现。默认候选为 3 折；不足时采用可行的分组留出，不强行使用 5 折。测试组不参与参数调整、标准化、特征选择或问题 3 的代理任务选择。

若同一物理轴承在多载荷下重复采集，它们应合并为一个组。单纯“留一载荷”可能仍然共享轴承个体；有元数据时应清除跨域重复个体，无元数据时将该实验注明为跨记录/工况检验，而不是未见轴承泛化检验。

### 7.2 模型定义

对 $c\in\mathcal C=\{\mathrm N,\mathrm{OR},\mathrm{IR},\mathrm B\}$ 建立一对其余类的二分类 SVM。将对应二值标签记为 $y_{ic}^{\pm}\in\{-1,+1\}$，决策得分为 $s_c(\mathbf z)$。RBF 核定义为

$$
k(\mathbf z,\mathbf z')=\exp\left(-\gamma\|\mathbf z-\mathbf z'\|_2^2\right),\qquad\gamma>0.
$$

输入先使用训练折均值和标准差标准化，常量列剔除；每折独立拟合，验证/测试仅应用已有变换。基本 SVM 理论依据为 [R4]。

设训练集中类别 $c$ 有 $F_c$ 个独立文件组，组 $i$ 有 $m_i$ 个有效窗口，组标签为 $y_i$。若一组含多个同标签文件，将其窗口一起计数。窗口权重和优化问题为

$$
v_{iw}=\frac{1}{4F_{y_i}m_i},\qquad
\min_{\boldsymbol\beta_c,b_c,\xi}\ \frac12\|\boldsymbol\beta_c\|^2+C\sum_{i,w}v_{iw}\xi_{iw},
$$

$$
y_{ic}^{\pm}\left(\boldsymbol\beta_c^{\mathsf T}\psi(\mathbf z_{iw})+b_c\right)\ge 1-\xi_{iw},\qquad \xi_{iw}\ge0.
$$

$\psi$ 是核隐式映射。权重使四类总权重各为 1/4、同类文件组贡献相同，同一长记录不会仅因窗口更多而主导训练。工程实现传入显式样本权重，不再叠加默认类别平衡造成重复加权；若实现将权重归一化为均值 1，则必须同步换算 $C$ 的口径。

初始参数网格可取 $C\in\{0.1,1,10,100,1000\}$、$\gamma\in\{0.1/p,1/p,10/p\}$，按文件级宏平均 F1 选参；这是搜索起点，不是已获最优值。采用标准库二次规划求解器，初始容差可取 $10^{-3}$，所有收敛警告必须记录并解决。线性核只作为同一 SVM 家族的简化对照。

### 7.3 聚合、置信度与评价

主分类结果将同一文件的无重叠窗口得分平均后取最大类：

$$
\bar s_c(g)=\frac1{m_g}\sum_{w=1}^{m_g}s_c(\mathbf z_{gw}),\qquad
\widehat y_g=\arg\max_{c\in\mathcal C}\bar s_c(g).
$$

同一试验组含多个文件时，测试还应报告组级等权结果。窗口数量不能作为置信区间的独立样本量。

如需概率列，使用训练部分的**分组折外文件级得分**拟合温度 $T>0$：

$$
p_c(g)=\frac{\exp[\bar s_c(g)/T]}{\sum_{a\in\mathcal C}\exp[\bar s_a(g)/T]}.
$$

温度以文件等权负对数似然最小化求得，候选搜索范围 $[0.05,20]$，边界命中则记录并复查；四类独立校准样本不足时仅报告决策得分、间隔和窗口一致率，不称其为可靠概率。避免直接依赖不按组划分的内部概率校准。源域温度不能保证目标域校准成立。

校准参数必须绑定完整模型版本。CORAL 后重训的 SVM 不能直接沿用未适应 SVM 的温度；若要输出其概率，必须对包含标准化、CORAL 和重训分类器的完整适应规程，重新生成不使用评价组标签的分组折外文件得分并单独校准。当前数据规模下，目标结果默认提供四类得分、得分间隔和一致率，不预设概率列有效。

以文件级混淆矩阵得到 $\mathrm{TP}_c$、$\mathrm{FP}_c$、$\mathrm{FN}_c$：

$$
F_{1,c}=\frac{2\mathrm{TP}_c}{2\mathrm{TP}_c+\mathrm{FP}_c+\mathrm{FN}_c},\quad
\operatorname{MacroF1}=\frac14\sum_{c\in\mathcal C}F_{1,c},\quad
\operatorname{BA}=\frac14\sum_{c\in\mathcal C}\frac{\mathrm{TP}_c}{\mathrm{TP}_c+\mathrm{FN}_c}.
$$

同时报告总体准确率、各类 precision/recall/F1、每类独立测试组数和计数/行归一化混淆矩阵。零分母须说明：缺乏真实支持的类不能把召回率记作已评估的 0 或 1；相应四类宏指标标为不可估计。只有 1 个 N 类测试组时，N 类召回率只能取 0 或 1，不能据此宣称已精确估计正常识别能力。可做组级重采样区间，但须说明极少数类下的不稳定性。

## 8. 问题 3：无监督 CORAL 迁移诊断

### 8.1 选择理由与适用边界

CORAL 通过对齐两域特征二阶统计量进行无监督域适应，无需目标标签。[R5] 本题在低维机理特征上使用该思想，并加入文件等权统计、协方差收缩和可调适应强度，以降低少量目标记录下的估计不稳定性。这些是针对本题的工程设计，不能在实验前宣称具有性能创新。

共有标签不等于共有分布。即使边际均值、协方差接近，同类条件分布仍可能错位；类别比例差异也会改变总体协方差。对齐是否有效必须通过代理任务和稳定性检验判断。主方案不使用伪标签自训练，避免尚未验证的错误标签被迭代放大。

### 8.2 正则化、部分对齐的定义

以问题 2 冻结的共享特征结构为基础，在当前适应训练数据上重新拟合训练侧标准化。两域均应用该标准化。对域 $u\in\{\mathrm s,\mathrm t\}$，定义文件/独立组等权、组内窗口等权的经验统计：

$$
\boldsymbol\mu_u=\frac1{G_u}\sum_{i=1}^{G_u}\frac1{m_i}\sum_w\mathbf z_{iw},\qquad
\mathbf C_u=\frac1{G_u}\sum_{i=1}^{G_u}\frac1{m_i}\sum_w
(\mathbf z_{iw}-\boldsymbol\mu_u)(\mathbf z_{iw}-\boldsymbol\mu_u)^{\mathsf T}.
$$

$G_u$ 为域内独立组数；这是等组权二阶中心矩，不声称是独立窗口下的无偏协方差。此处不再对源域按类别强行均衡，以免把人为类别先验直接植入域统计；分类损失仍用上一节的类别权重。源、目标类别比例差异仍需在风险中报告。

$$
\widetilde{\mathbf C}_u=(1-\lambda)\mathbf C_u+
\lambda\frac{\operatorname{tr}(\mathbf C_u)}p\mathbf I+\varepsilon\mathbf I,\qquad
\mathbf A=\widetilde{\mathbf C}_{\mathrm t}^{1/2}\widetilde{\mathbf C}_{\mathrm s}^{-1/2}.
$$

所有向量采用列向量约定，$\mathbf I$ 为单位矩阵；使用对称特征分解计算主平方根及逆平方根。$\lambda\in[0,1]$ 控制收缩，$\varepsilon>0$ 防止奇异。源域向目标域的部分变换为：

$$
\mathbf A_\alpha=(1-\alpha)\mathbf I+\alpha\mathbf A,\qquad
\widetilde{\mathbf z}_{\mathrm s}=\mathbf A_\alpha(\mathbf z_{\mathrm s}-\boldsymbol\mu_{\mathrm s})
 +(1-\alpha)\boldsymbol\mu_{\mathrm s}+\alpha\boldsymbol\mu_{\mathrm t},\qquad
\widetilde{\mathbf z}_{\mathrm t}=\mathbf z_{\mathrm t}.
$$

$\alpha=0$ 严格退化为不适应的源模型输入，$\alpha=1$ 为完全均值/协方差适应。由于加入收缩和部分适应，实际经验协方差不必完全相等，不把二阶距离非零视为程序错误。变换源域训练特征后，用其原标签重新训练同一家族加权 SVM；目标保持上述标准化坐标，不重复应用源域变换。

候选 $\alpha\in\{0,0.25,0.5,0.75,1\}$、$\lambda\in\{0.1,0.3,0.5,0.9\}$；标准化空间初始 $\varepsilon=10^{-6}$，比较 $10^{-8}$ 与 $10^{-4}$ 检查数值敏感性。这些数值仅为初始网格。线性代数出现非有限数、明显负特征值或病态放大时，先检查实现，再增加收缩或降低维度。

### 8.3 不用目标标签的选择与检验

1. 源域最终测试组仅用于问题 2，保持不动。在其余源域开发组内部，预先登记嵌套的代理迁移划分：外层独立代理组只用于评价，内层代理组用于选择特征、频带、窗长、SVM 参数及 $\alpha,\lambda,\varepsilon$。按载荷或采集配置形成域差异，并优先隔离物理轴承；只有文件身份时明确证据只是跨记录/工况，不是未见轴承泛化。
2. 每个外层回合先隔离外层代理组及其标签，使用剩余组完成内层选择。每次内层划分重新拟合训练侧标准化、特征筛选、CORAL 和 SVM，代理侧只以无标签输入参与适应统计，标签只计算验证指标。按内层文件级 MacroF1 选择候选，同分优先较小适应强度与简单特征；不以真实目标熵或散点图紧凑程度选参。
3. 内层选定配置后锁定该回合全部选择，用外层训练部分重训，并允许外层代理组的无标签输入参与传导式统计，再预测外层代理组。外层标签此时才用于一次性评分；与 $\alpha=0$ 在同一外层组上比较，报告各类召回、组数及回合差异。外层分数评价的是预先登记的选择规程，不再用于回选参数、频带或方法；若看分后修改规程，原外层只能降级为开发证据，须另行独立验证。
4. 嵌套验证仅在每次训练均有四类、独立组数足够时执行，不能机械套用固定折数。正常组太少、同轴承身份无法排除或外层缺类时，应明确报告四类独立迁移收益不可估计。可继续用全部开发组的非嵌套代理任务选择工程参数，但此时其分数只是开发证据，不能称为独立验证的迁移提升。FE 目录缺 N，故单独 FE 代理只能支持三类压力测试。
5. 规程及评价冻结后，按同一预登记内层选择规程在全部开发组确定最终配置；再完成源域测试评估。最终 A–P 诊断可以用全部合格源域重训，原独立分数仍只属于原评估规程，不把重训拟合值写成新测试结果。
6. 对 A–P 的全部无标签数据拟合目标统计并预测 A–P，属于**传导式域适应**。这符合题面已知无标签集合的任务，但不等于证明能泛化到未来新列车文件。补充逐文件留出适应统计的检查，评估某个目标文件是否过度影响自身预测。

迁移前后展示相同尺度下的均值距离、协方差差异：

$$
D_\mu=\|\boldsymbol\mu_{\mathrm s}-\boldsymbol\mu_{\mathrm t}\|_2,\qquad
D_C=\frac{\|\mathbf C_{\mathrm s}-\mathbf C_{\mathrm t}\|_{\mathrm F}^2}{4p^2}.
$$

迁移后用变换后的源域重新计算；统计均保留相同文件权重。它们是对齐诊断量，既不是准确率，也不能单独证明故障知识迁移成功。可用固定 PCA 投影查看前后结构，但散点图不作为性能证据。

若内层开发任务普遍出现性能下降、正常类明显恶化，则在开发中降低 $\alpha$ 或保留 $\alpha=0$。独立外层出现负迁移时如实报告，不看完外层结果再调参并沿用原分数。目标预测随微小设置频繁翻转只触发不稳定标志与复核，不能因此宣称某个无标签配置更准确。没有独立收益证据时明确写“迁移收益未获独立验证”，不隐藏负结果，也不自动增加第三套模型。

### 8.4 目标文件输出契约

最终预测表必须包含 A–P 各一行，主标签取 N/OR/IR/B 四类之一；另外列出有效窗口数、得分间隔或可用概率、窗口一致率、重采样/参数扰动一致率、留一文件适应结果、是否建议复核、简短证据。低可信标志不替代题目要求的主标签；完全不可读取的文件则先修复输入，不伪造主标签。

窗口一致率是窗口预测与文件主标签一致的比例。稳定性检查以原始文件组重采样源域、改变可接受窗长/频带，以及对目标转速约值做敏感性分析；例如 9–11 Hz 只是围绕题面约 10 Hz 的人为压力测试区间，并非测得的转速置信区间。

复核阈值优先从开发集折外正确/错误样本的间隔和一致率分布中确定，报告覆盖率与错误检出表现；样本不足时只输出连续量和风险说明。没有目标真标签，不能报告真实目标准确率、F1 或证明错误率上界；16 个文件也不要求四类各 4 个。

## 9. 问题 4：解释实际迁移模型

本方案优先覆盖**迁移过程解释与事后解释**，题目允许任选一点或多点。物理特征易于理解，不意味着 RBF SVM 整体已成为完全透明模型，因此不把它包装成强事前可解释模型。

| 解释层面 | 分析内容 | 验证方式与限制 |
|---|---|---|
| 机理输入 | 冲击统计、包络、谐波和调制与局部缺陷的联系 | 源域已知标签案例逐一核对；目标几何未知时只作机制一致性说明 |
| 迁移过程 | $\mathbf A_\alpha$ 改变哪些特征方向；迁移前后均值/协方差及分类变化 | 同时展示代理任务收益和不适应基线；矩阵更对齐不代表诊断更正确 |
| 特征组贡献 | 在独立源域测试或代理验证上，将相关特征组整体置换，观察文件级 MacroF1 下降 | 以文件组为单位置换，不将单窗当独立样本；相关特征单独置换可能误导 |
| 单文件解释 | 遮蔽相关原始信号频带、重新提取特征并预测，查看主类得分变化 | 与等宽、不相关频带遮蔽比较；记录被移除能量，防止只解释幅值损失 |
| 不确定案例 | 各窗口分歧、留一文件适应翻转、源/迁移预测冲突 | 真实展示反例；稳定不等于正确，置信高不等于因果证据充分 |

原始频带遮蔽实验必须冻结训练标准化、CORAL 变换和分类器，避免解释时重新训练造成比较对象改变。目标特征保持第 8 节的目标坐标，源域案例则使用相应的源域变换。若解释某一“对齐后的组合特征”，必须说明其由哪些原始特征混合而来。

全局解释计划选取各类代表案例，局部解释至少覆盖一个稳定案例和一个存在分歧的案例；如无分歧案例，则展示稳定性最弱的样本并说明选择规则。不给 A–P 编造具体案例；需由实际结果选取。预测与机理不一致时，先检查转速、几何、传感器和预处理，保留矛盾证据，不按预测标签反向挑选支持图。

## 10. 假设分级与可证伪条件

### 10.1 关键假设：3 条

| 编号 | 假设与依据 | 如何检查 | 不成立时的后果 |
|---|---|---|---|
| K1 | 四类标签语义在两域一致，目标主状态可用 N/OR/IR/B 表达；依据题面附件 1 | 核对标签来源与设备说明；查看明显域外或复合故障迹象 | 闭集四分类不能代表真实状态；保留题目输出并显著标注超出适用范围 |
| K2 | 至少部分共享特征仍保留跨域类别信息；依据题目迁移动机，尚待实验支持 | 跨工况代理任务、可用时跨设备压力测试、机理一致性 | 单靠边际对齐无法保证识别；应报告证据不足，补元数据/标注后再设计 |
| K3 | 训练与测试分组能隔离主要相关记录；这是泛化评价的前提 | 哈希、采集编号、轴承编号、多通道与近重复检查 | 不能将分组不足的高分解释为独立泛化性能 |

### 10.2 可放宽假设：5 条

| 编号 | 假设与依据 | 放宽或复验方式 |
|---|---|---|
| R1 | 一个短窗内转速近似稳定；题面给出目标约 600 rpm | 比较 0.5/1/2 s 窗，检查谱峰展宽；有转速轨迹时用真正的角域重采样 |
| R2 | 源域频率计算初始取零接触角、近似纯滚动；与题面简化公式一致 | 有设计参数时用真实角度；按峰宽检查滑移；不强制谱峰精确命中 |
| R3 | 共有低于 5 kHz 频带保留部分诊断信息；来自最低采样率约束和候选设计 | 检查高采样率原始谱；比较带宽与源域子集，不把该假设当已证实 |
| R4 | 每个目标文件的主状态在 8 s 内基本一致；来自逐文件标定要求 | 分窗趋势和变点迹象检查；若异质则报告主状态及不一致区段 |
| R5 | 二阶适应是有用的低复杂度起点；依据 CORAL 方法而非本题实证 | 多代理任务、适应强度扫描、留一目标文件检查；失败时回退不适应基线 |

不假设目标类别均衡，不假设源目标转速相同，不假设同一传感器增益，不假设 16 个目标文件都是故障，也不假设源域几何适用于目标域。

## 11. 编程交接、候选图表与验收标准

### 11.1 实现顺序和依赖

推荐依赖：NumPy、SciPy（读取旧版 MAT、滤波、重采样、Hilbert、谱分析）、h5py（必要时读取 v7.3 MAT）、pandas（台账和结果表）、scikit-learn（分组划分、标准化、SVM、指标、PCA）以及 Matplotlib。本轮已用 NumPy 2.5.3、SciPy 1.18.1 读取全部 177 个 MAT，h5py 3.16.0 作为兼容备用；尚未验证分类、特征提取及绘图的完整运行链路。

1. 生成数据台账及组映射；确认标签、采样率、单位与正常组支持。
2. 冻结开发/测试划分，验证源域筛选规则，建立每折预处理与特征提取配置。
3. 在少量真实记录上走通“读取 → 特征 → 加权 SVM → 文件聚合 → 指标”，先证明接口及分组正确。
4. 用开发组完成参数选择，再一次性评估冻结源域测试组；保留失败案例与每类支持数。
5. 完成源域代理迁移实验、冻结 CORAL 参数规程，最后使用目标无标签数据适应并生成 A–P 文件级表。
6. 生成迁移前后对照、解释和稳定性结果，记录随机种子、数据哈希、运行版本与参数；再进入论文阶段。

建议后续表格字段如下，字段是程序接口文字，不是数学符号：

| 表 | 最小字段 |
|---|---|
| 文件台账 | 文件路径、哈希、域、原始编号、独立组、标签、故障端、观测通道、采样率、RPM、载荷、尺寸、点位、单位、入选状态及原因 |
| 窗口特征表 | 文件/组标识、窗口起止秒数、有效性、数据划分、真实标签或未知、固定顺序特征 |
| 源域评价表 | 文件/组标识、真实标签、预测标签、得分、模型配置、每类支持数、指标 |
| 目标结果表 | A–P 文件编号、预测标签、窗口数、四类得分或合规概率、间隔、窗口一致率、扰动一致率、复核标志、解释摘要 |

### 11.2 候选图表清单

下列只是后续绘图计划，本轮不制作无数据支撑的结果图。编程阶段按技能需覆盖每个子问题的原始、过程与结果三类图；四问至少对应 12 张不同内容的候选图，另有总体流程图。

| 子问题 | 原始/证据输入图 | 过程图 | 结果图 |
|---|---|---|---|
| 问题 1 | 四类代表时域波形与原始谱 | 滤波前后和包络提取对照 | 共享特征分布及类别/工况覆盖 |
| 问题 2 | 原始独立文件的类别与工况分组图 | 分组验证参数热力图 | 文件级混淆矩阵和各类召回率 |
| 问题 3 | 源目标原始转速、带宽与特征差异 | 固定投影下适应前后结构及二阶距离 | A–P 文件级预测与稳定性条形图 |
| 问题 4 | 待解释文件原波形、包络与机制证据 | 相关/对照频带遮蔽流程与响应 | 特征组贡献及稳定/分歧案例对照 |

总体流程图应覆盖输入、分组、特征、源域诊断、无标签适应、文件聚合、验证和解释，并明确目标标签不进入训练。文件级结果必须可从窗口表回溯。

### 11.3 必须满足的验证条件

| 检查项 | 通过条件 | 失败时动作 |
|---|---|---|
| 数据事实 | 实际文件/字段与台账一致；无法核验项明确记录 | 暂停对应计算，补充或修复输入 |
| 泄漏 | 相同原始采集、重复版本及可识别的同轴承组不跨评价边界 | 重建组和全部受影响实验 |
| 频率与单位 | 重采样、时间轴、转速、几何和 BSF/2BSF 口径明确 | 修正后重提特征；不得仅改图注 |
| 四类覆盖 | 每次训练含四类；评价侧逐类披露支持数 | 降低折数、调整可行划分或限制结论 |
| 输入一致 | 两域共享特征名称、顺序和含义完全一致 | 回到问题 1 修订特征合同 |
| 迁移收益 | 内层选参、独立外层评分，与同组基线比较并检查少数类损害 | 组数不足时仅报告开发证据和“收益未独立验证”；外层失败不回调后复用原分数 |
| 目标结论 | 16 个文件标签可回溯；未知真值不写成已测准确率 | 补齐输出或纠正评价表述 |
| 可解释性 | 解释的是实际冻结模型；包含对照扰动和不确定案例 | 重新生成解释，不以漂亮可视化替代验证 |

## 12. 最值得投入的工作与当前边界

优先投入应是可靠分组、保留稀少正常样本、统一信号物理口径、验证共享特征与控制负迁移。候选方法改进可概括为三点：文件/类别双重权重抑制长文件和多数类支配；几何无关共享输入兼容未知列车轴承；部分 CORAL 与代理任务、留一目标文件检查结合。三者都能转化为具体消融实验，但是否优于基线尚无结果。

本轮已获得建模分析稿，并已实查当前目录完整的源域与目标域原始数据。下一阶段可直接在现有数据上做信号质量深化、分组台账、特征提取与受控实验，不需要重新提供原始数据包。额外有价值的信息是目标传感器位置及单位、目标轴承型号/几何参数、各文件精确转速、源域原始采集/轴承编号。缺失精确目标几何不阻断共享特征基线，但限制故障频率定位解释；物理轴承身份缺失会限制独立泛化结论。

## 13. 文献依据与核验记录

以下文献支持具体方法或术语，不是本题模型性能的证据。未使用商业解题报告或网上 A–P 标签。

| 编号 | 可追溯来源 | 本报告用途 | 核验状态 |
|---|---|---|---|
| R0 | 用户提供的《2025 年中国研究生数学建模竞赛 E 题：高速列车轴承智能故障诊断问题》，9 页 | 任务、数据声明、题面 BSF 公式 | 本地全文与图像核验；输入哈希见第 2 节 |
| R1 | Randall, R. B.; Antoni, J. (2011). Rolling element bearing diagnostics—A tutorial. *Mechanical Systems and Signal Processing*, 25(2), 485–520. [DOI](https://doi.org/10.1016/j.ymssp.2010.07.017) | 包络与冲击诊断的理论背景 | 已核对出版方检索摘要及书目信息；全文访问受限，不引用未读细节 |
| R2 | Mais, J., SKF USA Inc. *Spectrum Analysis: The key features of analyzing spectra*，第 18 页“Vibration – spectral analysis”. [官方 PDF](https://cdn.skfmediahub.skf.com/api/public/0901d1968024acef/pdf_preview_medium/0901d1968024acef_pdf_preview_medium.pdf) | BPFO/BPFI/BSF/FTF 与球缺陷频率口径 | 已核验官方 PDF 封面与第 18 页文本；不援引其中历史严重度阈值 |
| R3 | Hu, L.; Zhang, L.; Gu, F.; Hu, N.; Ball, A. (2019). Extraction of the largest amplitude impact transients for diagnosing rolling element defects in bearings. *Mechanical Systems and Signal Processing*, 116, 796–815. [DOI](https://doi.org/10.1016/j.ymssp.2018.07.022) | 核对自旋与两次接触冲击的区别 | 已核对出版方索引的标题、作者、卷页和公式/引言预览；未采用其 ELAIT 算法 |
| R4 | Cortes, C.; Vapnik, V. (1995). Support-vector networks. *Machine Learning*, 20, 273–297. [出版页](https://link.springer.com/article/10.1007/BF00994018) | 核 SVM 的基本依据 | 已打开原始出版页核验 |
| R5 | Sun, B.; Feng, J.; Saenko, K. (2016). Return of Frustratingly Easy Domain Adaptation. *Proceedings of the AAAI Conference on Artificial Intelligence*, 30(1). [出版页](https://ojs.aaai.org/index.php/AAAI/article/view/10306)；[DOI](https://doi.org/10.1609/aaai.v30i1.10306) | CORAL 二阶域适应 | OpenAlex/AnySearch DOI 交叉匹配；已打开原始出版页和论文 PDF |
| R6 | Case Western Reserve University, Bearing Data Center. [Download a Data File](https://engineering.case.edu/bearingdatacenter/download-data-file) | 核对 DE/FE/BA/time/RPM 字段及采样资料 | 官方数据页检索核验；不据此推定题目实际文件元数据 |

双引擎检索记录：已运行技能自带 `hybrid_scholar.py`，查询 `bearing fault diagnosis domain adaptation` 和 `Return Frustratingly Easy Domain Adaptation`。最终两次联网运行均退出 0、两引擎均返回结果；R5 的 `sources=[openalex, anysearch]`、`cross_validated=true`。初次沙箱内运行因 DNS 不可达返回空集合，虽退出 0 仍判为失败，联网重试后才采用结果。其他来源通过原始出版方/官方站点核验，未声称均经过双引擎交叉匹配。

作者自检范围：已覆盖四项任务、共享输入定义、频率口径、模型数量、分组及嵌套防泄漏、单位/符号、假设等级、参数来源、失败条件及文献可追溯性。已完成 §2.3 所列原始数据基础审计；尚未执行近重复/削顶等深化检查、模型训练和真实诊断，不将方案分析或数值完整性检查当作诊断实验结果。
