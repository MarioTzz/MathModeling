# 问题三阶段验收记录

2026-09-21 按项目 `math-modeling-skill` 的单问门禁核验。验收范围仅限第三问的数学模型、代码、无标签结果、图件与复现文件；第四问尚未开始。

| 阶段 | 结论 | 独立核验要点 |
|---|---|---|
| M1 模型方案 | PASS | 14 个编号公式、符号与实际代码顺序一致；源域代理选择、固定测试与目标无真值的结论边界明确。初审提出的符号和代理措辞问题已在冻结模型稿中修订。 |
| P1 最短链路 | PASS | 真实数据 smoke 从文件中位数到对齐、树拟合与四类分数导出成功；训练侧缩放、目标标签隔离、对齐数值和异常边界均做独立检查。 |
| P2 全量终检 | PASS | 独立重跑得到 α=0.5、源测试 20/23、目标 16 文件与 8 个复核标记；34 个输入、15 项确定性结果哈希核对通过，摘要语义一致。 |

作者自检 25 项单元检查全部通过。`figures/q3/` 共 11 张正式图，每张有可编辑文本的 SVG、300 DPI PNG 和灰度预览；三类数据图各 3 张，另有 2 张流程图。`check_figure.py` 与 `figure_audit.py --questions q3 --strict` 均通过，独立审阅者实际查看了彩色和灰度图。初次 P2 检查发现过程图的两个特征块显示标签顺序与数据顺序不一致，现已显式重排并全量重跑；最终图义与 `alignment_diagnostics.csv` 一致。

模型 JSON/NPZ 导出后独立回读，在 16 个目标文件、23 个源测试文件及 60 个树阈值邻域探针上与内存分数一致，最大分数差为 0。目标 600 rpm 特征由原 MAT 重算，与问题一缓存特征的最大差为 $4.80\times10^{-14}$，逐文件标签不变。独立隔离重跑的 CSV、模型文件、报告和 22 张彩色/灰度 PNG 均与正式结果一致；HTML 面板因输出目录路径不同而仅比较语义和图件引用。

正式结果为目标 A—P 的**模型预测**，并非实测真值。目标域不存在可用于核验的标签，因此目标分类准确率未计算；固定源测试的 20/23 也不能替代目标准确率或问题二 90 维主模型的 22/23。目标文件 B、C、E、G、I、L、N、O 在至少一项对照中预测变化，已在结果表明确标记。复现命令、输入哈希、依赖版本和输出哈希见 `results/q3/复现清单.json`。
