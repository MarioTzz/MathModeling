# 问题二优化模型运行与代码说明

本次仍按“数据读取→数据处理→参数计算→模型求解→结果检验→可视化→结果导出”执行。正式入口为 [`问题二_求解.py`](问题二_求解.py)；优化数值函数在 [`utils/q2_optimized.py`](utils/q2_optimized.py)，14幅图由 [`utils/q2_optimized_plots.py`](utils/q2_optimized_plots.py) 生成。配置为 [`configs/q2.json`](configs/q2.json) 的 `optimized` 段。旧版RBF模型及报告保留为对照，其结果不再是当前主结论。

## 各步实现及公式对应

| 顺序 | 程序调用 | 数学说明 | 检查与输出 |
|---|---|---|---|
| 数据读取 | `load_record`、`extract_multiband_windows` | 式(2.31)—(2.36) | 核对原MAT SHA-256、DE通道、转速、采样率和116文件覆盖 |
| 数据处理 | `prepare_record`、`order_spectrum`、`_physics_features` | 式(2.37)—(2.42) | 三频带在原采样率解调后对齐20转窗；题面BSF与自旋半值分列 |
| 文件变量 | `aggregate_file_medians`、`feature_sets` | 式(2.43) | 逐文件中位数，五家族维数12/38/90/90/108，文件ID不进入预测列 |
| 参数计算 | `tune_extra_trees` | 式(2.44)—(2.45)、(2.51) | 类权重只由当前拟合折计算，5次重复分层文件折 |
| 模型求解 | `fit_extra_trees`、`prediction_table` | 式(2.46)—(2.47) | 先70训练选择、23验证选择特征家族，再93开发文件重调和拟合 |
| 结果检验 | `classification_metrics`、`full_cv_audit`、`scores_from_export` | 式(2.48)—(2.53) | 23文件测试、5次三折补充审计、零噪声、树节点重建、增益和噪声压力测试 |
| 可视化 | `make_optimized_figures` | 图表合同 | 3原始、4过程、5结果、2流程；SVG及300DPI PNG |
| 结果导出 | `save_model`、`write_json`、`save_csv` | 模型状态和结果表 | JSON/NPZ无pickle模型状态、CSV、报告、输入输出哈希清单 |

## 环境与命令

本机复现环境以 [`requirements-q2.txt`](requirements-q2.txt) 为依赖口径，项目临时依赖位于 `.analysis-work/`（该目录不提交）。本机已通过的命令为：

```bash
MPLCONFIGDIR=.analysis-work/matplotlib \
XDG_CACHE_HOME=.analysis-work/font-cache \
PYTHONPATH=.analysis-work/q2-deps:.analysis-work/q1-deps:.analysis-work/python-deps:. \
/Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 \
问题二_求解.py --overwrite
```

其他机器可用兼容的Python 3.12安装 `requirements-q2.txt`，然后执行 `python3 问题二_求解.py --overwrite`。输出位于 `模型适配数据集/q2_optimized/`、`results/q2_optimized/` 和 `figures/q2_optimized/`。已有目录必须含程序生成标记，且显式传入 `--overwrite`；原始数据、问题一特征表及既有RBF归档不修改。隔离复现可用 `--output-root .analysis-work/q2-independent`，新目录不用 `--overwrite`；图表面板链接自动按实际相对路径生成。

测试命令：

```bash
PYTHONPATH=.analysis-work/q2-deps:.analysis-work/q1-deps:.analysis-work/python-deps:. \
/Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 \
-m unittest discover -s tests -v
```

`source_classifier.json`记下类顺序、90个文件中位数变量名及500棵树参数；`source_classifier_arrays.npz`保存每棵树的分支和叶分布。`scores_from_export`从这两个文件重建四类分数，正式运行要求与sklearn的结果最大差不超过 $10^{-12}$。模型导出的分数不是校准概率。`model_interface.json`记录同一信号处理接口；后续目标域应用需核对目标轴承几何及可用转速，不能直接把源轴承尺寸视为目标已知量。

## 已运行结果和解释范围

最终选择 `multiband_spin_half`，即题面BSF和自旋半值分别计算，预测家族使用后者的90列，物理定义见[优化模型说明](问题二_优化模型与求解说明.md)式(2.33)—(2.34)。70文件内部调参、23文件验证的5个家族、93开发文件重调、23文件原测试与116文件补充交叉验证的逐项结果见 [`results/q2_optimized/问题二_结果报告.md`](results/q2_optimized/问题二_结果报告.md)。观察测试22/23（95.65%）；唯一错误为48 kHz外圈居中故障判成滚动体故障。测试集曾被查看，不能称为独立盲测保证。
