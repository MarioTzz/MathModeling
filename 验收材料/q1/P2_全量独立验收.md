# 问题一 P2 独立终验回执

## 1. 范围（阶段）

验收阶段为问题一 P2：完整运行、数值结果、两路数据接口、敏感性、科研图表和复现证据。沿用已通过 M1/P1 的模型合同，审查范围仅为问题一，不包括问题二分类器、问题三运输求解/迁移预测或问题四。

本次按根目录 AGENTS.md、math-modeling-skill 的 Subagent 调度、编程手质检清单及科研可视化规范执行。审查者未修改权威模型、代码、配置、输入和正式产物，仅在 `.analysis-work/q1-review/` 保存独立运行及审查记录。模型说明新增带宽限制和固定数值核验容差属于实现解释，编号 1.01—1.36 的模型合同不变。

## 2. 输入快照（验收对象）

| 对象 | SHA-256 |
|---|---|
| 问题一_求解.py | f26da29d93a84cadddbe7d43a1f3285e14b43811add08a367de18d273251f47d |
| utils/q1_model.py | 22c190f6f962ac248f3104a6e1d7bafba402d8c38b2099b72a9af802345758ea |
| utils/q1_plots.py | 310de2a7f004a63addb8ef8e958411517a80e4371b6cd42983005930b12e51e1 |
| configs/q1.json | 4e3f02da34b1c3974ed081f3d74564e60482ad84e27897f4419cf81ec3c1fa2d |
| tests/test_q1_model.py | de2965db70908f8b738191522b9af8d85a23564c83be4b450dd19d008b051b1e |
| 问题一_数学模型与求解说明.md | 7118ff8ba27e89682eb3efcd731da2cf902f03a7baff343e9ec31d08f1594997 |
| 术语表格.md | 04e4aaa7686e3c3dcad9abb91ac284d6aad7f5358f14c19571c71c6e886708a0 |
| 问题一_运行与代码说明.md | 1a95da1593c52476f70e8f6665f25941d9c764648e45e82389615c4d13265f02 |
| 模型适配数据集/q1/README.md | bc8f027e925674a1bdb43a92227fad27adcbc2def1945319677f112df14c2eed |

正式数据、结果和图分别为 `模型适配数据集/q1/`、`results/q1/`、`figures/q1/`。独立运行位于 `p2-run/`。132 个原始 MAT 以及代码、配置和相关文档共 144 项输入快照、70 项生成输出快照均按正式复现清单重新验算；人工交付 README 不计入生成输出。

## 3. 状态（通过状态）

**PASS。无未解决的 P0/P1/P2 缺陷。** 问题一的模型、数值实现、实际结果、图表和两路数据准备达到本阶段交付要求。通过范围不包含后续诊断精度、目标标签、迁移收益或最优传输的有效性。

## 4. 证据（检查清单）

### 4.1 独立运行与已知答案

在项目根目录执行全量程序，首次独立运行完成；两项文档问题修正冻结后，以相同隔离目录再次完整运行：

```text
PYTHONPATH=.analysis-work/q1-deps:.analysis-work/python-deps /Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 问题一_求解.py --output-root .analysis-work/q1-review/p2-run --overwrite
```

退出码 0，最终运行约 13.9 秒，日志为 `p2-reproduction.log`。5 项已知答案测试独立重跑全部通过，日志为 `p2-tests.log`；覆盖已知调制阶次和跨采样率、完整滤波增益齐次性、退化输入拒绝、文件等权尺度和目标隔离、非训练信息对候选选择的隔离。

### 4.2 公式、训练隔离与接口

审查脚本不导入被审数值模块，分别保存为 `check_p2_exports.py`、`check_p2_signal_and_evidence.py`、`check_p2_selection.py`。三份对应 JSON 检查记录均为 PASS。

| 检查 | 证据与结果 |
|---|---|
| 输入与划分 | 116 源文件、16 目标文件；源划分 70/23/23。标签、RPM、采样率、输入哈希继承既有清单 |
| 正常类限制 | 训练 N 仅 2 文件，二折各 1；不将重采样次数当作新增独立样本 |
| 窗口准入 | 10/20 转均满足全文件、转速扰动和裁边场景的完整窗口要求；40 转失败有记录。没有补零或复制短记录 |
| 全窗口特征 | 原始 MAT 独立重算主线 996、一秒基线 707 窗口的全部 12 特征。FIR 用 sinc/Kaiser 公式重建，包络、降采样、阶次网格及谱统计复算；最大尺度化绝对误差 4.965×10⁻¹⁵ |
| 窗口尺度 | 主线及基线均仅用 70 源训练文件，文件等权。与独立重算的最大绝对误差分别 5.401×10⁻¹³、5.969×10⁻¹³ |
| 文件出口 | 132 行、16 维；按同一组 8 特征计算中位数/IQR，再按源训练文件拟合尺度。描述量误差不超过 3.713×10⁻¹³，标准化误差不超过 2.971×10⁻¹² |
| 候选选择 | 从 6 候选各 70 训练文件中位数独立重建类别平衡 Fisher、Spearman 贪心去冗余及二折评分；12 分数最大误差 7.772×10⁻¹⁶，获选 1500—3000 Hz / 20 转 |
| 稳定筛选 | 独立重现 100 次分层文件重采样，12 特征入选频率逐项相同；Fisher 分数最大误差 3.553×10⁻¹⁵；0.5/0.6/0.7 三阈值集合完全一致 |
| 未知标签 | 目标域标签始终空字符串、编码 −1；窗口和文件接口均保留明确元数据，特征白名单未混入目标编号或类别编码 |
| 后续拟合说明 | 当前全训练集拟合的标准化表不作为无泄漏内折验证输入；若重新选择频带/转数，README 和运行说明明确须从原始 MAT 重算六候选 |

### 4.3 敏感性和研究结论

冻结频带、选中特征与训练尺度，独立重算全部 160 行转速敏感性及 792 行裁边敏感性。统一尺度和各自尺度的转速漂移最大绝对误差均为 4.885×10⁻¹⁵，裁边漂移为 1.017×10⁻¹⁵。15 个增益检查均满足声明的数值容差；数值检查没有被表述成真实域迁移有效性的证据。

对每路 16 个目标文件的 4 个非零转速扰动，共 64 个文件—扰动观察，统一主线尺度漂移中位数为：等转数 **0.2316421144**，固定一秒 **0.1137981915**。正式报告如实说明本次等转数漂移较大，没有宣称其更稳健。候选分数明确是训练内 Fisher 表征分数，不是准确率。正常样本限制、原始采样带宽/增益影响、代表谱峰不能等同分类规则等条件均写在相关结果附近。

源域机理图使用固定首个训练文件；OR/IR 代表最大峰与题面基阶偏差约 0.0152 阶，B 代表偏差约 0.4866 阶。图表与报告保留该不一致，未移动峰值或据此更改标签。目标域未套用未知轴承几何。题面滚动体冲击频率与自旋频率的区别沿用 M1 原题核验。

### 4.4 图表、视觉与复现

实际逐张查看 12 张彩图：原始图 3、过程图 3、结果图 4、流程图 2；另实际查看转速敏感性、类别划分、Q1 流程、特征分布的 4 张灰度预览。中文、坐标、样本单位、图例、线型、布局均可读，未见裁切或遮挡。结果图以文件为统计单位；正常类数量标注为 2；重采样比例未解释为置信概率，转速图阴影是文件 IQR。总流程中 Q2/Q3/Q4 明确为待实施，Q1 双接口已完成。

对 `figures/q1/*.png` 和 `figures/q1/*.svg` 分别执行格式检查，严格模式退出码 0、全部 PASS；图表分类审计退出码 0、`ok=true`、`issues=[]`。记录为 `p2-figure-check.log` 与 `p2-figure-audit.json`。PNG 为约 300 DPI，SVG 为可编辑文字/路径且未嵌入栅格图。

| 复现检查 | 结果 |
|---|---|
| 正式 144 项输入哈希 | 全部匹配 |
| 正式 70 项生成输出哈希 | 全部匹配 |
| CSV、JSON 与生成报告 | 正式运行与独立运行一致；复现清单中的时间/路径及图清单中的相对路径按用途分别核验 |
| PNG | 12 彩图 + 12 灰度预览，24 张逐字节相同 |
| SVG | 12 张文字、路径几何和样式语义一致；生成时间与随机资源 ID 不作为差异 |
| 图清单链接 | 正式目录和隔离目录的 12 个图链接均实际解析存在，规范化后的说明正文一致 |

## 5. 发现（缺陷等级）

**当前 P0：无；P1：无；P2：无。** 本轮发现并由作者修正、审查者复验关闭的两项 P2 问题：

1. 数据说明曾未充分区分“当前频带/转数下重新筛选特征”和“内折重新选择频带/转数”。当前已明确后者必须重读原始 MAT、重算六候选。
2. 图清单曾使用固定相对路径，使隔离复现目录链接失效。当前按结果目录到图目录计算路径，两种运行目录均已核验。

独立复算器曾使用浮点 linspace 表达阶次网格，在谐波整数端点处产生审查差异；改为合同所定义的十进制网格整数比值后，与实现全部一致。SVG 随机资源 ID 已按语义比较；格式检查曾误传目录参数，改为逐图文件后全部 PASS。这些是审查方法的修正，不是权威产物缺陷。

## 6. 返工（下一步）

无须返工。问题一可按用户已授权的交付范围提交；本审查者未执行 Git 操作。交付后等待用户确认，再开展 Q2/Q3/Q4。本回执不授权或预先验收后续阶段。

若模型、数值代码、输入、配置或正式结果再发生实质修改，需按影响范围复验；新增不改变已审内容的验收摘要不影响本回执。
