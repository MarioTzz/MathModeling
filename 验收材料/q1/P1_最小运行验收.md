# 问题一 P1 独立验收回执

## 1. 范围（阶段）

阶段为问题一 P1 最小可运行结果。按已通过的 M1 模型合同，审查数值核心、5 项已知答案测试及真实小样本的输入—筛选—窗口出口—文件出口链。仅执行 `--smoke`；未执行全量、完整敏感性、正式图表、Q2 分类、Q3 迁移/运输或 Q4。`utils/q1_plots.py` 尚在编写，不在本次验收范围。

本次补充读取 `math-modeling-skill/references/roles/编程手/SKILL.md`，并沿用此前已读的项目规则和阶段门禁说明。审查者未修改被审模型、配置、核心代码或测试，只在本审查目录保存运行和检查产物。

## 2. 输入快照（验收对象）

| 对象 | SHA-256 |
|---|---|
| `问题一_求解.py` | `7f1bd6556fb0bc060bd3e8690d82215c1368165605288c2f81519d47e1eb94ba` |
| `utils/q1_model.py` | `22c190f6f962ac248f3104a6e1d7bafba402d8c38b2099b72a9af802345758ea` |
| `tests/test_q1_model.py` | `de2965db70908f8b738191522b9af8d85a23564c83be4b450dd19d008b051b1e` |
| `configs/q1.json` | `4e3f02da34b1c3974ed081f3d74564e60482ad84e27897f4419cf81ec3c1fa2d` |
| `问题一_数学模型与求解说明.md` | `5a792248e923a8172f2f671fbe3bfabb529a3711bb389f76bb54ed8b7a207e85` |

11 个实际输入文件及相关依赖的快照见 `p1-run/results/复现清单.json`；本次独立导出复核结果与核心快照见 `p1-independent-checks.json`。

## 3. 状态（通过状态）

**PASS**。无未解决的 P0/P1 缺陷，允许开展问题一全量计算、完整敏感性与正式绘图。本通过仅证明最小数值链与模型合同一致，不代表问题一全量交付完成。

## 4. 证据（检查清单）

下列命令均从 `/Users/mariozz/MathModeling` 执行：

```text
PYTHONPATH=.analysis-work/q1-deps:.analysis-work/python-deps MPLCONFIGDIR=.analysis-work/matplotlib MPLBACKEND=Agg /Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 问题一_求解.py --smoke --output-root .analysis-work/q1-review/p1-run
```

退出码 0；真实输入为 9 个源训练文件与 2 个无标签目标文件。输出 95 个主线窗口、71 个一秒基线窗口，8 个选中特征，11 行、16 维文件描述量。此次小样本选择为 3000—5000 Hz / 20 转；正式运行会重新选择，不沿用小样本结论。

```text
PYTHONPATH=.analysis-work/q1-deps:.analysis-work/python-deps MPLCONFIGDIR=.analysis-work/matplotlib MPLBACKEND=Agg /Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest discover -s tests -p test_q1_model.py -v
```

退出码 0；5 项全部通过，覆盖已知 3 阶调制峰及三采样率、完整滤波链增益齐次性、空窗与常数包络拒绝、文件等权尺度和目标隔离、非训练标签/数值改变不影响候选选择。

```text
PYTHONPATH=.analysis-work/q1-deps:.analysis-work/python-deps /Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 .analysis-work/q1-review/check_p1_exports.py
```

最终退出码 0。该独立检查不导入被审数值模块，直接读取原始 MAT 与输出表重算：

| 检查 | 实际结果 |
|---|---|
| 原始哈希、标签、划分、RPM 和名义采样率 | 与预处理清单一致 |
| 窗口起止、转数、完整窗口数、唯一键及文件覆盖 | 主线和基线均通过 |
| 全部窗口前六个原波形特征 | 最大绝对误差 4.9738×10⁻¹⁴ |
| 主线窗口标准化 | 最大绝对误差 2.4869×10⁻¹⁴ |
| 基线窗口标准化 | 最大绝对误差 3.7303×10⁻¹⁴ |
| 文件中位数与线性分位数 IQR | 最大绝对误差 7.2928×10⁻¹⁵ |
| 文件描述量二次标准化 | 最大绝对误差 8.3844×10⁻¹³ |
| 训练范围 | 三个尺度的 fit_file_ids 均仅含 9 个源训练文件 |
| 内折及候选统计 | 两折均含 N/OR/IR/B，正常各 1 文件；6 个候选的文件中位数仅含源训练文件 |
| 未知目标标签 | 两个窗口出口及两个文件出口均为空字符串 / −1 |
| 特征白名单与范围 | 标准化窗表仅含元数据及选中数值列；归一化谱指标在 [0,1] 内 |
| 输出哈希 | 与本次运行清单全部一致 |
| 作者/独立运行一致性 | 除时间、路径相关复现清单外，全部生成载荷逐字节一致 |

另逐段对照式 1.01—1.35 与核心代码：原采样率对称 FIR、反射延拓后 Hilbert、包络抗混叠降采样、Hann 阶次谱、谐波邻域并集、两折训练内选择、分层文件重采样、窗口及文件两级尺度均符合合同。小样本按设计跳过完整敏感性和正式绘图。

## 5. 发现（缺陷等级）

**P0：无。P1：无。**

P2 解释提醒：本次小样本某一留出折每类仅 1 文件，类内方差全部为 0，六个候选在该折均得到约 27.6310 的对数 Fisher 分数；这是当前定义在小样本上的退化行为，不能把此次获选频带/转数作为全量最优参数。当前完整运行会对全部 70 个训练文件重新计算，因此不阻塞 P1。

独立检查脚本首次以 `Series.equals` 比较 CSV 中的 RPM 时因整数/浮点存储类型不同而失败；随后逐值核对确认所有 RPM 数值一致，改为值比较后完成全部检查。该失败属于审查脚本的比较口径，不是被审代码缺陷。

## 6. 返工（下一步）

无阻塞返工。按当前合同进行全量求解、实际敏感性及正式出图，完成后执行 P2。P2 应对实际训练/验证/测试/目标覆盖、全部特征与接口、机理图、三类候选图和流程图、最终复现清单作独立核验。用户确认前不进入 Q2/Q3/Q4。

数值核心、输入、配置或模型发生实质变化时，需复验受影响的 P1 内容。本回执不提前验收仍在编写的图表模块。
