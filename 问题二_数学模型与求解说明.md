# 问题二：文件—类别双重均衡 RBF-SVM 数学模型与求解说明

## 1. 本问要解决的问题

问题二要求利用问题一得到的源域故障特征，建立能够区分正常状态 `N`、外圈故障 `OR`、内圈故障 `IR` 和滚动体故障 `B` 的诊断模型，并评价其在未参与训练的源域文件上的表现。预测对象是**完整原始文件**，窗口只是同一文件内的相关观测，不能把窗口数当成独立样本量。

本问采用“基础模型＋算法改进”的路线：基础模型为一对其余的径向基函数支持向量机（RBF-SVM），改进点是把类别不平衡和文件窗口数不平衡同时写入损失权重，并把标准化、特征筛选、参数选择和评价全部限制在相应训练文件内。该模型直接解决源域四分类，不使用目标域标签，也不在本问输出 A—P 的诊断结果。

问题一发现，20 转窗口在本次转速扰动实验中的漂移没有优于 1 秒窗口。因此本问不预先宣称等转数表示更好，而是在相同模型、相同文件划分和相同选择预算下比较两种表示。表示路线只由验证集选择；测试集在路线和参数冻结后使用一次。

## 2. 数据范围与符号

| 符号 | 含义 | 本题取值或单位 |
|---|---|---|
| $\mathcal C$ | 类别集合 | $\{N,OR,IR,B\}$ |
| $K$ | 类别数 | $4$ |
| $\mathcal F$ | 当前拟合文件集合 | 随内折、训练或开发阶段改变 |
| $N_c$ | $\mathcal F$ 中类别 $c$ 的文件数 | 个 |
| $i$ | 文件索引 | 同一原文件始终属于同一折 |
| $j$ | 文件 $i$ 的窗口索引 | $0,\ldots,m_i-1$ |
| $m_i$ | 文件 $i$ 的完整窗口数 | 个 |
| $y_i$ | 文件 $i$ 的真实类别 | $\mathcal C$ 中一类 |
| $x_{ijk}$ | 第 $i$ 文件第 $j$ 窗的第 $k$ 个原始候选特征 | 问题一的 12 个候选量之一 |
| $z_{ijk}$ | 由当前拟合文件估计尺度后的特征 | 无量纲 |
| $a_{ij}$ | SVM 损失中的窗口权重 | 无量纲，和为 1 |
| $h_c$ | 类别 $c$ 的 RKHS 函数部分 | 仅该部分被正则化 |
| $f_c(z)$ | 类别 $c$ 的一对其余决策分数 | 未校准间隔分数 |
| $s_{ic}$ | 文件 $i$ 对类别 $c$ 的算术平均分数 | 未校准间隔分数 |
| $C$ | 软间隔损失强度 | 网格搜索参数 |
| $\gamma$ | RBF 核宽度参数 | 网格搜索参数 |
| $\widehat y_i$ | 文件级预测类别 | $\mathcal C$ 中一类 |

源域共 116 个核心文件，沿用问题一固定的训练/验证/测试划分：训练 70 个、验证 23 个、测试 23 个。训练集类别文件数为 `N/OR/IR/B = 2/35/15/18`；正常类独立文件极少，所有评价均按文件统计，并逐文件报告正常类结果。20 转表示含 996 个源域和目标域窗口，1 秒表示含 707 个；本问只用其中源域窗口训练和评价。

## 3. 模型假设

1. **文件标签一致。** 同一源域文件的所有窗口继承该文件的工作状态标签。题目数据没有给出窗内状态切换，因此本问不另造复合故障或状态转移标签。
2. **文件是独立评价单位。** 同一文件内窗口可以相关；不同文件才作为训练分组、验证分组和评价样本。
3. **源域标签可用于监督学习。** 训练、验证和测试文件的标签仅在各自被允许的阶段使用；目标域标签未知且不进入本问。
4. **问题一候选特征可用于源域判别。** 每次换拟合文件集合时，稳定筛选和尺度参数均重新估计，以检验而非假定这些特征的判别作用。
5. **原生 SVM 分数不是概率。** 本问报告类别间隔、分数差和窗口投票比例，不把它们解释为故障发生概率。
6. **正增益不改变形状特征。** 幅值增益压力试验只同比缩放 `ac_rms`、`mean_abs`、`peak_to_peak`；该齐次性已在问题一的完整滤波链中验证。噪声压力试验则回到原始信号重新提取特征。

## 4. 数据适配与训练内特征处理

### 4.1 文件级稳定特征筛选

当前拟合集合中，先以每文件特征中位数消除窗口数差异：

$$
\widetilde x_{ik}=\operatorname{median}_{0\le j<m_i}x_{ijk}.
\tag{2.01}
$$

Fisher 分数先在当前拟合文件或当前重采样文件中做一次临时无量纲化。记全部文件中位数的均值和总体标准差为 $\mu_k^{F}$、$\sigma_k^{F}$，仅当 $\sigma_k^{F}>10^{-12}$ 时定义

$$
u_{ik}=\frac{\widetilde x_{ik}-\mu_k^{F}}{\sigma_k^{F}},\qquad
\bar u_{ck}=\frac{1}{N_c}\sum_{i\in\mathcal F:y_i=c}u_{ik},\qquad
v_{ck}=\frac{1}{N_c}\sum_{i\in\mathcal F:y_i=c}(u_{ik}-\bar u_{ck})^2.
\tag{2.02}
$$

类别等权 Fisher 分数为

$$
J_k=\frac{K^{-1}\sum_{c\in\mathcal C}(\bar u_{ck}-K^{-1}\sum_q\bar u_{qk})^2}
{K^{-1}\sum_{c\in\mathcal C}v_{ck}+\varepsilon}.
\tag{2.03}
$$

式(2.03)中的 $\varepsilon=10^{-12}$ 是无量纲数值稳定项；临时标准差不达阈值的常数列直接排除，不向有量纲方差添加该常数。在当前拟合文件内进行 $B=100$ 次按类别、按文件有放回重采样，每次都重新计算式(2.02)。若特征 $k$ 在第 $b$ 次重采样中经 Fisher 排名和 Spearman 相关阈值 $0.9$ 后被保留，记 $I_k^{(b)}=1$，否则为 0。稳定入选比例为

$$
\pi_k=\frac1B\sum_{b=1}^{B}I_k^{(b)}.
\tag{2.04}
$$

最终排名先按 $\pi_k$ 降序，再以当前完整拟合集合上的 $J_k$ 降序破平局；保留 $\pi_k\ge0.6$ 的特征后，按该顺序贪心剔除与已选特征绝对 Spearman 相关系数超过 0.9 的列，最多保留 8 维。所有阈值沿用问题一，不根据问题二测试结果修改。

### 4.2 文件等权标准化

对当前拟合文件集合定义尺度权重

$$
w_{ij}=\frac{1}{|\mathcal F|m_i},\qquad
\sum_{i\in\mathcal F}\sum_{j=0}^{m_i-1}w_{ij}=1.
\tag{2.05}
$$

由此计算每个入选特征的中心和尺度：

$$
\mu_k=\sum_{i,j}w_{ij}x_{ijk},\qquad
\sigma_k=\sqrt{\sum_{i,j}w_{ij}(x_{ijk}-\mu_k)^2}.
\tag{2.06}
$$

标准化坐标为

$$
z_{ijk}=\frac{x_{ijk}-\mu_k}{\sigma_k}.
\tag{2.07}
$$

若 $\sigma_k\le10^{-12}$，程序停止并返回特征退化错误。验证、测试和压力试验只应用当前模型已冻结的 $\mu_k,\sigma_k$。

## 5. 文件—类别双重均衡 RBF-SVM

### 5.1 三种损失权重

为分离改进点，设当前训练窗口总数为 $M$、类别 $c$ 的窗口总数为 $M_c$。三种权重定义为

$$
a_{ij}^{\mathrm U}=\frac1M,
\tag{2.08}
$$

$$
a_{ij}^{\mathrm C}=\frac{1}{K M_{y_i}},
\tag{2.09}
$$

$$
a_{ij}^{\mathrm{FC}}=\frac{1}{K N_{y_i}m_i}.
\tag{2.10}
$$

三者总和均为 1，使同一 $C$ 网格具有可比的总体损失尺度。式(2.09)只保证类别等权，式(2.10)进一步满足

$$
\sum_{i:y_i=c}\sum_j a_{ij}^{\mathrm{FC}}=\frac1K,
\qquad
\sum_j a_{ij}^{\mathrm{FC}}=\frac{1}{K N_c}.
\tag{2.11}
$$

因此每类总权重相同，同类每个文件的总权重也相同。求解器关闭自动类别加权，避免把式(2.10)重复计算。

### 5.2 核函数与一对其余分类器

RBF 核为

$$
\mathcal K_\gamma(z,z')=\exp\{-\gamma\lVert z-z'\rVert_2^2\}.
\tag{2.12}
$$

对于类别 $c$，二元标签定义为

$$
t_{ic}=\begin{cases}+1,&y_i=c,\\-1,&y_i\ne c.\end{cases}
\tag{2.13}
$$

主模型对每个类别分别求解加权软间隔问题，其中 $h_c\in\mathcal H_\gamma$ 是核函数部分，$b_c$ 是不正则化的偏置：

$$
\min_{h_c\in\mathcal H_\gamma,b_c,\xi_{ijc}}
\frac12\lVert h_c\rVert_{\mathcal H_\gamma}^2
+C\sum_{i,j}a_{ij}^{\mathrm{FC}}\xi_{ijc},
\tag{2.14}
$$

$$
\text{s.t.}\quad
t_{ic}\{h_c(z_{ij})+b_c\}\ge1-\xi_{ijc},\qquad \xi_{ijc}\ge0.
\tag{2.15}
$$

对应决策函数写为

$$
f_c(z)=h_c(z)+b_c
=\sum_{r\in\mathcal S_c}\alpha_{rc}t_{rc}\mathcal K_\gamma(z_r,z)+b_c,
\tag{2.16}
$$

其中 $\mathcal S_c$ 是类别 $c$ 的支持向量集合。程序显式训练四个二分类器，与式(2.13)—(2.16)一一对应。

### 5.3 文件级决策

主汇总规则对窗口分数作算术平均：

$$
s_{ic}=\frac1{m_i}\sum_{j=0}^{m_i-1}f_c(z_{ij}),
\tag{2.17}
$$

$$
\widehat y_i=\arg\max_{c\in\mathcal C}s_{ic}.
\tag{2.18}
$$

将最大和次大文件分数之差定义为诊断间隔：

$$
\Delta_i=s_{i,(1)}-s_{i,(2)},
\tag{2.19}
$$

其中 $s_{i,(1)}\ge s_{i,(2)}$ 是排序后的前两项。窗口投票比例只作为一致性描述：

$$
q_i=\frac1{m_i}\sum_j\mathbf1\!\left(\arg\max_c f_c(z_{ij})=\widehat y_i\right).
\tag{2.20}
$$

鲁棒性对照另计算各类窗口分数中位数及窗口多数投票，但二者不参与模型选择。

## 6. 参数选择与测试封存

### 6.1 文件分组二折

在当前开发文件集合内，各类别文件经固定种子 42 排序后交替进入两个内折，记折映射为 $g(i)\in\{0,1\}$。第 $h$ 折的拟合与留出文件分别为

$$
\mathcal F_h^{\mathrm{fit}}=\{i:g(i)\ne h\},\qquad
\mathcal F_h^{\mathrm{hold}}=\{i:g(i)=h\}.
\tag{2.21}
$$

问题一的 `1500—3000 Hz` 频带由 70 个训练文件预先选定，因此 Q2 内折不重复选择频带；两张未标准化候选表是在这一固定频带下形成的 20 转和 1 秒表示。Q2 的每一折独立执行式(2.01)—(2.07)，防止**本问的特征筛选和尺度**泄漏。内折结果应解释为“固定 Q1 候选表征条件下”的参数选择；完整泛化能力由未参加 Q1 频带选择的外层验证/测试文件评价，不能把内折分数称为整条 Q1—Q2 流程的无泄漏泛化估计。RBF 参数网格固定为

$$
C\in\{1,4,16,64,256,1024\},\qquad
\gamma\in\{0.03125,0.125,0.5,2\}.
\tag{2.22}
$$

线性核对照只搜索同一 $C$ 网格。四个消融方案使用相同文件折和相同特征筛选规则；三个 RBF 方案各评估 24 组 $C,\gamma$，线性方案评估其适用的 6 个 $C$，不人为扩大某一同类方案的搜索范围。较大的 $C$ 范围与式(2.08)—(2.10)总权重归一到 1 的目标尺度相配套。这些网格和噪声级是运行前固定的有限实验预算，不声称是由本数据证明的全局最优取值。

### 6.2 文件级选择指标

令 $TP_c,FP_c,FN_c$ 均按文件计算。类别精确率、召回率和 F1 为

$$
P_c=\frac{TP_c}{TP_c+FP_c},\qquad
R_c=\frac{TP_c}{TP_c+FN_c},
\tag{2.23}
$$

$$
F_{1,c}=\frac{2P_cR_c}{P_c+R_c},
\tag{2.24}
$$

零分母按 0 处理。主要指标与次要指标分别为

$$
F_1^{\mathrm{macro}}=\frac1K\sum_cF_{1,c},\qquad
BA=\frac1K\sum_cR_c.
\tag{2.25}
$$

内折参数先最大化两折平均 $F_1^{\mathrm{macro}}$，再最大化 $BA$；仍平局时依次选择较小 $C$ 和较小 $\gamma$。这项规则限制边界复杂度，不查看测试结果。分折实现固定为：每类文件标识先排序，再由 NumPy `default_rng(42)` 置换，按置换位置奇偶交替分折。类别分数若出现机器精度内完全相等，按固定类别顺序 `N, OR, IR, B` 取先者。

### 6.3 两阶段冻结流程

第一阶段仅用 70 个训练文件完成筛选、尺度拟合、调参和模型拟合，再在 23 个验证文件上比较 20 转与 1 秒两条表示路线；验证指标来自只拟合这 70 个文件的模型。表示选择只比较主模型式(2.10)的 RBF-SVM，按验证集 $F_1^{\mathrm{macro}}$、$BA$、正常类召回依次决胜；若完全相同，选窗口更少的 1 秒路线。

第二阶段冻结表示路线，把训练集和验证集合并为 93 个开发文件。四个消融方案分别用相同预算重新进行文件分组二折并选定参数，随后用全部开发文件重做特征筛选、尺度拟合和模型训练。测试指标均来自这些 93 文件模型；23 个测试文件只在上述步骤完成后评价一次。该流程可表示为

$$
r^*=\arg\max_{r\in\{20\text{转},1\text{秒}\}}
\big(F_{1,\mathrm{val}}^{\mathrm{macro}},BA_{\mathrm{val}},R_{N,\mathrm{val}}\big),
\tag{2.26}
$$

$$
(C^*,\gamma^*)=\arg\max_{C,\gamma}
\big(\overline F_{1,\mathrm{inner}}^{\mathrm{macro}},\overline{BA}_{\mathrm{inner}},-C,-\gamma\big).
\tag{2.27}
$$

测试结果用于估计冻结模型的源域泛化能力，不回流修改式(2.22)、特征阈值或表示路线。

## 7. 消融、稳健性与误差边界

在选定表示上比较四个冻结方案：RBF＋窗口均权、RBF＋类别均衡、RBF＋文件—类别双重均衡，以及线性核＋文件—类别双重均衡。三种 RBF 方案只改变式(2.08)—(2.10)，线性核对照将式(2.12)替换为

$$
\mathcal K_{\mathrm{lin}}(z,z')=z^\mathsf Tz'.
\tag{2.28}
$$

对正增益 $g$，幅值特征集合 $\mathcal A=\{\texttt{ac\_rms},\texttt{mean\_abs},\texttt{peak\_to\_peak}\}$ 按

$$
x'_{ijk}=\begin{cases}g x_{ijk},&k\in\mathcal A,\\x_{ijk},&k\notin\mathcal A,
\end{cases}\qquad g\in\{0.5,1,2\}
\tag{2.29}
$$

变换，再应用冻结尺度。对白噪声压力试验，在源测试文件原始去均值信号 $u_i[n]$ 上构造

$$
\epsilon_i[n]\overset{\mathrm{iid}}{\sim}\mathcal N(0,1),\qquad
\widetilde\epsilon_i[n]=\frac{\epsilon_i[n]}{\operatorname{RMS}(\epsilon_i)},\qquad
u_i^{(\eta)}[n]=u_i[n]+\eta\,\operatorname{RMS}(u_i)\widetilde\epsilon_i[n],
\quad \eta\in\{0,0.01,0.05,0.1\}.
\tag{2.30}
$$

其中 $\widetilde\epsilon_i$ 是本次有限长度高斯序列经其实际 RMS 精确归一化后的方向，因此加性噪声的样本 RMS 恰为 $\eta\operatorname{RMS}(u_i)$。随后从问题一滤波、包络、窗口和特征步骤完整重算。随机种子由 `SHA-256("42|file_id|noise_level")` 的前 8 字节确定，不使用随进程变化的 Python 哈希。稳健性以文件预测保持率和式(2.25)报告，不重新拟合任何参数。

正常类测试仅有 1 个文件，$R_N$ 只能取 0 或 1，无法据此稳定估计总体正常类召回。报告宏 F1、平衡准确率、每类召回、混淆矩阵、逐文件分数和聚合规则差异，不提供虚假的精确置信概率或把窗口当独立重复计算区间。

## 8. 完整求解过程与代码对应

1. **场景抽象**：读取问题一两套未标准化窗口特征及固定源域文件划分；把文件设为分组和评价单位。
2. **变量定义**：依据式(2.01)—(2.07)在每个拟合折内重新选择特征并拟合文件等权尺度。
3. **模型构建**：依据式(2.08)—(2.16)生成三类可比权重，显式训练四个一对其余 SVM。
4. **数据适配**：依据式(2.17)—(2.20)把窗口分数还原为文件决策和一致性指标。
5. **求解迭代**：依据式(2.21)—(2.27)先选择表示，再用开发文件重调并冻结模型。
6. **结果验证**：依据式(2.23)—(2.25)计算文件级指标，按式(2.28)—(2.30)完成核函数、权重、增益、噪声和聚合规则检验。

程序将按“数据读取—数据处理—参数计算—模型求解—结果检验—可视化—结果导出”执行。`utils/q2_model.py` 实现式(2.01)—(2.30)的数值函数，`问题二_求解.py` 负责两阶段冻结、真实运行、图表和导出。SVC 使用容差 $10^{-3}$、最多 200000 次迭代；若任一二分类器 `fit_status_` 非零或出现收敛警告，程序停止而不输出该组结果。模型参数、选中特征、尺度、支持向量与系数保存为可复用文件，供问题三在用户确认后调用。

## 9. 预期输出与停止条件

本问应得到：选定表示路线；主模型与三项对照的文件级验证和测试指标；测试混淆矩阵、逐文件类别分数、正常类明细、支持向量与计算时间；增益、噪声和聚合规则压力试验；模型状态及复现清单。双重均衡只有在文件级指标和正常类表现的取舍合理时才被称为有效改进；若消融不支持提升，仍保留它作为预先指定主模型并如实报告结果。

本问完成并提交 GitHub 后停止，等待用户确认，再进入问题三。目标域 A—P 的类别标定、CORAL、模型融合和非平衡最优传输均不在本问执行。
