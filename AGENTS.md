# 项目协作与论文写作规则

本文件是本项目的统一 Agent 入口，适用于当前目录及其子目录。项目主题为 2025 年中国研究生数学建模竞赛 E 题“高速列车轴承智能故障诊断问题”。

## 1. 工作目录与事实来源

- `PROJECT_ROOT` 为本文件所在目录；数学建模的 `SKILL_ROOT` 为 `math-modeling-skill/`，使用根目录版本，不另用其 `dsh-plugin/` 内的重复副本。
- 题目原文、原始数据和实际运行记录构成研究事实依据。发现冲突时回到原始证据核对，并同步修订受影响产物。
- 接手任务时先核实当前阶段。区分“拟采用的方法”“已运行的实验”和“已有证据支持的结论”，不能把方案描述改写为实验结果。
- 输入附件、`数据集/` 和第三方技能原件保持只读；研究产物写入项目目录，临时依赖与检查记录放入 `.analysis-work/`。技能升级作为独立维护任务处理。

## 2. 本地技能入口与分工

开始相应任务前，完整读取对应 `SKILL.md`，再按其路由读取必要参考。说明本次使用的技能及用途；路径存在不代表已经读取或执行。

| 技能 | 本地入口 | 职责 |
|---|---|---|
| `math-modeling` | [`math-modeling-skill/SKILL.md`](math-modeling-skill/SKILL.md) | 阶段调度、模型与代码、证据核验、论文交付及质量门禁 |
| `nature-writing` | [`skills/nature-writing/SKILL.md`](skills/nature-writing/SKILL.md) | 章节任务、论证结构、证据安排和段落组织 |
| `nature-polishing` | [`skills/nature-polishing/SKILL.md`](skills/nature-polishing/SKILL.md) | 学术表达、段落衔接、信息密度和结论强度 |
| `humanizer-zh-academic` | [`skills/humanizer-zh-academic/SKILL.md`](skills/humanizer-zh-academic/SKILL.md) | 中文模板句、机械衔接和空泛高频措辞的清理 |
| `stss` | [`skills/stss/SKILL.md`](skills/stss/SKILL.md) | 研究贡献、方案与结论中的防御性、自我辩解和重复保留语审查 |
| `nature-shared` | [`skills/nature-shared/SKILL.md`](skills/nature-shared/SKILL.md) | 两个 Nature 技能的共享依赖，按引用加载，不单独发起流程 |

任务涉及本项目论文、报告或其他研究文字的起草、改写、润色和结构调整时，四个写作技能共同参与；参与深度与修改范围匹配。局部修改可由 `nature-writing` 仅检查段落职责，由 `stss` 仅检查相关表达，无问题时保留原文，不要求四轮全文重写。纯数据查看、代码、环境配置或 Git 操作不触发论文写作流程。

建模分析读取 `math-modeling-skill/references/roles/建模手/SKILL.md`；求解与出图读取 `math-modeling-skill/references/roles/编程手/SKILL.md`；论文写作读取 `math-modeling-skill/references/roles/论文手/SKILL.md`。建模分析报告沿用建模手的交付约定，不因文字润色切换成完整论文任务。

## 3. 写作协作顺序

1. **确定任务与证据**：由数学建模技能明确当前阶段、修改范围、目标章节、模型假设、数据来源、真实结果和相关图表；先读取已有术语台账，新增或歧义项才补充确认。
2. **组织论证**：由 `nature-writing` 安排“研究问题—方法选择—证据—解释”的关系，明确每段的任务。局部润色沿用已确定的论证，不扩展用户未要求的研究范围。
3. **改善表达**：由 `nature-polishing` 调整信息顺序、句式与衔接，使论断强度和证据强度一致。
4. **整理中文**：由 `humanizer-zh-academic` 清理模板化表达，保持学术语体、专业术语及原意。
5. **检查保留条件**：由 `stss` 删除无信息的自我保护式说明；影响研究解释、适用范围、数据划分或安全判断的条件，精确写在对应结论附近。
6. **核对与交付**：按数学建模当前角色核对公式、数值、单位、图表、引用和结果一致性，执行适用的完成门禁，再按用户要求交付。

完整建模流程继续执行 `M1/P1/P2/W1/W2` 阶段门禁，详见 `math-modeling-skill/references/Subagent调度.md`。写作技能不替代独立质检，也不以语言质量证明模型有效。单阶段任务执行相应门禁；缺少关键结果时先完成有证据支撑的内容，说明待补项。现有验收结论只对其实际审查范围有效，实质修改后按原技能要求复验。

## 4. 中文建模论文的适配规则

- 默认用中文，优先说明数据、方法和模型能够完成什么、为何采用以及优势的证据。少用“不能怎样”“避免什么”等重复否定或防御性表述；必要假设、统计不确定性和适用条件完整保留。
- Nature 技能的原始语言分支仅有 `en` 和 `zh-to-en`。本项目保留中文时，采用其通用结构与表达原则，不触发中译英分支，也不虚构一个 `zh` 配置值。只有用户明确要求英文时才使用相应语言分支。
- 两个 Nature 技能均先读取各自 `manifest.yaml` 的 `always_load`，按任务选择章节和论文类型参考，目标默认为 `generic`。结果组织和讨论写作按清单加载对应共享参考；不因技能名称含 Nature 就套用该期刊篇幅或投稿格式。
- 竞赛格式、篇幅与提交要求以目标届次的官方规则和用户确认的任务约定为准；未核验的要求标明待核验。Nature 特定期刊规则只用于用户明确指定的投稿任务。
- `humanizer-zh-academic` 用于提高可读性，不作为 AI 检测工具。保留真实的研究细节，不为“人味”添加未提供的访谈、个人感受、意外发现、故意错误或口语噪声；不承诺检测分数或通过率。其词频和句式建议不能压过专业表达与证据需要。
- 润色保持数值、样本量、公式、统计含义、因果方向、引文归属和结论范围不变。需要实质修正时列出原始依据，回到相应建模或编程阶段处理。
- 输出服从用户要求：需要正文时交付正文，需要对照表时交付对照表。技能的默认附加说明可合并到简短修改摘要；内部风险评分、审查台账和自检过程不混入论文正文。已明确要求改写或润色时直接执行，无需为常规措辞调整再次等待确认。

## 5. 术语与原文审阅

- 首次使用专业缩写时给出全称；同一概念使用同一名称、符号和单位，保持正文、图题、表格与公式一致，勿用近义词替换来制造语言变化。
- 本项目重点区分正常状态与故障类别 `N/OR/IR/B`、目标域文件编号与类别编码、基座 BA 通道与平衡准确率，以及自旋 BSF 与题面滚动体故障频率；具体定义以题目原文和数据字段为准。
- 截图中“主线交互”等驾驶场景词语属于其他课题，不移植到轴承诊断。沿用其原则：根据实际语境写明故障部位、工况、观测通道、源域/目标域及预测对象。
- 文章截图仅用于定位问题。涉及文章修改、术语解释或结构判断时，回到对应原始 Word 文件，结合完整章节、前后段落、表格、公式、图注及批注审阅。原稿为 Markdown 或 LaTeX 时审阅对应源文件及关联材料；原稿缺失时请求提供，并将可给出的意见限定为截图可见范围。

## 6. 文档工具与交付范围

- 涉及 DOCX 时，四个写作技能之外，还须读取本地 `math-modeling-skill/tools/docx/SKILL.md`；当前会话提供 `documents` 技能时一并使用，负责原稿读取、编辑、排版和最终检查。通过会话技能目录定位它，不在项目规则中写死某台电脑的插件缓存路径。
- DOCX 转换、结构检查和渲染遵循本地建模技能的脚本与门禁；LaTeX、PDF、表格和论文检索按数学建模入口加载对应工具。工具缺失时明确说明已完成部分和未验证部分。
- 只改当前请求涉及的内容，保留无关修改。Git 提交、推送、全局安装或额外插件安装以用户本次明确授权为准；下载技能不自动授权其仓库中的 hooks、后台服务或外部数据上传。

来源版本、许可证和项目级发现入口见 [`skills/README.md`](skills/README.md)。
