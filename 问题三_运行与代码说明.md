# 问题三：运行与代码对应说明

## 1. 程序完成的步骤

`问题三_求解.py` 按“数据读取→数据处理→参数计算→模型求解→结果检验→可视化→结果导出”的顺序运行。输入是问题一已经冻结的 `模型适配数据集/q1/files.csv`、`window_features_all.csv` 与 `results/q1/summary.json`；540/660 rpm 扰动从 `数据集/目标域数据集/` 中的 16 个原 MAT 文件重新计算。原始输入保持只读。

| 步骤 | 实现 | 数学模型对应 |
|---|---|---|
| 读取与核对 | `read_inputs` 检查 132 文件、996 窗、70/23/23 源划分及目标无标签 | 固定样本集合和实验边界 |
| 数据处理 | `file_median_table` 对九项特征按文件聚合；`fit_robust_scale` 仅在拟合侧源文件求中位数/IQR | 式(3.01)—(3.05) |
| 参数计算 | `shrunk_covariance` 与 `fit_alignment` 按 3+6 维机理块计算收缩协方差、均值和映射 | 式(3.06)—(3.09) |
| 模型求解 | `proxy_metrics` 在两个源域代理方向比较四个 α；`fit_transfer` 训练 500 棵树并预测 A—P | 式(3.10)—(3.12) |
| 结果检验 | 23 个固定源测试文件的两条路线；目标留一、540/660 rpm 重算、无对齐分支对照 | 式(3.13)—(3.14) |
| 可视化 | `utils/q3_plots.py` 绘制原始结构、代理和对齐过程、预测分数与稳定性、流程图 | `figures/q3/` |
| 导出 | CSV、逐文件报告、树状态 JSON/NPZ、模型接口和哈希清单 | `results/q3/` |

`utils/q3_transfer.py` 承担式(3.03)—(3.12)的数值实现；`utils/q3_outputs.py` 输出模型和带证据边界的报告；`utils/q3_manifest.py` 记录输入、环境及确定性结果哈希。九项特征名、两个块、α 候选和树参数集中在 `configs/q3.json`，不在文件名中猜测目标标签。

## 2. 可复现运行

本机使用 Python 3.12.14、NumPy 2.3.5、pandas 2.2.3、SciPy 1.18.1、scikit-learn 1.8.0、Matplotlib 3.10.9；精确依赖版本和输入文件哈希见 `results/q3/复现清单.json`。在项目根目录执行：

```bash
LOKY_MAX_CPU_COUNT=8 PYTHONDONTWRITEBYTECODE=1 MPLCONFIGDIR=.analysis-work/matplotlib XDG_CACHE_HOME=.analysis-work/font-cache PYTHONPATH=.analysis-work/q2-deps:.analysis-work/q1-deps:.analysis-work/python-deps:. /Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 问题三_求解.py --overwrite
```

机器上的依赖目录可能不同；使用其他符合版本的 Python 时，保留 `PYTHONPATH` 指向运行所需的依赖即可。`--smoke --output-root .analysis-work/q3-smoke` 只跑 8 个源拟合文件、4 个目标文件，检验数据读取至预测导出的最短链路。完整运行生成 16 个目标文件预测、转速扰动、图件和模型状态。输出目录若已有非本程序生成的文件，程序会拒绝覆盖。

单元检查：

```bash
PYTHONPATH=.analysis-work/q2-deps:.analysis-work/q1-deps:.analysis-work/python-deps:. /Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest discover -s tests -v
```

图件检查：

```bash
/Users/mariozz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 math-modeling-skill/references/roles/编程手/scripts/figure_audit.py figures/q3 --questions q3 --strict
```

## 3. 结果文件的解释

`target_file_labels.csv` 每文件一行，`prediction` 为四类中的模型标签，`score_*` 是未经概率校准的树投票比例，`margin` 是前两类分数差。`review_flag` 在无对齐、目标留一、任一转速情景的预测与正式模型不同的情况下为真；它表示应复核，不表示已有真值证实出错。`source_test_predictions.csv` 同时保留源基线及同映射对齐模型的 23 文件判断，不能把两个模型的行数相加当成 46 个测试文件。

`alignment_diagnostics.csv` 记录两个特征块对齐前后的均值和经验协方差距离，仅检验特征分布是否移动。`model/model_interface.json` 给出源域缩放统计量、α 和两块线性映射；`model/trees.json` 与 `trees.npz` 包含可回读的 500 棵树状态。程序使用独立树节点回读接口复算 16 个目标文件，最大分数差为 0。完整输入哈希、结果哈希及唯一复现命令见复现清单。

目标域只有 16 个无标签文件；其预测、票差、留一和转速分析都不构成目标准确率。源域九维迁移模型的固定测试为 20/23，不能沿用问题二 90 维源域主线的 22/23 作为本问成绩。问题一的特征选择和本问九维取舍已使用开发信息，固定源测试也在前期研究中查看过，因此报告中的性能属于当前研究条件下的实测对照。
